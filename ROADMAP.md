# CAIRN — ROADMAP

**This file is the single source of truth for status and sequencing.** Architecture, invariants, and rationale live in `CAIRN_build_brief.md`; this file references it by `§` rather than duplicating it. When the two disagree, the brief wins on *design* and this file wins on *what to do next*.

**Cardinal rule:** Ground or abstain — never invent. (Invariants I1–I6: see brief §0.) In the patent domain this sharpens to **locate & evidence, never adjudicate** (D10).

**Two tracks.** *EDGAR 10-K* (M0–M5) is the architecture-proving **reference build**. The first paying engagement is a **patent refresh-and-update** (D10), tracked under **Patent Engagement** below — a specialization of the same corpus-agnostic engine, not a rewrite. The engine is built EDGAR-first (it's shared); the patent domain pack layers on top once client intake resolves the open decisions.

### Status legend
`TODO` · `WIP` · `BLOCKED` · `DONE` — task checkboxes mirror this (`- [ ]` / `- [x]`; `- [~]` = partial / first-cut).

### ▶ Current focus
**The console is complete** (RT-10/RT-2/RT-7, D48–D52): seven panes — corpus · locate · evidence · drawings · adjudicate · mark-a-sheet · record — behind one frame whose header carries the corpus, its calibration state and the judgment count above every pane. The reviewer can now **write back**: a ranked review queue, one-click judgments, and drag-a-box to assert what OCR missed, all appended to a hash-chained record a machine cannot displace. **Build it:** `scripts/build_console.py`; **serve it:** `scripts/serve_console.py --reviewer "…" --on YYYY-MM-DD`. **▶ Next (unblocked, non-billed):** **PE-2** (the four structural §112 checks — domain depth for the live engagement, now with a surface to show it in), or **VER-1**'s remaining operations. **Open defect:** SIGBUS in `tiled_search` on the odd-geometry sheet. **Awaiting Julian:** trademark clearance on *Cairn*; intake Q13/Q16; one hour of blind annotation (now easy — the Mark-a-sheet pane).

> **▶ Immediate (D62–D65, 2026-08-14).** Four defects found by reviewing the running
> console, all on the evidence path, all now fixed with standing tests. The consequence
> to act on: **D65 retroactively invalidates all 23 recorded answers for US5447630A** —
> their bindings carry no corpus hash and their offsets no longer resolve, so the
> engagement's evidence record is empty until the questions are **re-run against the
> current store**. That re-run is the next task. Two gaps stay open: the audit record
> persists a bare `ok` with no per-atom verdicts (so a past failure cannot be attributed
> from the log alone), and the Adjudicate pane still shows no image crop, which makes
> "is 20 really drawn here?" unanswerable at the point of judgment.

> **Done:** the evidence view is now a **parallel full-document view** — the whole
> canonical document on the left with cited ranges highlighted in place, click a
> figure to scroll to it. `python scripts/build_evidence_view.py` → `evidence_view.html`.

> **Deferred, with a dependency:** **M2-T6 (Layer-E)** drives the *real agent* over
> the tools, which needs the **MCP interface from M4** — so it lands around **M4-T4**
> (agent loop end-to-end), not before. **M2-T8** (constraint coverage) likewise
> finishes when the live agent emits the question frame (M4). Both are tracked and
> non-blocking.
>
> **Review the build yourself:** `python scripts/build_evidence_view.py` → open
> `evidence_view.html` (grounded answers, click-to-source, coverage, abstentions).

> **M2 builds the shared engine's grounding tools** (`verify`, `check_support`),
> which both EDGAR and the patent track reuse. Carry the patent seams (D10): the
> abstention path generalizes to the *adjudication refusal class*; the atom resolver
> to patent atoms (claim terms, numerals, dates). Retrieval recall tuning (the
> G009 lexical gap) also lands here, where the Layer-0 recall gate drives it.

> **Working mode:** single primary agent develops directly on `main` (no PR-per-task gate
> in this repo). CI still runs on every push to `main`; a red gate or a violated invariant
> is treated as not-done and must be fixed before advancing focus.

---

## How the agent uses this file

1. Read **Current focus**. Take the topmost unchecked task in that milestone.
2. Branch per the task's `branch:` field. Implement to the task's **acceptance criteria (AC)**.
3. Open a PR. The **Layer-0 deterministic component evals (brief §3)** run as required CI. **No merge on a red gate or a violated invariant (I1–I6).** (Layer-E agent evals run periodically, not per-PR.)
4. On green: merge, check the box, append a line to the **Changelog**, and advance **Current focus** to the next task.
5. A milestone is `DONE` only when its **Gate** passes. Do not begin the next milestone until then.
6. Never start anything under **Backlog (v2)**. If a v1 task tempts you toward esoterica, see brief §9 (the trap): ambition goes into the oracle, nowhere else.

---

## Working agreements

- **Branch naming:** `feat/<milestone>-<slug>`, `fix/…`, `chore/…`. One subsystem per branch.
- **PR rules:** small, single-purpose, oracle-gated. Every PR states which invariants it touches and how its tests cover them.
- **Invariant tests are CI, not afterthoughts.** I3/I4/I5/I6 each have a standing test that runs on every PR once introduced (M1 onward).
- **Determinism:** anything on the evidence path runs seeded / temperature 0 (I6). Non-determinism there is a bug, not a tuning knob.
- **The oracle is sacred.** Don't weaken a gate to make a PR pass. If a gate is wrong, change it in its own PR with rationale logged in Decisions.

---

## Decisions log (append-only)

| ID | Date | Decision | Rationale | Revisit when |
|----|------|----------|-----------|--------------|
| D22 | 2026-06-30 | **Refuse-to-adjudicate is a first-class fifth outcome (extends D16).** `answer / abstain / correction / partial / refuse` — a refusal is **not** an abstention: the evidence is often *present*; what's declined is the **legal conclusion** (novelty/validity/infringement/claim construction — D10's cardinal rule, UPL boundary), while the located evidence is offered for professional review. Layer-E derives it from `expected_behavior: "refuse-to-adjudicate"` and scores it separately (`refusal_accuracy`; refusals no longer dilute `abstention_accuracy`); the evidence view renders it with its own badge/colour (orange `--refuse`); the loop docs name it. A `refuse` presents no conclusion → no `verify` record (its audit-log visibility lands with session outcomes / RT-1). **Truth contract bumped 1.0 → 1.1** (a strengthening → minor, per D21's monotonic rule — its first exercise); replay now **preserves recorded provenance stamps verbatim**, so 1.0-era and pre-provenance records still replay byte-identically (TC-2 proven across a version bump). | Surfaced by the US5447630A review: rendering a refusal as `abstain` hides the engagement's most important behavior — the boundary is a *deliberate act*, not an absence, and must be visible and separately measurable. | Formalize how a refusal is *logged* (a structured refusal record) with RT-1/session outcomes; the PE-5 negative item anchors it in Layer-E. |
| D23 | 2026-07-08 | **Patent golden set ratified — labels frozen, append-only (oracle-is-sacred, mechanically enforced).** The 25-item `golden_patent.json` (US5447630A) is ratified by the patent-domain authority via the ratification worksheet. Three judgment calls settled: **(A)** P017 (the maintenance-schedule trap) **stays `abstain`** — the grounded-negative behavior is *not* adopted for v1 (it would require loosening the M2-T8 coverage rule for negatives, a truth-contract change); the coverage gate already lands P017 on abstain. **(B)** the PE-4 front-matter flips are confirmed — P013 (filing date) & P014 (inventors) are answerable, P015 (assignee) stays `abstain` (no assignee on this front page). **(C)** P007 stays `answer` — the label is correct (claim 15 recites the variable-speed fan); the agent's repeated miss is a decoy weakness that must keep counting against the agent, not be labeled away. Frozen by a content hash: `layer_e.ratified_manifest_sha256` over the ratified item ids, stamped in the file's `ratified` block and checked by a **Layer-0 CI test** (`test_ratified_labels_are_frozen`) — editing or deleting a frozen item fails the build; adding a new item does not (append-only). Re-ratifying is the only sanctioned change path (new decision + re-stamp). | The oracle must be sacred *by construction*, not by honor: a label quietly changed to flatter a score is the exact failure the golden set exists to prevent. A CI-enforced content hash makes that edit fail loudly while still allowing the set to grow. | Extends to `golden_seed.json` (EDGAR) — stamp it the same way when its labels settle; the primitive is corpus-agnostic. |
| D24 | 2026-07-17 | **Denial/correction cue-scan adopted as a Layer-E MEASUREMENT + non-blocking advisory flag (provability research Rung 1) — BUILT.** `cairn.cues.denial_cue_hits`: closed evaluative-denial set (`incorrect, erroneous, mistaken, overstated, restated, superseded, corrected, revalued`), span-local window 160 chars, attribution verbs (`reported/claimed/stated/alleged`) **excluded** (they are the corpora's assertion vocabulary). Evidence view renders a non-blocking "⚠ denial/correction cue in cited span — review (advisory, D24)" line; worded as *cue near citation*, never "refuted". `scripts/census_denial_cues.py` measures the corpus base rate. **CENSUS RESULT (the number this rung exists to learn):** EDGAR AAPL-10K → 5 cue occurrences, **all 5 benign boilerplate** ("amended and restated" plans/bylaws/articles near dates), 0 true refutations; US5447630A → **0 cues anywhere**. `tests/test_cues.py` (6: positive control, benign-span false-positive boundary, cue-less ceiling pinned as MUST-miss, window locality, closed-set membership) + 2 evidence-view tests. | The research's verdict: the only deterministically visible refutation slice is the lexically-marked span-local one; a measurement is the cheapest way to learn the real base rate before spending a gate. Deterministic, stdlib, I6-clean, gates nothing → no contract bump. | Cue-set extensions require census evidence, not intuition (the set being closed is the false-positive story). |
| D25 | 2026-07-17 | **Cue-scan promotion to a Layer-0 abstain-trigger: DEPRIORITIZED ON EVIDENCE (the D24 census came back empty).** On both real corpora the gate would have fired **only false positives** (5/5 benign "restated" boilerplate on EDGAR; nothing at all on the patent). Recorded as a **negative result**, per the research's own protocol — the advisory flag (D24) stays; the gate is not built. | A gate whose measured catch rate is zero and whose only firings are false positives is pure answer-rate cost; shipping it would be rigor theatre. "Never wrong" is not served by blocking nothing but good answers. | Re-run the census on each NEW engagement corpus (one command); if a corpus shows true cue-marked refutations near cited figures, promote per the research's D25 protocol (benign-span FP gate + zero golden flips + ratchet, minor bump). |
| D26 | 2026-07-17 | **Veridicality/NLI entailment stays Layer-E, billed, never a runtime gate** — adopted as standing posture (it is already CAIRN's de-facto architecture: `layer_e.judge_entailment` / `judge_refutes_premise`). Its role: quantify the cue-less refutation residual D24/D25 cannot reach. | A runtime NLI call breaks I6 and puts AI on the deciding path; measured ~80% ceiling with its dominant error class (numeric insensitivity) landing exactly on CAIRN's flagship class. A yardstick for the permanent hole, not a fix. | Runtime use = **major** contract bump + full ratchet evidence; out of scope for v1. |
| D27 | 2026-07-17 | **Documentary: Gödel–Löb provability logic and general FOL→prover entailment are recorded inapplicable / out of scope.** GL is the modal logic of formal-arithmetic provability — "does this passage assert Q" never reduces to it (category error). General FOL entailment is undecidable; its decidable fragments cannot express the numeric-revaluation reasoning, and the NL→logic translation step is itself the model-driven, silently-failing part. | Closes the two seductive big-gun routes with reasons earned by 42 hostile reviews (docs/provability_research.md §6), so they are never re-litigated ad hoc. | New evidence of a decidable fragment covering the numeric-refutation class faithfully and model-free (none in current literature). |
| D28 | 2026-07-17 | **Drawing-sheet OCR adopted as an INGESTION-TIME step behind a frozen, hashed manifest** (Julian's explicit call: a reliable pre-trained OCR with sufficient warnings). Engine: **Apple Vision** (on-device, no network, `[ocr]` optional extra — macOS-only, never a runtime/CI dep). `scripts/ocr_patent_figures.py` runs ONCE per engagement → `figures/ocr_manifest.json` (per-observation confidence, raw source text, OS/Vision provenance, an explicit not-100%-reliable warning, image sha256s); **runtime and CI are deterministic over the manifest** (`cairn.figures_map` — pure functions), mirroring I3's ingest-hash pattern: OCR variance is quarantined at ingestion, never on the evidence path (I6 preserved). FIG→sheet assignment accepts an OCR label only if it names a KNOWN text-side figure (a garbled 'FIG.A' cannot invent figure A) + single-gap **elimination** flagged as its own method; numeral sightings carry conf + raw text; all OCR-derived facts render as *located by OCR*, never citations (D21). | Prototype-first validated the bet: Vision read the 1995 scans near-human (FIG labels, 'Sheet N of 8' self-ids, numerals at conf 1.0); the alternative (torch OCR stacks) fails the boring-dependency rule. Freezing at ingestion converts a non-deterministic tool into deterministic input — same move as content-hashing the corpus. | OS upgrades may change Vision output: manifest is frozen + hashed, so drift is visible (re-OCR is a deliberate re-ingest, new manifest hash). Cross-engine corroboration (e.g. a second OCR) if an engagement demands higher recall. |
| D29 | 2026-07-22 | **Multi-engine OCR with complementary recall** (Julian: non-Mac systems + redundancy). The ingestion OCR is now an **engine seam**: Vision (darwin), **Tesseract** (system binary, all platforms), **RapidOCR** (PaddleOCR models on ONNX — pip-only, no torch, all platforms); `--engines auto` unions all available. All engines normalize to ONE observation format (`figures_map` pure converters, CI-tested); same-spot same-label reads across engines **merge with union'd `engines` provenance** (corroboration recorded, not noise). Measured on US5447630A/FIG 2 — the engines' blind spots are DISJOINT: Vision reads view-marker B but is totally blind to A (every API, every scale); Tesseract reads A (psm-11 edge-band, conf 0.96) but misses B; RapidOCR has best whole-image recall (24/30 vs Vision ~18) but reads neither letter. Union first-pass recall ~doubled (guided recoveries needed: 32→11); FIG 4's label now reads directly (no elimination); **recited-not-drawn emptied**. New guards earned en route: prose-fragment rejection ('FINAL PURIFIED EFFLUENT. 1' is annotation text, not numeral 1); Vision tiling guarded darwin-only; tesseract edge-band marker rescue (markers sit at sheet edges). Cloud OCR APIs REJECTED as default: engagement drawings are client-confidential and never leave the machine. | Redundancy is not belt-and-suspenders here — the engines fail DIFFERENTLY, so the union is strictly stronger than any single engine, and cross-engine agreement is a real evidence tier. | Per-engagement: re-run the census if an engine version changes (manifest records versions); consider Windows.Media.Ocr as a native adapter if a Windows ingest machine materializes. |
| D30 | 2026-07-25 | **Sub-10 reference numerals are recited but never LOCATED on the sheets** (Julian: "the OCR for figures lower than ten are all nonsense" — with a screenshot of the 'numeral 2' box sitting on the word GRAYWATER). A **sighting policy, not an extraction policy**: the text side still recites them with element and figure ('2 · toilet · FIG. 1'), which is reliable; the IMAGE side does not attempt them, because a one- or two-stroke glyph beside a leader line is indistinguishable from line art to every engine, and the sightings produced were garbage. `figures_map.is_locatable` gates OCR capture, guided prediction, boxes, and page tags; the view renders such labels as **text-only**; and their absence is NOT counted as `recited_not_drawn` (we abstained on position by policy, not by failure). | Locating nothing is honest; boxing the wrong ink is not. This is the same discipline as the rest of the system — assert what is grounded (the label, its element, its figure) and abstain on what is not (its position) — applied to the image side. Note it is NOT the ≥10 floor L0006 warned about: that floor deleted labels from the TEXT model, where sub-10 numerals are real and correctly named; this one only declines to claim a position. | A future engine (or per-figure crops) that reads small isolated glyphs reliably on a known-positive test; then lower the threshold with evidence, per corpus. |
| D56 | 2026-07-28 | **MCP Apps assessed and deferred, with the conflict named.** Julian asked whether MCP Apps is the right format. It is a real, official extension (core spec **2026-07-28**; apps dialect 2026-01-26) that renders sandboxed interactive HTML inline in a conversation, and it would be a **strong fit for Locate and the review queue** — adopting it would let us **delete the loopback server entirely** (D49), which we added against the hardening brief's advice that a local server's *absence* of a listener is the win. Deferred anyway. | **The conflict is architectural, not cosmetic.** In MCP Apps the app calls **tools** — and `/adjudicate` is deliberately *not* a tool (D50), precisely so the agent cannot call it and a machine cannot manufacture human judgments. Porting adjudication to a tool call would reintroduce exactly what D47/D50 exist to prevent. **Elicitation looks like the right primitive** — server-initiated, answered by the human through the client, with the client required to identify the asking server and allow decline — but form mode is restricted to *"flat objects with primitive properties only"* while our target is nested (page/numeral/x/y/w/h), and it cannot express drag-a-box at all. That is design work, not a swap. Separately, the **Record of Inquiry must NOT move**: MCP Apps' stated advantage is living inside the conversation, which is precisely wrong for an artifact a professional signs, archives, and opens in five years. | Revisit when (a) the adjudication write path has a designed home in elicitation or an app-to-host channel that is not a model-callable tool, and (b) the engagement is not live — retooling the surface mid-engagement is risk without payoff. |
| D67 | 2026-08-15 | **Abstentions and refusals now reach the evidence record; the US5447630A log was cleared and re-run.** The view rendered only `verify(ok)` records, so every abstention was dropped, and `verify`'s outcome enum had no `refuse` — the refusal could not be *logged at all*. Both fixed: `refuse` is a loggable outcome that cites without presenting (new `_CITES` set — it lights its spans and lists what it found, but gets no conclusion card), and an `insufficient` support record renders as an abstain card with its closest spans. Old 54-record log archived, not deleted; six new questions run covering all five outcome classes (2 answer incl. a derived value, correction, partial, abstain, refuse). | The two outcomes that most distinguish Cairn from a chatbot were the two it could not show. A session where it correctly declined four of ten questions rendered as six answers and no trace of a decline — which reads as a tool that always answers, the exact opposite of the claim it makes. And "locate first, then decline" (D22) is only worth something if the located half survives to the page. | **Two recording gaps remain.** (1) A *reasoned* abstention — spans clear the floor but the agent judges they do not answer the question (D12's second mechanism) — has no representation in the log; only the deterministic kind does. (2) Concretely measured while choosing demo questions: "What is the manufacturing cost of the treatment system?" scores **9.17 → supported** on a corpus that never mentions cost. That is the non-separable floor (D53/D55) producing a false *supported*, and it is why (1) matters — on this corpus the agent's reasoning is doing most of the abstention work with nowhere to record it. |
| D66 | 2026-08-15 | **The adjudicate queue shows the sheet where it asks the question.** Each located row renders a zoomed crop of the drawing centred on the flagged mark, with the mark ringed and a zoom control; a row with no location says so instead of framing empty space. Queue 34 → 33 (numeral 20 is no longer falsely flagged, D64). | "Is 20 really drawn here?" is unanswerable from a description, and the queue asked exactly that while showing nothing — so the reviewer had to open the Drawings pane and find the spot by eye for every row, or judge the *description* of the evidence instead of the evidence. A judgment surface that does not show what is being judged degrades the judgment it records, and those records are the one thing in Cairn a machine may never write. | **The first version put the ring beside numeral 12 rather than on it**: it did the y-flip in the page's JavaScript, as if manifest `y` were top-down, when `y` is measured from the BOTTOM to the box's lower edge. Caught by checking against three numerals with 3-engine corroboration before shipping — `box_to_display`'s own docstring warns about "one tested function and one hopeful line of JavaScript", and the hopeful line was written anyway. The conversion now happens in Python, through the tested pair. |
| D65 | 2026-08-14 | **I3 was opt-in, and unreachable: the drift check ran only `if binding.content_hash is not None`, and no read tool ever returned a hash.** Supplied 0 times in 61 bindings on US5447630A. `get_span`/`get_document` now return the hash they read against; an unhashed binding resolves `unhashed` and fails. Contract **2.1** (strengthening → minor). | A guarantee that nothing can satisfy is not a weak guarantee, it is an absent one — the field existed on `AtomBinding` from the first commit (M2-T1) and could never be populated through the tool surface, so I3's drift arm never once ran on real work. When the corpus moved, all 61 offsets went stale in silence and the console presented genuine quotes boxed around unrelated text, which reads as fabrication. A check that cannot run is a check that failed. | **Retroactively invalidates all 23 recorded answers for US5447630A** — correctly; they do not resolve. Julian ratified the wipe. The engagement's questions need re-running against the current store. Also open: the audit record persists a bare `ok` and no per-atom verdicts, so a past failure still cannot be attributed from the log alone. |
| D64 | 2026-08-14 | **The pointer construction "…as shown at 20" was not a recitation.** `reference_numerals` reads the words before a numeral as its element; there they are "shown at"/"at", which `_NOT_ELEMENT` rejects. A second pass recognises the lead-in and recovers the subject across "as" and the appositive comma. Recovers 20 and 43 on US5447630A; `located-not-recited` 21 → 19. | The cost was not a missing legend entry but a **fabricated discrepancy**: the reconciliation reported "numeral 20 appears on sheet 2 but is never recited in the specification — review" against a numeral plainly recited at offset 12108. On a locate-and-evidence tool a false finding is the expensive kind of wrong, because the reviewer spends real time on it. Found by Julian noticing that Mark-a-sheet and Drawings disagreed — two panes reading different sources with no reconciliation shown. | `LTM`/`STM` are alphabetic labels in appositive form ("a solid treatment module, STM,") and remain unrecognised — supporting them means matching bare acronyms, which risks far more noise than it recovers. 19 located-not-recited remain, most of them OCR artefacts. |
| D63 | 2026-08-14 | **An answer whose live `verify` fails is withheld, not annotated.** The evidence view filtered on the *logged* `ok`, re-ran verify, printed "✕ verify" — and still drew the green ANSWER badge with highlights at the stale offsets. The gate now lives in `render_evidence_view`, so a caller constructing `Interaction`s directly cannot bypass it. The withheld card names the failing atom, its status, and **where the claimed literal really occurs**. | The loop rule ("present only if `ok` AND `coverage.complete`") was enforced in the agent loop and nowhere in the surface that renders the result, so the strongest statement Cairn makes about itself was made by a component that did not check it. Highlighting at an unresolvable offset is worse than not highlighting: it paints a box around unrelated text and lends it the authority of a citation. | The audit record still keeps no per-atom verdicts, so the *view* can diagnose a failure but the *log* cannot. That asymmetry is the remaining half of D65. |
| D62 | 2026-08-14 | **A verify record's question comes from the record, never from a neighbour.** The evidence view carried the last `check_support` query forward; on the first engagement that was wrong on **all 23** presented records — 18 showed the BM25 query in place of the question, 3 attached an answer to an unrelated earlier question. Now taken from `frame.question` (D13); a frameless pre-D13 record shows the nearest query **labelled as a query**. | A variable that survives the loop iteration outlives the record it came from. Proximity in a log is not attribution, and a retrieval string is not a question — presenting one as the other misrepresents the interaction on a surface a client signs. The cross-topic cases are the dangerous ones: nothing on the card revealed that the question above it belonged to a different interaction. | — |
| D61 | 2026-08-09 | **Layer-E run 3 — the first to grade the citation, and the gap is 21 points.** Julian authorised the billed run. `decision_accuracy` **0.95**, `evidence_accuracy` **0.7857**, **`decision_minus_evidence` = 0.2143**, with 6 of 20 items unscoreable (abstain/refuse have no gold span to match). Three evidence failures: **G002** (commercial paper), **G007** and **G019** (both term debt). Entailment 0.93, Brier 0.069. | This is the number RT-8 existed to produce and no previous run could report. Under label-only scoring these three items pass — the decision was right, the tool presented, the expected class matched — and the citation did not overlap the gold span. FEVER's published 50.91% → 31.87% is the same shape; ours is 0.95 → 0.79 on 14 scoreable items. **What is NOT yet established: whether the three misses are genuine.** A first attempt to inspect them dumped every citation in the region rather than the segment per question, so it showed nothing; the possibility that the scorer is strict where a figure legitimately recurs elsewhere in the filing has not been ruled out. The headline gap is what the scorer reports, not yet what has been confirmed. | Per-item segment inspection of G002/G007/G019 — the cheap next step, and the one that decides whether 21 points is a finding about the agent or about the scorer. Also: 6 unscoreable items means abstain/refuse evidence (D12 `closest`, D22 locate-first) is still ungraded, which RT-8 flagged and did not close. |
| D60 | 2026-08-09 | **Contract 2.0 — I6 relaxed to "no model on the DECIDING path", ratified by Julian.** The literal form (*"no runtime model calls"*) was a proxy for the guarantee, not the guarantee, and it forbade embedding-based retrieval for a reason that does not apply to it. Amended: retrieval **may** embed a query with a pinned model, because retrieval *proposes and never decides*. Everything downstream — the support floor, `verify`'s span resolution and hash check, coverage, the outcome classes — remains model-free, then and now. Two conditions: **document embeddings are ingestion-time and frozen** (exactly OCR's discipline, D28), and **the model is pinned and recorded in provenance**, because an embedding model has the same version-drift problem D45 found in OCR. RAG Decision 2 answered as **move (A)**: the `RetrievalBackend` contract is now written out — four obligations, tested against the one backend that exists — and **nothing is built**. Decision 3's trigger: the first multi-document engagement where BM25 recall is measured short. | **Versioned as a major bump because it is genuinely a weakening**, even though no guarantee about *output* moved. The old form was checkable by grep; the new one takes judgment about what counts as deciding, and that is a real reduction in how cheaply the claim can be audited. Calling it a strengthening because outputs are unaffected would be exactly the quiet weakening the monotonic rule exists to prevent. The asymmetry that makes it safe *here and nowhere else*: a wrong retrieval yields a worse answer or an abstention, never a wrong citation, because `verify` re-checks the span however it was found — pinned by a test. **Writing the contract immediately caught an over-statement in it:** obligation 4 said a backend must never return nothing, but returning nothing when no query term appears is *zero signal*, not a decision; returning nothing because a score looked low would be. The wording now draws that line. | Building an embedding backend (Decision 3's trigger); and whether frozen document embeddings need their own manifest hash, as the OCR manifest has. |
| D59 | 2026-08-09 | **VER-1: the verifiable-computation set widened, and a boundary written as a test.** Added `percent`, `min`, `max`, `average`, `count`, `round` to the numeric set, plus `date_delta` in a new `_DATE_OPS` category — dates dispatch **before** numeric parsing, because `to_number` rejects a date literal and would have failed the operand list first. `OPS_VERSION` → **2**; existing records keep `1` because the provenance stamp is preserved verbatim on replay (D21/TC-2). Two choices worth recording: `count` counts the operands the agent **cited** — a claim about the evidence assembled, not a search of the document, so an incomplete citation set gives a visibly wrong count; and `round` is identity, because the tolerance comparison already rounds the recomputation to the precision the agent wrote, and a second rounding rule would be one more thing to keep in step. | **A duration is not a term, and that is now enforced rather than asserted.** `date_delta` returns days and nothing else: patent term involves adjustments, extensions, terminal disclaimers and maintenance status, none of them arithmetic. `test_a_duration_is_not_a_term` fails if any adjudication-shaped operation (`patent_term`, `expiry`, `validity`, `novelty`, `infringement`, `scope`, `prior_art`) is ever added to the set — so D10's boundary is a gate, not a convention someone remembers. Every operation also has a paired reject test, because an operation that cannot fail is not verifying anything. **The date parser's own test caught a real bug**: truncating the month to three characters made "Septober 4, 2024" a valid September date. Month names are now matched whole. | Unit conversion, deliberately not attempted: factors are external constants, so each conversion must record the factor used from a vetted table rather than guessing — its own design with its own auditability requirement. |
| D58 | 2026-07-28 | **Antecedent basis measured, then declined — the census is the deliverable (PE-2).** §112(b) antecedent basis looks mechanical, which is why it keeps getting proposed: an element referred to as "the X" should have been introduced as "a X". Measured on both patents rather than assessed: **51% / 46%** of back-references flagged under whole-phrase matching, **28% / 24%** under head-noun matching (English noun phrases are head-final, so "a portable electronic device" introduces "the device"). Both walk the dependency chain. `scripts/census_antecedent_basis.py` makes the number reproducible. | The residue is not noise to be tuned away — it is **coordinated introduction**, which is ordinary drafting: claim 1 says *"separating an incoming flow into at least first and second components"*, introducing both elements once, plural, under one determiner, and then refers to *"said second component"* singly. A pattern matcher cannot pair those without coordination and number agreement. **Declining is the finding.** A flag here is defect-*shaped* — it reads as a §112(b) problem — so emitting it against a real element at 1-in-3 would be the tool adjudicating, badly, in exactly the way D10 forbids. That is worse than the check not existing, and it is the L0004 pattern: census the firing condition on real data, and when it floods, record the negative result rather than shipping the gate. | A parser that resolves coordination and number agreement (spaCy-class, which would put an NLP model on a Layer-0 path — its own decision). Term-usage consistency is the same class and remains deferred for the same reason. |
| D57 | 2026-07-28 | **Protocol statelessness: architecturally aligned, SDK-era behind.** The `2026-07-28` revision **removes the `initialize` handshake**: version, identity and capabilities ride as per-request `_meta`, and a modern server **MUST** implement `server/discover`. Measured, not assumed — our SDK reports `LATEST_PROTOCOL_VERSION = 2025-11-25`, has `InitializeRequest`, and has no `discover`. The spec calls `2025-11-25` and earlier **legacy**, and its compatibility matrix is blunt: **legacy server + modern client = fails**, with no fall-forward for the client. The `mcp>=1.0` pin was also unbounded while **2.0.0** has shipped, so a fresh install would silently pull the modern era into a handshake-era adapter; now `>=1.28,<2`. | **The good news is that statelessness is what I6 already demanded.** Cairn's tools are pure functions over a frozen corpus: no session accumulation, no per-connection state, the audit log append-only on disk rather than in memory. A fresh registry answers identically, and an intervening call does not change a later answer — both now pinned by a test. So the era migration is an adapter change, not an architecture change. **One thing genuinely needs thought:** RT-1's session markers are *our* concept written into the audit log at spawn (`CAIRN_SESSION_LABEL`). That is fine for stdio-per-spawn, but under a stateless HTTP transport where any request may hit any process, "which session was this" stops being answerable from process identity and would need to travel with the request. | The 2.x migration itself, which changes `mcp_server.py`'s API surface and needs `server/discover`; and the session-marker question above, before any move off stdio. A standing test fails if the SDK changes era, so this cannot drift in under a dependency bump. |
| D55 | 2026-07-28 | **A second patent corrects D53: most of the "non-separability" was an artifact of two golden questions.** Julian asked for a second-patent test. Built `golden_patent2.json` for US8046721B2 — a **separability probe, not a ratified oracle**, with every answerable topic verified by term count and every content-absent one verified at **zero** mentions. Result: overlap **−2.1**, non-separable but *mildly*. That made US5447630A's **−16.7** look anomalous, so I inspected its content-absent items: the two scoring **23.0** both quote *"US 5,447,630"*, and the passage they win on is the title line `United States Patent US 5,447,630 A`. Removing the identifier: 23.0 → 6.8 and 23.0 → **0.0**; the corpus's true gap is **−5.3**, not −16.7. | **Two findings, and the second is the useful one.** (a) The corrected picture: both patents are mildly non-separable (−5.3, −2.1) against EDGAR's clean **+9.2** — and EDGAR is 4× longer with 2.5× the vocabulary, so the driver looks like corpus size and diversity rather than "patents". (b) **A question naming the document defeats the floor entirely.** Every document has a title line, it carries no content, and quoting the identifier matches it — so metadata questions look supported regardless of whether the answer exists. That is a systematic weakness of a BM25 support floor on a single-document corpus, and it is worst exactly where a reviewer would most rely on it. `golden_patent.json` was **not modified** — it is ratified and frozen (D23), so this is analysis over it, never an edit. | Whether to strip document furniture from the retrieval index (would fix (b), but furniture is corpus-specific and over-filtering is this repo's recurring failure); whether a two-document corpus separates, which would test the size hypothesis directly. |
| D54 | 2026-07-28 | **Mark-a-sheet shows every existing mark, and lets a reviewer revise or remove one** (Julian, 2026-07-28). The pane previously only *added*. It now overlays all 145 located marks on the engagement's sheets — OCR reads in green, reviewer sightings dashed blue and labelled with who recorded them — each clickable to **revise the reading** or **refute it entirely**. `annotate.box_to_display` is the exact inverse of `box_from_pixels`, in Python and tested as a **round trip**: a box drawn, stored and drawn back must land on the same ink. | The round trip is where a y-flip hides, and a flip in *both* directions would cancel invisibly — so there is also a one-way test (`y` near 1 must draw near the top). The pane positions marks but never computes their position, mirroring D51's split. Neither action edits anything: a revision appends a judgment naming what it supersedes and both stay readable (D47), which is why "remove" is a `refute` rather than a delete. Human marks are visually distinct from machine ones because a reviewer's own sighting mistaken for an OCR read would make the record meaningless. | The overlay reads the MERGED manifest, so a reviewer mark can itself be revised later — correct, and worth noticing: it means the sheet shows evidence of mixed provenance, which is the honest picture rather than two separate layers. |
| D53 | 2026-07-28 | **A fitted floor is not a calibrated one, and the verdict is decided in exactly one place.** Julian asked what qualifies as a calibration record, which prompted checking whether the patent store could actually be calibrated — it could: `golden_patent.json` carries 5 content-absent items, so my earlier claim that it lacked them was wrong. Fitting it produced **threshold 6.2 with gap −16.7: the scores OVERLAP.** Some questions US5447630A cannot answer outscore some it can (content-absent up to 23.0, answerable down to 6.4), so **no single floor divides them**. The record now carries `separable` and `gap`, `resolve` reports `NON-SEPARABLE CALIBRATION` rather than success, and the fitter writes the failed fit **flagged** instead of hiding it. | Writing that record un-flagged would have turned the console banner green — the exact false comfort this mechanism exists to remove, produced by the mechanism itself. "Calibrated" must not be able to mean "we ran the fitter". The deeper defect was structural: the same three-state branch existed in **three** places (resolve, the report, the console builder), and the console went on reporting *calibrated* after the other two were fixed. `calibration.describe()` now decides and phrases it once — three copies of one verdict is how a store comes to read "calibrated" on one surface and "non-separable" on another, which is precisely what happened. | What the overlap *means* for the patent corpus: the deterministic content-absence check (D12's first mechanism) is close to inert there, so abstention rests on the agent's reasoning (D12's second) — which is measured at Layer-E, not guaranteed. Worth its own investigation before the engagement leans on abstention. |
| D52 | 2026-07-28 | **The Corpus pane, and a deliberate narrowing of RT-2.** The pane answers its own title — documents with hashes anyone can re-verify, the support floor and whether it still holds, the **corpus-scoped constants this corpus has not exercised either way**, the drawings state, and the judgment count with its chain status. RT-2 as written asked for *ingest and remove* controls; those are **not** built, and the pane says so in place of them. | Ingestion invalidates three things at once: the support floor was fitted to *these* documents and a BM25 score does not survive the corpus changing (D44's reasoning one level up); every citation already in the audit log resolves against offsets in a *specific* document version, and replay (I6) is a claim about that corpus, not a later one; and the corpus is read-only to the agent by construction (I4), so a corpus-write endpoint on a listener is a different security posture than D49 argued for. That does not make ingestion wrong — it makes it **deliberate**, so it stays a CLI act with the same weight as calibration. Surfacing *untested* constants is D43's lesson carried to the reviewer: inert is not validated, and calling it a pass is how a fitted value quietly becomes a law. **This is a scope reduction against the item as written, recorded rather than quietly delivered.** | Adding a guarded ingest flow later (it would need to force re-calibration, mark prior log segments as pre-change, and justify the write endpoint on its own terms). |
| D51 | 2026-07-28 | **Drawing a box to assert what OCR missed (RT-7c) — the loop's last leg.** The review queue can only offer marks the tool already *located*; a miss has no coordinates, so a reviewer had no way to supply them. Now they drag a rectangle over the sheet and name it, and it renders back as a `method: "human"` mark carrying who saw it and when. **The coordinate conversion lives in Python, not the page** (`annotate.py`): the browser reports pixels from the top-left, the manifest stores normalized fractions from the bottom-left, and a y-flip between those frames has already cost this project once (L0007). A transform written in JavaScript is one no Layer-0 test can reach — so the page sends only what it knows (pixel corners plus the displayed size) and never its own normalized coordinate. Verified end-to-end: a box drawn 180–230px from the top of a 1400px sheet recorded `y=0.8357`, exactly where it was drawn. | **A box is a sighting, not a search** — and the page says so. Recording one asserts "I can see this here"; it does **not** re-run OCR over the region, because OCR is ingestion-time and frozen (D28) and calling an engine at review time would put a model call on the runtime path (I6). That matters to a reviewer, not just to the architecture: someone who believed they had triggered a re-scan would read an empty result as evidence of absence. Sheets are **copied into** the console directory rather than served from the engagement dir — the server is confined to one root (D49), and widening it to reach client material would trade a containment guarantee for a copy. | Guided re-search from a reviewer's mark is deliberately NOT here: it belongs to a later ingestion pass, which the box can inform. The Corpus pane (RT-2) is the last placeholder. |
| D50 | 2026-07-28 | **The review queue closes the loop (RT-7b).** The product roadmap named Cairn's one structural gap: the expert whose judgment it exists to support could read every surface and write to none. D47 made judgment *durable*; this makes it *reachable* — the drawings reconciliation becomes a worklist where each row is one decision and one click, and each click appends a provenance-stamped judgment. **Ranked by consequence, not count:** a mark located but never recited outranks one recited but not located, because the first may ASSERT something the document does not support (the wrong-place class) while the second only withholds a location. Live on the engagement: **34 outstanding items**. | Three properties the pane must not lose, each tested. **Confirming is not the default** — nothing is pre-selected and there is no bulk accept, because a queue that can be cleared without reading it produces a record saying a human looked when none did, which is worse than no record. **The reviewer is named by the server, not the page**, so a judgment cannot be attributed by whoever has the tab open; a server started without `--reviewer` renders read-only and the endpoint refuses. **An empty queue is not a clean bill of health** and says so. Critically, `/adjudicate` is **deliberately not a tool**: every other endpoint dispatches into the MCP registry, and if this one did, the agent could call it and a machine could manufacture human judgments — the precise failure D47 exists to prevent, guarded here at the transport. Exercised live: recorded, then refused on replay (append-only), 403 cross-origin, 404 as `/tool/adjudicate`, and refused entirely on a nameless server. | Drawing a box to assert a *missing* mark (RT-7c) — the queue can only act on things already located, since a miss has no coordinates. Non-figure targets (spans, findings) — the record already carries them; the queue does not yet build them. |
| D49 | 2026-07-28 | **The Locate pane, and the loopback-only server it required (RT-10b).** A static page cannot run BM25, and the tempting fix — reimplementing retrieval in JavaScript so the console stays self-contained — would put a **second oracle** in a system whose central claim is that the same corpus and query give the same result (I6). Two retrieval implementations can drift; one cannot. So the pane calls the *actual* tools, which needs a transport. `serve.py` is that transport, and the pane **does not answer**: Cairn makes no model calls, so it runs the locate step, shows supporting spans or `insufficient` plus the closest passages, and leaves the reading to the reviewer. Span text is fetched through `get_span`, so what a reviewer reads is **hash-verified** rather than a cached copy that could have drifted. | The hardening brief is explicit that a local server's *absence* of a listener is the win and one should not be added without cause. The cause is determinism, and the cost is paid deliberately: **loopback binds are refused, not warned** (a warning gets scrolled past); `Origin` is validated, because a browser will let any page POST to 127.0.0.1 and DNS rebinding makes that reachable from a hostile site (the MCP Inspector RCE class); the handlers come from the **same registry** as the MCP server, so schema validation, path containment (D35) and bounded inputs (D41) apply unchanged and there is no second code path to keep in step. All four guards were exercised against the running server, not assumed: cross-origin → 403, `/etc/passwd` → refused, unknown tool → 404, `0.0.0.0` → refused to start. Every locate was recorded (I5). | No authentication, because there is no remote surface — if that ever changes it needs the brief's P1 items first, as its own decision. The pane is read-shaped: adjudicating from inside the console is still RT-7b/c. |
| D48 | 2026-07-28 | **One frame around the surfaces that never linked to each other (RT-10).** The console wraps the existing generators rather than replacing them: each still emits a standalone page, because that is what gets emailed, archived beside an engagement, and opened years later without this tool. What a set of pages cannot have on its own is what the frame adds — a **persistent header** (corpus, calibration state, adjudication count, contract version) and navigation across the five stages. Absent panes state what is missing and why. | This is where **D33's rule lands architecturally**: a caveat that scrolls away is a caveat that did not work. An uncalibrated support floor skews toward **false abstention** — refusing questions the corpus can answer — which is exactly the failure a reviewer reads as diligence, so it is a banner above every pane rather than an entry under a settings tab. Building it immediately surfaced a defect in the calling convention (`--from-audit` takes the log path; passed as a bare flag it fell through to the demo path and its hardcoded EDGAR document) — the cohesion work paying for itself on its first run. | The Locate pane — asking inside the console — is the intended front door and is not built; the Corpus pane awaits RT-2; in-console adjudication awaits RT-7b/c. The frame uses iframes, so a future single-document render is a change of mechanism, not of design. |
| D47 | 2026-07-28 | **An append-only record for the reviewer's judgment (RT-7a).** Cairn had no durable input channel for the expert it exists to support: the only route was a hand-edited mutable JSON array, which failed as that design must — a confirmation of FIG. 2's view-marker "A" was replaced by a single-engine OCR read and is unrecoverable. Now `adjudication.py`: four kinds (**confirm / refute / correct / note**), each carrying `by` + `on` + a stable id, appended to a hash-chained log. There is no update and no delete; a revision is a new entry naming what it supersedes, and `effective()` is a **fold over history, never a truncation of it**. `refute` is the only way anything leaves the manifest view, and it takes a named human on a dated record. | Built on `AuditLog` rather than beside it, so tamper evidence means the same thing everywhere and `verify_chain()` is one mechanism. The load-bearing guard is `supersede_ok=False`: a machine writer may record a *disagreement* — that is evidence — but may not displace a human's call, which is precisely the loss that motivated the work. Migration is deliberately honest about its own limit: the legacy file kept no history, so what it holds now is all that can be recovered, and the migrated notes say the provenance was reconstructed. On the live engagement it recovered **nothing** — the file is empty — so the "A" must be re-recorded by the person who saw it. | Drag-to-annotate in the GUI and reviewer-guided re-search (RT-7b/c); adjudications rendered into the record of inquiry (RT-5's missing section); adjudication of non-figure targets (spans, findings) — the target shape is already opaque enough to carry them. |
| D46 | 2026-07-28 | **The deliverable is the record of the inquiry, not the answer (RT-5 first increment).** Under 37 CFR 11.18(b) a practitioner's certification is **non-delegable**, so what a professional can actually buy is the documented inquiry — what was searched (with hashes), what each question resolved to, what was cited at which offsets, what was surfaced and **set aside**, and under exactly what declared limits. Two wording rules are enforced as behaviour, not style: the report says it *evidences / documents / supports* an inquiry and never that it *satisfies / ensures / discharges* one, and it states that retrieval is a **ranked BM25 slice, not an exhaustive search**. Limits render **before** every findings section. | A record that implies search breadth it does not have is a liability artifact rather than an asset — the landscape survey's clearest commercial finding, and the reason the wording is tested rather than merely written (`test_it_never_claims_to_discharge_the_inquiry` checks the CLAIM, not a word count: "discharge" is fine inside "does not discharge it" and fatal without the negation). Including the **set-aside** candidates is the other half: a record showing only what was used invites the reading that nothing else was seen, and it is precisely what a reviewer needs in order to disagree. RT-9's calibration warning is carried onto the client-facing surface, because an abstention taken under a foreign floor is the one a reviewer would otherwise trust. | Human adjudications + reviewer notes (blocked on RT-7's append-only sidecar — a confirmation has already been lost to an overwrite); per-session segmentation (RT-1); and the real Q13/Q16 answers, against which the current defaults are stated rather than assumed. |
| D45 | 2026-07-28 | **RT-6 protocol part 2 — the drawings. Four more fitted assumptions failed, three of them before a single numeral was read.** (a) **The fetcher could not fetch:** `doc.rstrip("AB")` was fitted to `US5447630A` and does nothing to `US8046721B2` ("B2" ends with a digit), *and* Google Patents serves newer grants under a second scheme (`US08046721-20111025-D00009.png` — zero-padded, dated, D-numbered). It reported *"no drawing sheets found — is the id right?"*: a confident, plausible, wrong diagnosis for a condition it could not distinguish from a bad id. (b) **`HEADER_BAND = 0.88` deleted 13 real numerals** — corpus 2 has **no running header on any of its 16 sheets**, so a guard fired where its precondition did not hold, silently removing 100/200/300/400/1002/… Now conditional on per-sheet evidence (`sheet_has_header`), recorded per page. **188 → 206 numerals; corpus 1 byte-identical at 145.** (c) **`_FIG_LABEL`** matched "FIG. 2" but not "Figure 2", so **zero** captions were found across 16 sheets. (d) **Cross-sheet geometry was unchecked** — `geometry_report` now flags a sheet whose pixels disagree with the grant's own median (3 flagged on corpus 2; clean on corpus 1). | **The failures cluster in ingestion, not in the evidence layer** — eight defects across two protocol runs on a system passing 249 tests, none found by a test, all found by pointing it at an unfitted corpus. The geometry check matters structurally: every coordinate downstream is a **normalized fraction**, so a lower-resolution rendition served under the right name is invisible in the manifest **by construction**; this is the only place it can be seen. It reports rather than refuses because a genuine fold-out is legal and the check cannot tell them apart. Also settled: the merge radius (the swarm's "sharpest untested constant") **transfers** — +0.0072 headroom on corpus 2 vs +0.0002 on corpus 1, so the tightness is a property of corpus 1 rather than of the value. | **Open defect:** `--confirm` dies with **SIGBUS** on `drawings-page-12.png` (the 1497×1536 sheet), reproducibly, inside `tiled_search`; undiagnosed to an engine, first pass unaffected. Buffered stdout means it produces *no* output, and the stale manifest on disk made the first attempt look like a successful no-op. `MIN_LOCATABLE_NUMERAL` was inert a second time — still untested. |
| D44 | 2026-07-28 | **A support floor now carries the corpus it was fitted to, and its absence is loud (RT-9).** `check_support`'s floor is a **BM25 score**, which does not transfer between corpora — it scales with document length and term distribution — and it fails toward **false abstention**, the direction that looks like diligence. Measured (D43): *"how is the device unlocked"*, the entire subject of US8046721B2, scores **4.56** against the EDGAR-calibrated **15.0**, so Cairn returned `insufficient` on an answerable question and nothing said why. Now: a store carries `calibration.json` (floor + corpus id + doc ids + **corpus hash** + date + method), `resolve()` reports *calibrated* / *STALE* / *UNCALIBRATED*, and the warning is stamped on every `check_support` / `check_claim` record. | **Warn, not refuse** — deliberately. Refusing would make an uncalibrated store unusable for exploration, and the honest failure is not "you may not ask" but "this abstention is not trustworthy and you must be told". The warning sits at the record's **top level** rather than in `provenance` because it qualifies the *result*, and a reader skimming for problems must not have to open a nested block to find one. It is preserved verbatim on replay for the same reason the provenance stamp is (D21/TC-2): a replay that dropped it would launder the caveat. **A design bug caught while building this:** the store's record initially overrode an explicitly-passed threshold — the same silent-override class the mechanism exists to remove. An explicit floor now wins and is labelled `EXPLICIT OVERRIDE`. | Nothing yet *requires* calibration before a client engagement (it warns); the patent stores remain uncalibrated pending a patent golden set with content-absent items. `acronym_labels` is contract-documented and caller-audited, not mechanically gated. |
| D43 | 2026-07-28 | **`_FIG_CAPTION` matches range captions; RT-6's protocol validated end-to-end on a second patent** (US8046721B2, Apple 2011 — post-2000 software art, chosen to stress the 1000-series and wide sub-figure ranges). The bug: the caption pattern required `FIG. N <verb>`, so a RANGE caption ("FIGS. 4A-4B illustrate…") could not match — `-4B` sits between the number and the verb. It cost twice: those captions were lost, **and** the unmatched text inflated the gap to the next caption past `_CAPTION_GAP`, terminating the caption run. Effect: `parse_figures` silently dropped **FIG. 6, 9 and 10** — each recited three times, each captioned — because the gap measured **450** against a 400 limit. Fixed with an optional range tail: **22 → 25 figures**, the complete set, largest in-run gap now **160**. Corpus 1 unchanged (8 figures). Full record: `docs/corpus_fit_record_US8046721B2.md`. | **The protocol worked, and the falsifiers were the reusable artifact.** Two of three failures were predicted in advance by the registry's `falsifier` field; the third was found by chasing a falsifier to a *different* constant. And the general lesson from this one: **a threshold that looks too tight may be measuring a parse failure upstream of it** — loosening `_CAPTION_GAP` would have masked the bug and mis-attributed three figures' captions. Two other results: `NUMERAL_DIGITS = 4` validated on independent evidence (13 four-digit numerals recovered, all invisible pre-D34), and `MIN_LOCATABLE_NUMERAL` came back **inert** (lowest numeral recited is 36) — recorded as *still untested*, not as transferred, because a constant no test exercised has not been validated. | The run was **text-only**: no sheets fetched or OCR'd, so every OCR-side constant (the 0.02 merge radius with 0.7 px of headroom, the 0.88 header band, the strobogrammatic map) is still untested on corpus 2 — the largest gap in the record. A third corpus with single-digit numerals on clean drawings is needed for `MIN_LOCATABLE_NUMERAL`. |
| D42 | 2026-07-28 | **Corpus-fitted constants are registered with evidence and a falsifier, and the registry is gate-enforced (RT-6).** 17 constants inventoried; **12 (70%) are corpus-specific** — fitted to one 1995 patent or one 10-K. Building the inventory immediately found three defects it was designed to prevent: (a) **D34 was incomplete** — it widened the numeral cap at the head pattern and left the list-sibling pattern at three digits, so "the pipes 1002, 1010" recited 1002 and silently dropped 1010; all four extraction sites now interpolate one `NUMERAL_DIGITS`, pinned by a test. (b) The sibling pattern was comma-only, so the last element of "56, 58 and 60" was dropped; now handles the conjunction, and safely — a following noun phrase names its own element rather than mis-inheriting. (c) **Two fitted constants existed in duplicate** (`ocr_patent_figures._HEADER_BAND` vs `figures_map.HEADER_BAND`; `_SAME_SPOT` vs the merge radius default), free to diverge between the ingestion pass and the runtime layer that reads its output; both now single-sourced. | "A constant with no recorded provenance is indistinguishable from a law." The `falsifier` field is the operative one — provenance without it is a story. Two entries are called out as failing **silently**: `MIN_LOCATABLE_NUMERAL` suppresses locations rather than producing wrong ones, and the 0.02 merge radius sits **0.7 px** inside its safe window (the closest genuine same-token pair is 0.0202) — never measured when chosen. Scalars are pinned; open-ended lexical sets are provenance-only, because pinning a list that legitimately grows trains everyone to bump the number without reading the reason. | The end-to-end run on a second patent (the rest of RT-6's AC), and the merge/conflict radius asymmetry — a pair 0.015 apart is neither merged as one mark nor reported as a conflict. |
| D41 | 2026-07-28 | **Tool metadata is hashed and linted; tool inputs are bounded, not merely typed** (MCP hardening brief P0.1 + P0.2, completing the audit begun in D35). `tool_manifest_sha256` hashes the whole advertised surface — every name, description and schema — and a standing test pins it, so unreviewed drift fails the gate. `lint_tool_descriptions` rejects instruction-like language (`ignore`, `you must`, `always call`, `regardless of`) in descriptions. Inputs: `_obj` already closed the OBJECT (`additionalProperties: False`); this closes the VALUES — `doc_id` gains `maxLength: 128` and a name-shaped pattern, `query`/`claim` gain `maxLength`, offsets gain a ceiling, and `k` is capped at 100 (it was unbounded). | **Tool metadata is a trust surface, not documentation.** An agent reads descriptions and schemas and acts on them, so a description edit is a behaviour change with the reach of a code change — the rug-pull shape is to ship benign metadata, earn trust, then quietly rewrite what the model is told a tool does. The `doc_id` pattern is deliberately *stricter* than `DocumentStore.doc_dir` accepts: defence in depth, so a traversal shape is refused at the wire before it reaches a path join, and D35 refuses it again downstream. The hash was proved to have teeth on four known-positives before being trusted (description edit, schema loosening, tool added, tool removed — all detected), per L0010. | Brief P0.4 (written transport posture) and the P1/P2 items — remote auth, Server Card, SBOM — none of which apply while the server is stdio-only. |
| D40 | 2026-07-28 | **Renamed ATTEST → Cairn.** Julian's call, prompted by D38's landscape findings: `ATTEST` is a live US registration (Reg. 6,077,675, Classes 009/035/042 — which covers software services), the field around it is crowded, and "attestation" is heavily loaded in computing (`cosign attest`, `gh attestation verify`, RFC 9334, in-toto/SLSA). A cairn is a marker deliberately built so a place can be found again by someone who wasn't there — which is the repositioned differentiator (durability of the pointer, not the offsets). Package, module path, env-var prefix (`CAIRN_*`), CLI, docs and the git remote all move; **`corpus/` and `audit_log/` are untouched.** | Timing: the rename was ~as cheap as it will ever be — 684 occurrences across 77 files, and critically **zero occurrences in the append-only audit chain**, which records tool kinds and a contract version, not the product name. So no logged record was invalidated and the hash chain did not fork. Nothing had shipped to PyPI; there were no tags. Every month of delay adds surface. **This is not legal clearance** — trademark screening for Cairn is still counsel's, and the D38 packet stands. | Publishing under the name (PyPI/npm/Homebrew), and the clearance itself. |
| D39 | 2026-07-28 | **Layer-E grades the CITATION, not only the verdict (RT-8).** `score_item` computed `decision_correct = (presented == expected in PRESENTS)` and never read the golden item's `supporting[]` — so every headline metric was label-only accuracy, in FEVER's exact sense, for a system whose entire product claim is span provenance. A citation that is *real but points elsewhere* passed. Now `score_item(item, log_segment, store)` also resolves the gold quotes to offsets and checks the agent's bound spans overlap them; `aggregate` publishes `evidence_accuracy` and `decision_minus_evidence` beside `decision_accuracy`. | FEVER's published **50.91% → 31.87%** drop under conjunctive evidence scoring is the size of the illusion this hid. The gap is deliberately reported as its own number: a decision score that outruns its evidence score is measuring the verdict, not the provenance. Two adaptations FEVER cannot supply and this does: match against a **set** of acceptable gold spans (a figure recurs across a filing), and by **overlap** not equality (a tighter citation and a wider gold quote are the same evidence). Unscoreable items read as `null` + a note — never 0%, never clean. | `abstain`/`refuse` evidence (D12 `closest`, D22 locate-first) is not yet scored — those produce no passing `verify` record, so the located spans live in search/read calls; scoring them needs the segment, not the answer. Also queued: a no-corpus control arm (EDGAR facts are near-certainly in pretraining; the EDGAR-vs-patent gap is itself the leakage measurement) and risk–coverage/AURC, since `abstention_accuracy` and `answer_rate` are two points on one curve `support.THRESHOLD` slides along. |
| D38 | 2026-07-28 | **Two shipped overclaims scoped to their mechanism and corpus.** The attestation-landscape swarm found them in our own tree and both were verified: `demo.py` printed *"100% citation precision, 0 hallucinations"* — an absolute claim from a 20-item measurement, computed by gold-string containment — and `CAIRN_Patent_Tailoring_Consideration.md` called the core *"the zero-hallucination oracle"*. Now: "on this 20-item golden set … every citation resolved at its offset and hash-matched … measured on one corpus, not a general accuracy claim", and "the ground-or-abstain oracle". | The pattern the swarm documents across every cautionary tale is identical — **an absolute claim about a narrow measured distribution, an independent party measures a different one, and liability lands on the deployer**. Lexis+ AI's "hallucination-free" (17–33% measured, peer-reviewed) is the headline case; the FTC's Workado order is exactly the offence of a general accuracy claim from one corpus. Our own truth contract forbids these sentences and nothing caught them — README and the build brief were already correct, so the defect was that no gate covers marketing surfaces. D4's "preserves the zero-hallucination claim for the pitch" stands in the append-only log as written, but does not license the phrasing. | A leak_gate-style check over client-facing copy for absolute accuracy language; every metric bound inline to corpus + N + date. |
| D37 | 2026-07-28 | **Atom coverage is COUNTED, not set-differenced — a figure asserted twice needs two bindings** (I1). Found by the attestation-landscape swarm and reproduced before acting. `unbound = salient_tokens(sent.text) - covered` was a set difference, so a single binding discharged *every* occurrence of a figure in a sentence: `"Total assets were 364,980 and total liabilities were also 364,980"` with one binding returned `ok: True, unbound: []`. The second assertion — about a **different metric** — was ungrounded prose that the first citation silently vouched for. Now `Counter`-based (`salient_counts`), so N assertions require N bindings. Verified in three directions: one-binding/two-assertions now fails, two-bindings/two-assertions passes, one-binding/one-assertion is unaffected; all 226 pre-existing tests passed unchanged, so it is not over-flagging the corpus. | This is the exact class CAIRN exists to prevent — a claim presented as grounded whose grounding covers something else — and `verify` was the component asserting it. Note *how* it hid: the bug is invisible unless a sentence repeats a figure, which the golden set never does. A test suite built from realistic examples does not probe degenerate ones, and the independent-extraction check is precisely where a degenerate case matters. | Repetition across SENTENCES (coverage is per-sentence, which is correct — two sentences citing the same figure are two claims, each bound). Named entities and bare years remain not-independently-required. |
| D36 | 2026-07-26 | **Strobogrammatic twin rejection — a 180° phantom is one mark read twice, and that is provable** (Julian's inversion idea, 2026-07-26). Because observations are un-rotated back into page coordinates, a phantom lands at the **same page position** as the upright read it came from, and its label is that read's strobogrammatic inverse (rotation reverses order *and* maps digits: `86`→`98`, `106`→`901`). Position coincidence + inverse label is therefore **proof** of double-reading, not evidence of it. Measured: **19 coincidences** on US5447630A, including every phantom found by hand (FIG 4's `98` over the real `86`, FIG 5's `86` over the real `98`, `901` over `106`). Julian's other half — density buckets — was also tested: nearest-neighbour spread separates the correct rotation AXIS cleanly (0.005–0.05 on-axis vs 0.10–0.21 off-axis, an order of magnitude), and major buckets always come as opposite pairs, correctly finding {90,270} for the sideways sheet and {0,180} for the other seven. | **Density identifies the axis but not the direction** — {0,180} and {90,270} are indistinguishable by density, because a page turned 180° still presents text-like horizontal strokes. That is exactly why the 6↔9 problem survived D32's gate. The inversion test supplies the missing half, and it is a mechanism rather than a statistic, so no rate or threshold is needed. It also catches **strictly more than cross-engine corroboration**: several phantoms were read by two or three engines — engines fed the same inverted ink agree with each other, so corroboration is blind to a defect living in the pixels rather than the models (the D31 lesson, third instance). Both rules kept: corroboration decides whether a mark is *real*, the twin test whether it is a *separate* mark. | Digits omitted from the conservative map (2/5/7, which invert in some typefaces); a genuinely inverted sheet, where no upright twin exists and the test correctly stays silent. |
| D35 | 2026-07-26 | **doc_id is containment-checked at the path boundary, and the MCP tool surface is audited rather than assumed.** Prompted by the fleet's MCP security-hardening brief (`~/Documents/Claude/mcp-security-hardening-brief.md`), whose P0.3 says to *verify* a "we have no injection surface" assumption rather than rely on it. Audited: no `subprocess`/`eval`/`exec`/`shell=True` anywhere on the served path (`layer_e.py`'s subprocess is the offline harness, not a tool). But `DocumentStore.doc_dir` was `self.root / doc_id`, and the danger is not the obvious one — **pathlib discards the left operand when the right is absolute**, so `store / "/etc/passwd"` is `/etc/passwd`: traversal with no `..` to pattern-match. Now canonicalized and containment-checked, rejecting empty/`..`/NUL/absolute/escaping ids. | **The vulnerability was not reachable, and that is the finding.** `SpanStore` is dict-backed, so an unknown doc_id raises before any filesystem access — verified empirically, not read off the source. But that guard is *incidental*: a property of one class's storage choice, not a boundary anyone designed. The RAG extension under discussion contemplates corpora too large to preload, and lazy loading is precisely the refactor that would make a caller-supplied doc_id reach the filesystem. Doctrine: safety by construction, not by vigilance — put the boundary where the path is built so it survives the refactor that removes the accident. | The brief's remaining P0 items for CAIRN: tool-metadata hashing (descriptions are executable intent — a rug-pull surface), reject-by-default input schemas (today `doc_id` is an unbounded `{"type": "string"}`), and a written transport posture. Queued, not done. |
| D34 | 2026-07-26 | **Three integrity holes closed, found by the OCR failure-mode swarm** (`docs/ocr_failure_modes.md`; each reproduced before being acted on). (a) **Numeral vocabulary was too narrow, and narrow in a way that ASSERTED.** `_NUMERAL` capped at `\d{1,3}[a-z]?`, so the figure-keyed 1000-series (FIG. 10 → 1000/1010) that dominates post-2000 software art was unreadable — on such a patent *both* sides of the drawing/spec reconciliation come back empty and coverage reads clean over an empty map. Worse: `12A` fell through the lowercase-only suffix and matched as bare **`12`**, binding a distinct part to its base numeral's span — a wrong citation on the path where grounding binds (I1), not a missing aid. Now `\d{1,4}[a-zA-Z]?`. Verified a **no-op on US5447630A** (68→68, nothing added or removed). (b) **Quantities were bound as parts:** "at temperatures exceeding 500" yielded numeral 500, element "at temperatures exceeding"; `with below exceeding above near into onto through during per` join the function-word class already blocking `at`/`of`/`between` (68→64). (c) **`image_sha256` had exactly one consumer — the line that wrote it,** and `subprocess.run` never checked `returncode`/`stderr`. `verify_raster_binding` now runs on every manifest load, and `_assert_engine_ran` refuses a dead engine. | A hash nobody checks is decoration, and it guarded the assumption the whole figures view rests on: every box is a NORMALIZED fraction, so a re-fetched sheet resolves every box silently onto a different raster — the wrong-place class. The engine check is **L0009 finally installed in code**: that lesson was written after a dead Tesseract was mistaken for a Tesseract blind to this corpus, and it stayed prose for four milestones. A lesson that is not enforced by code is a lesson that will be re-learned. | Tier-A manifest audits (A6/A7/A10/A13 fire today), the 0.7 px of headroom on the merge radius, and one blind-annotated sheet — all in the report, all untriaged. |
| D33 | 2026-07-25 | **Discrepancies lead every report surface, and the OCR caveat leads them.** Standing rule, not a one-page tweak: any surface presenting OCR-derived locations opens with (a) a discrepancy tally above the evidence, each count linking to its detail, and (b) a caveat that states precisely **what is and is not reproducible** — the reading is frozen into a hashed manifest at ingestion so everything downstream is byte-identical (I6) and sheets are never re-OCR'd at answer time, but the reading itself can differ across engine version, OS, or machine. Also stated: **absence of a flag is not evidence of correctness** — the checks compare drawings against the specification, so a numeral OCR missed *and* the spec never recites is invisible to all of them; the counts are a floor, not a ceiling. | Julian: "for the sake of honoring this tool's entire premise." A tool whose whole claim is ground-or-abstain cannot bury the one step that is neither — OCR is *judged*. A caveat found at the bottom is a caveat that did not work. The wording is deliberately not the easier line "OCR is nondeterministic": that understates the determinism we do have (frozen manifest) and overstates run-to-run variance we do not. | If a report surface ever renders an OCR location without the tally + caveat above it, that is a contract bug, not a styling one. |
| D32 | 2026-07-25 | **Union across all four rotations, gated by corroboration — supersedes D31's single best angle.** Julian asked whether 0/90/270 is enough and whether one sheet can carry text at several orientations. Measurement says yes to the second, and D31 was wrong twice. (a) **Rotation is a per-GLYPH property, not a per-page one:** the running header `Sheet 1 of 8` parses at **all four angles** — a long line has enough context to be found however it lies — while an isolated `33` on a leader line reads only when upright in the raster. A sheet with an upright header over a sideways drawing (p.2) therefore has *no single correct angle*, and pick-one loses labels on **7 of 8 sheets** (`25 56 67 71` on p.2 alone). (b) **Token count is the wrong chooser:** it picks 90° for p.2 where 270° is the human-confirmed truth, because upside-down digits still look like digits. So: OCR every sheet at 0/90/180/270, un-rotate into page coordinates, union. A label seen ONLY on a rotated pass must be corroborated — spec recital **or** two engines at the same spot. **180° is held to cross-engine only**: no printed sheet is inverted, and 180° is precisely the 6↔9 generator (FIG 4's real `86` produced a phantom `98`; FIG 5's real `98` produced a phantom `86`), which spec recital cannot filter because both members of a 6/9 pair are usually recited. | Corroboration, not angle-guessing, is what makes higher recall safe. Union alone would have imported every phantom; the gate admits `20`/`33` (off-spec, three engines agree, Julian confirmed by eye) and rejects `901`←`106`, `99`, `98`, `86`, `90`. Note what this says about the spec gate generally: against a 68-numeral spec, recital is **necessary-but-weak** — it corroborates almost any plausible two-digit artifact. | A sheet genuinely printed inverted (cross-engine still admits it). Non-quarter-turn **skew**, which no rotation set fixes — deskew is the separate lever. |
| D31 | 2026-07-25 | **Per-sheet orientation detection — sideways sheets are OCR'd rotated and mapped back.** Julian marked where the FIG-1 misses (20, 33, 40, 43, 44) actually sit; the giveaway was that **FIG. 1 is printed SIDEWAYS** (a landscape figure on a portrait page) and every engine reads upright text only. Measured: FIG 1 yields ~7 numeric tokens upright vs **24 at 270°**, while all seven upright sheets gain nothing (and lose reads) when rotated — so rotation must be chosen PER SHEET, by evidence. `detect_orientation` picks the angle with the most label-shaped tokens; observations are mapped back with `figures_map.unrotate_observation`; the chosen angle is recorded per page in the manifest, so the decision is auditable. Result: FIG 1 goes 22 → **27 labels, recovering every numeral Julian listed** (20, 33, 38, 40, 43, 44). | An engine blind spot that is really a PRESENTATION mismatch is invisible to confidence scores and to cross-engine corroboration — all three engines fail identically on rotated text — so no amount of engine redundancy would have found it. Only comparing the sheet against itself at another orientation does. | Sheets rotated at other angles (skew, 180°) — extend `ROTATIONS` with the same evidence rule; per-figure crops if a single sheet mixes orientations. |
| D21 | 2026-06-29 | **The truth contract is a *declared, versioned, monotonic* artifact; rigor is upgraded behind stable seams and ratcheted by the oracle.** CAIRN's guarantees (ground-or-abstain; I1–I6; outcome honesty D16; locate-never-adjudicate D10; and the deliberate `verified ≠ entailed` non-guarantee) are declared in one place — [`docs/truth_contract.md`](docs/truth_contract.md), **truth-contract v1** — each mapped to its enforcing mechanism + layer (runtime / Layer-0 / Layer-E) + current strength. **Monotonic:** rigor may strengthen freely (minor bump); a change that *weakens* a guarantee needs a new **major** version + logged rationale + the oracle re-run — never silent. **Upgrade ratchet:** a new method (retrieval backend, calibrator, verify op, entailment judge) is adopted *behind its existing seam* and ships **only if the Layer-0 gate holds and Layer-E improves-or-holds**. **Provenance (TC-2):** outputs/audit records will stamp `contract_version` + the methods that produced/checked them, so old records stay interpretable and rigor is comparable across versions. **Anti-trap:** this is *declaration + provenance + the upgrade rule*, not a speculative plugin framework — formalize an interface only at a second real implementation. | New understanding/tech/research must be able to raise epistemic rigor without a rewrite and without silently breaking what past outputs promised; an explicit, versioned, provenance-stamped contract is what lets rigor evolve auditably. | Bump the contract version when entailment moves runtime-ward (v2 structural interception, Backlog) or any guarantee's strength changes; formalize `EntailmentProvider` / `Calibrator` interfaces at their second implementation. |
| D20 | 2026-06-29 | **The `check_support` floor is *fitted from labels and recorded*, not hand-tuned — and never auto-derived from the corpus alone.** `support.calibrate_threshold(golden, retriever)` places the floor in the gap separating answerable golden queries (top span should clear) from **content-absent** ones (`out-of-document-fact` / `not-disclosed-metric`); semantic traps are **excluded** (they retrieve real, high-scoring content → abstained by reasoning, D12). Deterministic (`fit_floor`: maximize correct splits, then margin → a clean split lands on the gap midpoint). Calibration is a **deliberate step that emits a value to record** in the engagement config — it does **not** silently re-float the evidence path on every ingest (oracle-is-sacred). Rediscovers EDGAR's hand-set ~15 (got 15.1; answerable ≥19.7, content-absent ≤10.5, clean gap 9.2). | A threshold is *query-relative*, so it can't be inferred from the query-free corpus at ingest; fitting from a labeled golden set is the principled, auditable route and keeps the value legible/reviewable rather than a hidden moving part. | A query-relative alternative with **no** per-corpus constant (top-hit outlier / z-score vs the result distribution, or score normalization) is a **Layer-E experiment** (Backlog) — adopt only if it beats the calibrated constant. |
| D19 | 2026-06-28 | **Relational verification confirms a *numeric fact*, never a legal conclusion** (VER-1 slice 2). `verify` gains boolean-result derivations — `gt`/`ge`/`lt`/`le`/`eq` and `within_range` (value within an inclusive low/high) — each recomputing the relation between cited operands and confirming the agent's asserted `true`/`false`. Directly serves patent **claim numerical limitations** ("between 5 and 10 wt%", "at least N"). **Boundary (D10), at its sharpest:** a value being "within range" is an *arithmetic* fact, **not** an infringement / novelty / validity determination — the op is named `within_range`, not `infringes`, and the agent must never phrase the boolean as adjudication (refusal class; the Layer-E negative-test enforces it). | Claim limitations are the domain's core quantitative comparisons; verifying the arithmetic while refusing the legal leap is exactly locate-&-evidence. | Adjudication negative-test lands in PE-5/Layer-E; range-exclusivity nuance ("between" vs "from…to") and unit-aware comparison are refinements. |
| D18 | 2026-06-28 | **`verify` recomputes a diversity of arithmetic from cited operands, matched at the *written* precision** (VER-1 first slice). Extends D9's derived-atom operations from `sum`/`subtract` to also `multiply`/`divide`/`ratio`/`percent_change`. Each stays a **pure function over the operand numbers**, recomputed and compared to the asserted result **rounded to the decimal places the agent wrote** (deterministic, honors stated precision, no arbitrary epsilon; division-by-zero / bad arity → `derived_ok=False`). The result is still **declared, not cited** (D9); no model calls (I6). **Boundary (D10):** arithmetic that *evidences quantities* only — it must never become adjudication (term / validity / infringement / claim-scope). | The √ case showed `verify` was too narrow — real questions need ratios, % change, products; precision-rounded matching is the clean, deterministic way to verify non-integer results without fuzzy tolerances. | **Unit conversion** (external factors → auditable table) and **compare / within-range** (claim limitations) are the next VER-1 slice; revisit the precision rule if a domain needs significant-figures rather than decimal-places. |
| D17 | 2026-06-28 | **Desktop-first: the working surface is Claude Code Desktop, not the M5 React app — build the Desktop integration now; defer M5.** Ship a Desktop DX layer: project `.mcp.json` (CAIRN as connected MCP tools), a `/ground` slash command that runs the loop end-to-end, a `--from-audit` bridge that rebuilds the evidence view from a live session's audit log, and [`docs/desktop_setup.md`](docs/desktop_setup.md). The M5 React replay app becomes a *client-deliverable* showcase, built when a deliverable needs it. | Desktop is how the first (patent) engagement will actually be run; the static evidence view + audit log already cover "show what happened." Polishing the React showcase before the daily driver optimizes the demo, not the work. | Build M5 when a hosted replay UI is needed for a client; revisit if Desktop can't host the review surface. |
| D16 | 2026-06-28 | **Grounded correction is a first-class outcome — four classes, not a present/abstain binary.** Layer-E scores each item against an expected outcome derived from the golden fields: **answer** (answerable), **abstain** (content-absent / semantic trap → stay silent), **correction** (false premise → present a *grounded refutation* citing the contradicting evidence), **partial** (in-corpus part answered + the rest explicitly flagged). `correction` and `partial` are expected to **present**; only `abstain` stays silent. The deterministic scorer checks the present/abstain decision matches the class; a model judge then confirms a `correction` actually **refutes** the premise (`judge_refutes_premise`) and that answers **entail** their spans. Surfaced by the first live baseline: G020 correctly refuted "why did assets decline?" but the old `presented == answerable` rule mis-scored it as a miss. | Refuting a false premise *with evidence* is the system's highest-value act ("ground or abstain" is too coarse — it hid a correct behavior as a failure). Making it a scored category both credits the agent and lets us measure it. Same shape as the patent posture: surface the contradicting span, never silently drop the question. | Revisit the partial-abstain scoring (present-with-caveat is graded only on the present/abstain decision today; the caveat-quality check is a later judge). |
| D1 | 2026-06-23 | **Corpus = SEC EDGAR 10-K / 10-Q.** | Free, public, high-stakes, strong gig pipeline, shareable demo. Adapter isolated to ingestion (brief §8). | Reference build ships; then add corpus variants. |
| D2 | 2026-06-23 | **Read-only v1.** No write/act tools. | Faster ship, easier trust, lower eval bar. | v2 only. |
| D3 | 2026-06-23 | **`check_claim` ships in v1.** | "Paste your own sentence, see if your docs back it" is too strong a demo to defer. It's the one thing above the minimal read-only path that earns its place. | If a leanest-possible first ship is needed, this is the cut. |
| D4 | 2026-06-23 | **Hallucination gate = literal 0**, operationalized: CI hard-fails on any entailment-judge flag that is **not** human-adjudicated as a false positive. Adjudicated false positives are logged and don't count. | Preserves the "zero hallucination" claim for the pitch while absorbing LLM-judge noise honestly. ε is a *process*, not a tolerance on the number. | If adjudication volume becomes unmanageable. |
| D5 | 2026-06-23 | **Citation precision hard gate starts at 0.9.** | Defensible opening bar; tighten as the golden set grows. | After M2 baseline is measured. |
| D6 | 2026-06-23 | **v1 runtime = Claude Code tool.** CAIRN ships as an MCP server + CLI that Claude Code invokes; the agent is the reasoner; CAIRN makes no runtime model calls. API-wrapped service (with inline entailment-gating) is v2. | Relocates the grounding guarantee to deterministic tools + a mandatory verify/log step. Splits the oracle into Layer-0 (deterministic, CI-blocking) and Layer-E (agent end-to-end, periodic via headless Claude Code). See brief "Runtime model" + §3, §4, §5. | v2 is scoped. |
| D7 | 2026-06-23 | **Golden schema = quote + locator** (not span IDs). A resolver binds `verbatim_quote → span_id` at M1 and must match each quote exactly once (resolution invariant). | Span IDs/offsets can't exist before M1 and depend on M1 normalization. Supersedes the brief's original §3 span-id schema (brief §3 now updated). Seed lives in `golden_seed.json`. | — |
| D8 | 2026-06-24 | **Highlighted evidence-view GUI is pulled forward** to a small, server-less static-HTML renderer (`render_evidence_view`, new **M2-T7**) the moment `verify` exists; the polished React/audit-log replay app stays at **M5** and upgrades it. **Contract:** the agent drafts an answer with each sentence tagged by `span_id`(s) → `verify` confirms every tag resolves + hash-matches (I1/I3) → `log` records it (I5) → the renderer deterministically emits a two-pane page (canonical document left with `<mark>` highlights, answer right with click-to-source hyperlinks). The agent's tagged markdown is an *authoring surface that passes through verify/log* — never a hand-maintained source of truth; hyperlinks are generated, never asserted. Render the **normalized canonical text** (the hashed, cited text), not the original filing HTML. | Visual payoff is the conversion surface (brief §6) and worth pulling to mid-project, but faithful highlighting needs char-offset spans (M1-T2) + validated citations (M2-T1), so the earliest *honest* viewer is post-M2. Hand-authored citation links would reintroduce the exact unverified-claim failure CAIRN exists to prevent (I1). Refines brief §6. | If the React app at M5 fully subsumes the static renderer, retire the latter. |
| D9 | 2026-06-24 | **Atom-resolver contract for `verify` / `check_claim`.** The agent decomposes an answer into **atoms** (load-bearing tokens: numbers, currency, %, dates, named entities) and binds each to a specific source location `(doc_id, content_hash, char_start, char_end)`. A **fixed, deterministic resolver** confirms the slice *at that offset* equals the atom **exactly** (literal match — defined whitespace normalization, word-boundary, accounting-`( )`-negative aware), hash-matches (I3), and lies within the **spans retrieved for this query** (no whole-corpus fishing). The agent **parameterizes** (supplies atoms + bindings); it **never authors** the resolver (oracle-is-sacred; extends D8's tag contract). Crucially, `verify` also runs its **own** atom extraction over the final answer and requires every detected load-bearing token to be bound — confabulation can't hide in untagged prose. **Derived** values (e.g. a computed delta) are not atoms: they declare operands (each an atom that resolves) + an operation the resolver recomputes. | Makes every figure/quote the agent ships **machine-checkable and located**, killing invented citations while keeping the checker deterministic and CI-gated. It is the user's "bounded lookup" idea, hardened. Existence ≠ support remains the runtime limit: right-number/wrong-line or a misphrased reading is caught offline (Layer-E), structurally in v2. Open implementation contingencies are tracked under **M2-T1**. | Revisit if the atom taxonomy proves too coarse, or when v2 pulls entailment inline. |
| D12 | 2026-06-25 | **Abstention has two mechanisms; `check_support` owns only the deterministic one.** `check_support` (I2) returns `insufficient` when **no retrieved span clears the relevance threshold** — i.e. abstention by *content absence* (golden G011 CEO-pay, G012 unit-sales, G013 churn; measured score: those top out ≤11, answerable gold spans ≥19, so a threshold ~15 separates them cleanly). The other unanswerables are **semantic traps that retrieve real content** — wrong period (G014 Dec-2024, G015 Q2-FY2025), wrong entity (G016 vs Microsoft), false premise (G020) — and a deterministic threshold *cannot* catch them (G014 scores as high as G001). Those are abstained by the **agent's reasoning** (D11; the M0 rig modelled this with temporal/entity/false-premise guards) and measured end-to-end at **Layer-E**, not by `check_support`. Reconciles the brief's "abstention trigger 100% on unanswerable" to **100% on the content-absent subset**; the threshold is a **per-corpus calibration** (patents recalibrate). | Deterministic tools can't reason about period/entity/premise — that's the whole point of D11. Forcing `check_support` to hit 100% on traps would mean baking corpus-specific semantic guards into a "deterministic" tool (the rig's throwaway move) and would falsely advertise determinism it can't have. The honest, still-strong claim: **provably abstains when retrieval finds nothing; semantic abstention is the agent's job, measured (calibration/Brier) at Layer-E.** | Revisit if a learned reranker changes score separability, or if Layer-E shows the agent missing a trap class. |
| D13 | 2026-06-26 | **Citations must cover the question's constraints, not just the answer token** (generalizes the date-binding fix). The agent decomposes each query into a typed **question frame** — entity, attribute/metric, qualifiers (period/unit/scope), comparison, polarity — emitted as structured JSON. `verify` extends from *answer-atom* coverage to **constraint coverage**: every frame constraint must resolve to a span *within the cited evidence*. Prose: the connecting clause is a span carrying both subject and attribute ("the wheel well is red"). Tables: the constraint maps structurally to row-label + column-header + cell. **No new model in v1 — the agent is the parser**; CAIRN's half (coverage) stays deterministic. A dedicated lightweight parser is a later optimization / the v2-service path. | Lifts the runtime guarantee from "the answer exists in the text" toward "the evidence demonstrably answers *this* question" — converting a large chunk of entailment from offline-judged into runtime-checkable. Same shape as the patent claim→spec support mapping (PE-3: limitation → spec spans). | **Necessary, not sufficient** for entailment: negation ("not red"), attachment ("the fender is red, beside the wheel well"), and coreference ("it is red") survive coverage and stay with the Layer-E judge / human. Revisit the frame schema with the first real engagement. |
| D14 | 2026-06-26 | **Prefer the nearest substantiation.** When an atom's literal occurs in multiple places, bind it to the occurrence **nearest the answer's other atoms** (minimize inter-citation distance), not the first match — so a reviewer sees the figure and its qualifier together. Enabling work for the EDGAR date case: normalization currently splits the balance-sheet column header (`September 28,\n2024`), so the nearest *contiguous* "September 28, 2024" is 3,584 chars away (cash-flows section) and the binding jumped to the cover. Fix = a **normalization improvement** (keep dates / table headers coherent) — re-hashes the corpus (re-ingest + re-bind golden quotes; resolution invariant catches drift), and also improves readability. | Proximity makes auditing trivial (figure + date in one eyeful) and reduces accidental wrong-context citations. The split-date shows flat normalization hurts grounding, not just display. | Fuller fix is a structure-aware ingestion (rows/headers/cells) — native to the patent doc model (PE-1); the targeted date-join is the minimal step. |
| D15 | 2026-06-27 | **Evidence display is interaction-scoped, with per-cluster bounding boxes.** The evidence view is **clean by default**; clicking an interaction's card lights **only that interaction's** highlights (figures + question-label terms, in both panes) and draws a **tight bounding box around each contiguous evidence cluster** in the document (one box per cluster, never one box spanning unrelated text). Clicking a figure also scrolls to it; clicking the active card clears. | Showing every query's highlights at once is ambiguous ("which belongs to which?"); scoping to the clicked interaction makes auditing one figure's evidence clean. Per-cluster boxes degrade gracefully on scattered evidence (G007 → 2 boxes; figure+period → 2 boxes) where a single enclosing box would be meaningless. | If a single interaction's clusters become very numerous (dense prose), revisit the gap threshold or add a "next cluster" control. |
| D11 | 2026-06-25 | **The agent reads freely; grounding constrains output, not input.** CAIRN does **not** restrict the agent to retrieval hits. It may read whole documents (`get_document`; `get_span(doc, 0, len)`) — `search_corpus` is a navigational aid, not a cage. The only hard constraint is on **output**: every asserted claim binds to a verified span (I1), enforced by `verify` / the atom resolver (D9). Adds `get_document(doc_id)` to the span store now and to the M4 MCP surface (+ `read_section` once the patent document model exists). Reconciles D9: "no whole-corpus fishing" means a citation must be **offset-specific** and its relevance **judged** (Layer-E), *not* that input is restricted. | Most grounding errors come from missing context (current-vs-non-current, the "in millions" caption, temporal scope), not excess context. Broad reading + strict output-grounding is both more capable and more accurate; the guarantee lives at `verify`, not in starving the input. Critical for patents: claim construction needs the full spec/figures. Scale caveat: read whole *documents*, with retrieval narrowing *which* docs in a large multi-doc set. | If context-window limits force snippet-only reading on very large single documents, revisit chunked-read strategies. |
| D10 | 2026-06-25 | **First client engagement retargets the corpus from EDGAR to a patent refresh-and-update** (see [`CAIRN_Patent_Tailoring_Consideration.md`](CAIRN_Patent_Tailoring_Consideration.md), [`CAIRN_Client_Intake_Questions.md`](CAIRN_Client_Intake_Questions.md)). EDGAR 10-K remains the **architecture-proving reference build** (M0–M5); patents are a **specialization of the same engine**, tracked in a new **Patent Engagement** section. The corpus-agnostic engine is shared and keeps being built EDGAR-first (current focus stays M1-T3). Patent-domain cardinal rule, sharpened: **locate & evidence, never adjudicate** — never conclude on novelty/obviousness/validity/infringement/FTO/definitive claim construction (refusal class + Layer-E *negative* evals; UPL boundary, professional in the loop). §10 open decisions (mechanism, jurisdiction, published/confidential, prior-art, output, portfolio) are **DO NOT INVENT** → routed to intake; the two blocking ones are mechanism (Q2) and confidentiality (Q17). Patent work that exceeds the shared engine (document model, typed provenance, structural checks) is a **domain pack**, not a one-file corpus swap. | EDGAR proved the core cheaply and its tests guard the shared engine; patents are the revenue target but most domain-specific choices are still unknown. Building the engine EDGAR-first then layering the patent domain pack reuses proven, tested primitives (Document/I3, SpanStore/D7, eval harness) and avoids rework while intake resolves the unknowns. | Reconcile again once intake answers land (esp. Q2 mechanism, Q17 confidentiality); revisit if a confidential/unpublished patent forces local-only handling that changes the engine. |

---

## M0 — Audition rig  ·  `DONE`
**Goal:** prove the risky core cheaply before building anything real (brief §2).
**Gate:** on the ~20-item toy set — citation precision high, hallucination 0 on answerable items, **abstains on 100% of unanswerable items**. If it can't, iterate the rig; do not proceed.
**Gate met (2026-06-24):** `cairn_rig.py` → answer correctness 100%, citation precision 100% (gate ≥90%, D5), recall 100%, hallucination 0%, abstention accuracy 100% on all 7 unanswerable items, 0 false abstentions. Standing test `tests/test_rig.py` locks the gate into CI.

- [x] **M0-T1** · `branch: chore/scaffold` — Repo scaffold: directory layout, env, deps, place `CAIRN_build_brief.md` + this file, CI skeleton. **AC:** clean install runs an empty test suite green. **DONE** — src-layout `cairn` package, pytest+ruff, CI skeleton; clean-venv install runs ruff + smoke suite green; CI green on merge.
- [x] **M0-T2** · `branch: feat/m0-toy-corpus` — Assemble 5–10 EDGAR excerpts as the toy corpus. **AC:** raw text stored verbatim; provenance (ticker, form, accession) recorded per excerpt. **DONE** — 5 verbatim excerpts from AAPL FY2024 10-K (cover, operations, balance sheets, cash flows, auditor report) in `corpus/toy/`; provenance + per-excerpt sha256 in `manifest.json`; rebuilt deterministically by `scripts/build_toy_corpus.py` (raw cached under gitignored `data/raw/`); standing integrity test in `tests/test_toy_corpus.py`. **Finding for human review:** golden item **G008 labels are inverted** — the filing reports *current* marketable securities $35,228M and *non-current* $91,479M; G008 calls $91,479M "current." Fix in M0-T3's verification pass, not by an unreviewed agent edit.
- [x] **M0-T3** · `branch: feat/m0-golden-seed` — Hand-label ~20 golden items in the **quote + locator** schema (brief §3, D7), **≥5 deliberately unanswerable**. **AC:** schema-valid; answerable/unanswerable split recorded; honesty fields (`value_seen`/`source_status`) present. **DONE — see `golden_seed.json` (20 items, Apple FY2024 10-K). Still requires human verification pass (fill `verbatim_quote`s from canonical text; confirm `unverified_from_memory` items G008 non-current, G009 auditor).**
- [x] **M0-T4** · `branch: feat/m0-rig` — `cairn_rig.py`: trivial retrieval + draft-from-spans + verify + abstention + inline metrics (a Python stand-in for the agent loop, to prove the core). **AC:** runs end-to-end on the seed; prints the four gate metrics; **meets the M0 gate.** **DONE** — BM25 (unigram+bigram) over line/block spans with section breadcrumbs; citation = score band (plural-and-ranked); verify = assert only values present in cited spans; abstention via relevance threshold + agent-judgment guards (temporal / entity-scope / false-premise / not-disclosed-in-10-K). Gate passes (see milestone line). Guards are documented stand-ins for the runtime agent's reasoning — the real reasoner replaces them at M2+.

---

## M1 — Ingestion + retrieval + span store  ·  `DONE`
**Goal:** subsystems 1–2 (brief §1). Evidence layer becomes real and immutable.
**Gate:** span hashes verify (I3); retrieval reproducible across two runs (I6); unit tests green.
**Gate met (2026-06-25):** I3 hash re-verify + tamper rejection (`tests/test_ingest.py`); all 21 golden quotes bind 1:1 (`tests/test_spans.py`); retrieval byte-identical across runs (`tests/test_retrieval.py`); 27 tests green. Known carry-forward: G009 (auditor) lexical-gap recall miss → M2 retrieval tuning (embedding backend / section-aware chunking).

- [x] **M1-T1** · `branch: feat/m1-ingestion` — EDGAR adapter: fetch + normalize + **content-hash at ingest** (I3). Corpus-specific code lives *only* here. **AC:** every stored doc carries its hash; standing I3 hash test passes. **DONE** — `src/cairn/ingest/` package: `document.py` (Document + sha256 content hash + `verify_document`, corpus-agnostic), `store.py` (on-disk store; re-verifies hash on every load), `edgar.py` (the *only* corpus-specific module — filing registry, fetch+cache, deterministic HTML→canonical-text normalization). Apple FY2024 10-K ingested to `corpus/store/` (219K chars, hash in `meta.json`) via `scripts/ingest_corpus.py`; M0's `build_toy_corpus.py` now reuses `edgar.normalize` (no duplicated corpus code). Standing I3 tests in `tests/test_ingest.py`: hash re-verifies, tamper (text or hash drift) rejected, normalization deterministic (I6), golden evidence preserved.
- [x] **M1-T2** · `branch: feat/m1-spanstore` — Chunk + span index by char offset `(doc_id, start, end)`. **AC:** `get_span` returns the exact slice and re-verifies against the doc hash; mismatch raises a hard failure. **DONE** — `src/cairn/spans.py`: deterministic line-level `chunk_document` (exact offsets, `canonical_text[start:end] == span.text`), `SpanStore.get_span` (re-verifies doc hash via `verify_document` before serving; out-of-range → `SpanError`), and `resolve_quote` enforcing the **resolution invariant** (D7: exactly-once, else `ResolutionError`). All **21 golden `verbatim_quote`s bound 1:1** to the canonical text via `scripts/resolve_golden_quotes.py` (G016 split into current/non-current so each resolves singly; `source_status` → `grounded`). Standing tests in `tests/test_spans.py`: exact-slice, hash re-verify on drift, deterministic chunking (I6), resolution invariant, duplicate/missing quote rejected.
- [x] **M1-T3** · `branch: feat/m1-retrieval` — Hybrid retrieval (BM25 + single embedding model), deliberately simple (brief §8). **AC:** returns candidate spans with offsets; **standing I6 reproducibility test** passes (identical results, two seeded runs). **DONE** — `src/cairn/retrieval.py`: deterministic Okapi BM25 (unigram+bigram features; standard query stopword/bare-number normalization) behind a corpus-agnostic `RetrievalBackend` interface; `Retriever` facade is multi-document-ready (patent track reuses it). Returns `Hit(span, score)` whose offsets round-trip through the hash-verified store. **Embedding half deferred** (D-note): seam left open (add a backend + RRF fusion) so CI stays offline/deterministic — BM25-only already gives recall@20 12/13 on the golden set. Standing tests in `tests/test_retrieval.py`: offsets resolvable, **I6 reproducibility** (byte-identical across independent runs), golden recall (all answerable but G009).

---

## M2 — Deterministic verify + abstention tools  ·  `TODO`
**Goal:** the agent composes; CAIRN provides deterministic `verify` + `check_support` and the eval split goes live (brief §3, §4). No model calls inside CAIRN (D6).
**Gate:** all **Layer-0 component evals** pass on the golden set — span resolution 1:1, citation integrity (I3), retrieval recall, abstention trigger 100% on unanswerable, `verify` rejects planted ungrounded claims, invariant tests (I3–I6) green.

- [x] **M2-T1** · `branch: feat/m2-verify` — `verify(answer_with_tags)` tool (I1): deterministically resolve every cited span, confirm hash-match, flag any sentence with no valid binding. Implements the **atom-resolver contract (D9)**. **AC:** a planted unbound claim is flagged; a clean answer passes; a number bound to the wrong offset (same digits, different line) is *not* falsely passed by binding to the literal alone; `verify`'s own atom extraction flags an untagged load-bearing token; result is logged. **DONE** — `src/cairn/verify.py`: `Answer`/`Sentence`/`AtomBinding`/`DerivedAtom` model; `verify` resolves each atom at its exact offset (whitespace-normalized literal match), re-verifies the doc hash (I3) and a stale binding-hash, **independently re-extracts figures** from the prose and flags any unbound, and **recomputes derived values** from bound operands (G005/G019 deltas). Returns a complete `VerifyResult` record. Standing tests in `tests/test_verify.py` (clean pass, planted-unbound flagged, wrong-offset mismatch, stale-hash, out-of-range, derived recompute + wrong-derived). **Note:** independent extraction requires **figures, percentages, and full dates** ("Month DD, YYYY") to be bound — a reviewer caught an ungrounded "as of <date>", so the period is now a mandatory atom (2026-06-26, extends D9). Named entities are verified *when bound* but not yet independently required (patent domain pack adds claim-term/numeral extractors). Result **persistence** lands with the audit log at **M3-T1**.

  <br>**Open contingencies for the resolver (D9) — decide while building, don't pre-bake:**
  - **Atom taxonomy** — which token types are mandatory-bound (numbers, currency, %, dates, named entities) vs free prose; how the agent demarcates them (reuse D8's `span_id` tags).
  - **Match normalization** — exact literal, but define whitespace handling (`"$ 364,980"` vs `"$364,980"`), string case, and **word-boundary** matching so `"100"` ≠ inside `"100,544"`. Resist numeric-fuzzy matching — it's a new trust surface.
  - **Accounting formats** — negatives in parens `"(19,154)"`, thousands separators, and scale/units stated in a table caption (the "million" lives in the header, not the cell → its own atom resolving to the caption span).
  - **Multiplicity** — 0 matches → abstain; 1 → bind; many → bind to a *specific offset* (the `364,980`-appears-twice case must verify at the right line, not merely "exists"); plural answers surface all (brief §4).
  - **Scope** — resolve against spans retrieved for *this* query, not the whole corpus (prevents out-of-context fishing).
  - **Independent extraction** — `verify` re-extracts candidate atoms from the final answer and requires coverage, so the agent can't evade by leaving a figure untagged.
  - **Derived values** — operands-as-atoms + a recomputed operation; the result itself is not cited (e.g. G005/G019 deltas).
  - **Offset stability** — bindings carry the doc `content_hash`; a stale offset against a new hash is a hard fail (I3).
  - **Logging** — every resolve/verify call appended to the audit log for replay (I5).
  - **Residual (state plainly, don't overclaim)** — right-number/wrong-*meaning* and misphrasing are *not* caught here (existence ≠ entailment); that's Layer-E offline, structural in v2.
- [x] **M2-T2** · `branch: feat/m2-support` — `check_support(question)` (I2): returns supporting spans or `insufficient` via the relevance threshold — the deterministic abstention decision. **AC (reconciled by D12):** returns `insufficient` on 100% of the **content-absent** unanswerable items (G011/G012/G013) with the closest spans shown; returns the gold span among supporting on answerable items (except the G009 lexical gap → retrieval tuning). Semantic-trap unanswerables (G014/G015/G016/G020) retrieve real content and are the **agent's** abstention, measured at Layer-E — *not* this tool. **DONE** — `src/cairn/support.py`: `check_support` runs the retriever, returns spans clearing a calibrated floor (`THRESHOLD=15.0`, per-corpus) ranked/plural, else `insufficient` + closest. Standing tests in `tests/test_support.py`.
- [x] **M2-T3** · `branch: feat/m2-plural` — `check_support` returns **all** qualifying spans (plural), ranked, never collapsing to one (brief §4; golden G007/G008). **AC:** multi-answer item returns ranked alternatives, each with its own evidence. **DONE** — fell out of the M2-T2 design (`check_support` returns every span clearing the floor, ranked by score, each carrying its own span = evidence). Verified: G007 surfaces both term-debt lines (current $10,912M + non-current $85,750M, equal score, adjacent ranks) and G008 both marketable-securities lines (current $35,228M + non-current $91,479M) — neither collapsed. Standing test `test_plural_items_surface_all_gold_spans_ranked` in `tests/test_support.py`.
- [x] **M2-T4** · `branch: feat/m2-claude-md` — Project `CLAUDE.md` documenting the required agent loop: search → draft from returned spans only → `check_support` → `verify` → present-or-abstain. **AC:** loop is unambiguous; references the tool contracts. **DONE** — expanded the `CLAUDE.md` "Runtime agent loop" in place: a **tool-contract table** (as-built Python signatures + return shapes for `search`/`check_support`/`get_span`/`get_document`/`verify`; `check_claim`/`get_audit_log` marked forthcoming) and the `Answer`/`Sentence`/`AtomBinding` shape, plus the refined loop with the **two abstention mechanisms (D12)**, read-freely (D11), plural-and-ranked, and the patent **refuse-to-adjudicate** class (D10). MCP names land at M4; the contracts already match the M1/M2 implementations.
- [x] **M2-T5** · `branch: feat/m2-layer0` — Layer-0 deterministic component-eval suite as required CI (brief §3). **AC:** PR shows the Layer-0 gate table; suite is fast and deterministic. **DONE** — gate table at [`docs/layer0_gate.md`](docs/layer0_gate.md) maps each eval → invariant → test (span resolution D7, citation integrity I3, retrieval recall/repro I6, abstention trigger I2/D12, `verify` I1/D9, plural §4, deterministic ingest; I4/I5 marked ⏳ M3). `layer0` marker registered; `conftest.py` auto-tags everything under `tests/` so the gate runs as `pytest -m layer0`; CI step renamed "Layer-0 deterministic gate." **39 evals, 0.70s, no model calls.**
- [~] **M2-T6** · `branch: feat/m2-layer-e` — Layer-E agent end-to-end eval: drive the Claude Code agent over the golden set in headless mode; score the transcript (hallucination/entailment via judge, abstention correctness, calibration → Brier + reliability curve to a results file). **AC:** runs on demand; produces the trend file. **Periodic, NOT a blocking CI gate.** **BUILT — one live pass yields the full metric set.** Scoring works **from the audit log** (not free-text transcripts): `cairn.layer_e` scores abstention correctness + verify-catches per item from each item's log segment (deterministic); the **entailment LLM-as-judge** (pluggable `ask`, so its logic is gate-tested) rules whether each cited span supports its claim; **calibration** (Brier + reliability) over the agent's stated `Confidence: 0.NN` vs whether it entailed. Standing `tests/test_layer_e.py` (12 tests, in the gate) covers scorer + judge-parsing + calibration with injected fakes. `scripts/run_layer_e.py --live` drives the agent, judges, calibrates → trend `audit_log/layer_e_results.jsonl`. **DONE** — ran on the full golden set 2026-06-28; first trend + analysis captured in [`docs/layer_e_baseline.md`](docs/layer_e_baseline.md) (abstention 0.86, answer 0.92, entailment 0.69, Brier 0.30). Remaining quality work (own follow-ups, not blocking): scorer credit for grounded false-premise rejection; agent plural-answering; calibration.
- [x] **M2-T7** · `branch: feat/m2-evidence-view` — `render_evidence_view`: a deterministic, server-less generator that turns one verified interaction (a `verify` result over a span-tagged answer) into a **self-contained two-pane HTML** — canonical document on the left with `<mark>` highlights, answer on the right with click-to-source hyperlinks (D8). Reads the span store + verify output; mutates nothing (I4). **AC:** for a sample tagged answer, every cited sentence hyperlinks to the exact highlighted span in the document pane; an unbound sentence is shown flagged, not silently linked; opening the HTML needs no server or network. **Not part of the M2 Layer-0 gate** — a deliverable that rides on `verify`; M5 upgrades it to the React replay app. **DONE** — `src/cairn/evidence_view.py` (`Interaction` + `render_evidence_view`); `scripts/build_evidence_view.py` writes `evidence_view.html` (6 demo interactions: clean lookup, derived, plural, content-absent abstain, temporal abstain, false-premise reject). Click a highlighted figure → its verbatim source span flashes; unbound figures shown with ✗ verify; pure file, no server. Standing tests in `tests/test_evidence_view.py`. (Answer prose is curated from golden until the live agent composes it at M4; citations/highlights/verify/abstention are real tool output.)
- [x] **M2-T8** · `branch: feat/m2-question-frame` — **Question frame + constraint coverage (D13).** Agent decomposes a query into a typed frame (entity / attribute-metric / qualifiers: period, unit, scope / comparison / polarity) as JSON; extend `verify` so the cited evidence must **cover every constraint**, not just the answer atom (the cited span(s) carry the question's subject + attribute + qualifiers — the "connecting clause"). **AC:** an answer that cites the answer token but not the question's subject/attribute is flagged `coverage-incomplete`; a span covering both passes; tabular constraints map to row-label + column-header + cell. Coverage is necessary-not-sufficient (negation/attachment/coreference → Layer-E). Frame is logged (I5, M3) and shown in the evidence view. *(The date-binding fix is the first instance.)* **DONE (live path landed 2026-07-06; truth contract → 1.2)** — `src/cairn/frame.py` (`QuestionFrame`/`Constraint`/`check_coverage` + the JSON bridge and `coverage_for_answer` over the cited atoms' **containing spans**, identical semantics to the view); the **`verify` tool accepts `frame`**, returns + logs `coverage` (replayable — re-derived on replay, I6); **the loop rule is binding: present only if `ok AND coverage.complete`** (CLAUDE.md step 5 + `/ground`); `interactions_from_audit` reconstructs logged frames so live sessions render the coverage strip. Acceptance case at tool level: the naive citation (364,980 from the liabilities+equity line) → **verify ✓, coverage ✗, presentation blocked**. `tests/test_tools.py` +3, `tests/test_session.py` +1. **Residuals (tracked in D13's revisit):** Layer-E measures live-frame adherence next run; tabular row×column structural mapping still text-substring; negation/attachment/coreference remain Layer-E.

---

## M3 — Audit log  ·  `DONE`
**Goal:** subsystem 5 (brief §1; I4, I5).
**Gate:** append-only + full-replay tests pass; write-asymmetry (I4) test passes.
**Gate met (2026-06-26):** tamper-evident append-only log (`test_audit`), byte-identical replay (`test_session`), read/write asymmetry (`test_i4`) — all green; Layer-0 gate's I4/I5 rows now filled.

- [x] **M3-T1** · `branch: feat/m3-audit` — Append-only log of query / retrieval set / answer / citations / abstention / confidence (I5). **AC:** entries immutable; tampering test fails the build. **DONE** — `src/cairn/audit.py`: `AuditLog`, a JSONL **hash-chained** log (`entry_hash` covers `seq+prev_hash+payload`); append-only API (no update/delete); `verify_chain()` detects edit/reorder/deletion → `TamperError`. Standing `tests/test_audit.py`.
- [x] **M3-T2** · `branch: feat/m3-replay` — Reconstruct any past interaction from the log alone. **AC:** replayed interaction is byte-identical on the evidence path (I6). **DONE** — `src/cairn/session.py`: canonical interaction records + `replay_support` re-derives the evidence path from the logged query alone; `replays_identically` confirms byte-identical reproduction. Standing `tests/test_session.py`.
- [x] **M3-T3** · `branch: feat/m3-asymmetry` — Enforce + test I4: agent can write **only** the log; corpus is read-only. **AC:** standing I4 test attempts a corpus write and is rejected. **DONE** — `tests/test_i4.py`: the agent-facing read path (get_document/get_span/retrieval/check_support/verify) leaves the corpus byte-identical; read classes expose no mutator; `AuditLog.append` is the only append surface. (Full enforcement at the MCP tool boundary lands at M4-T3.)

---

## M4 — MCP server + CLI (primary v1 interface)  ·  `TODO`
**Goal:** subsystem 8 (brief §1, §5). The only interface Claude Code uses. Read/write asymmetry enforced at the tool boundary.
**Gate:** boundary asymmetry test; the documented agent loop runs end-to-end over the tools on the golden set.

- [x] **M4-T1** · `branch: feat/m4-mcp-scaffold` — Server + CLI mirror + tool registration. **AC:** server starts; tools enumerated; CLI invokes the same functions. **DONE** — `src/cairn/tools.py` (`Tool` + `default_registry`: the single registry both interfaces share); `cli.py` + `python -m cairn` (`list` / `call <tool> <json>`) wired to `search_corpus`/`get_span`/`get_document`/`check_support`/`check_claim` (+ `get_audit_log` when an audit path is given); `mcp_server.py` (`build_server`, **optional `mcp` dep**, lazily imported so the Layer-0 gate stays stdlib-only); `cairn` console script. Standing `tests/test_tools.py` (registry enumerated, CLI invokes the same functions, MCP adapter builds — `importorskip`). Verified: `cairn list` + `cairn call get_span …` return real results; `build_server` constructs with the live SDK. `verify` tool + per-tool schemas land at M4-T2; log side-effects at M4-T3.
- [x] **M4-T2** · `branch: feat/m4-tools` — Expose `search_corpus`, `get_span`, `get_document` (read-freely, D11), `check_support`, `verify`, `check_claim`, `get_audit_log` (brief §5). **AC:** each tool's contract tested; `get_document` returns the full hash-verified text; `check_claim` resolves a user claim to supporting spans or none. **DONE** — `verify` wired via `answer_from_json`/`result_to_json` (the logged verify record stores the answer → replayable, I5/I6); every tool carries a real JSON-Schema `input_schema` advertised verbatim by the MCP adapter. Contract tests in `tests/test_tools.py` (verify bound/unbound, check_claim backed/unbacked, get_audit_log replay).
- [x] **M4-T3** · `branch: feat/m4-boundary` — Read tools have no side effects; `check_support` / `verify` / `check_claim` append to log only; corpus never writable (I4). **AC:** boundary test confirms read/write asymmetry. **DONE** — asymmetry is **structural**: only the three write tools close over the audit log (read tools hold no log reference, so they can't write even by mistake). Standing `tests/test_boundary.py`: reads have zero side effects (log+corpus byte-identical), each write tool appends exactly one entry, chain verifies, every record replays byte-identically, no-log degraded mode works. *(Done by a second agent; reviewed + merged.)*
- [x] **M4-T4** · `branch: feat/m4-agent-loop` — Run the actual Claude Code agent loop over the MCP tools on the golden set. **AC:** agent searches, drafts, calls `check_support` + `verify`, and abstains where required — end-to-end through MCP. **DONE** — the real headless agent ran all 20 golden items live through the CAIRN MCP server (`.mcp.json`; server logs to `audit_log/agent.jsonl`, I5; all 7 tools advertised), 0 errored. Auth: `claude -p --bare` (ANTHROPIC_API_KEY only, never keychain/OAuth) + `bypassPermissions` + `--add-dir` (re-loads CLAUDE.md). **First Layer-E baseline** ([`docs/layer_e_baseline.md`](docs/layer_e_baseline.md)): abstention 6/7 unanswerable (7/7 once G020's grounded false-premise rejection is credited), answer 12/13, `verify` caught 21 ungrounded drafts, entailment 69%, overconfident calibration (Brier 0.30). Follow-ups surfaced: scorer should credit grounded false-premise rejection (G020); agent under-answered a plural (G008); split agent-over-claim vs judge-strictness on the entailment misses.

---

## Layer-E quality follow-ups  ·  `TODO`
**Goal:** close the gaps the first live baseline surfaced ([`docs/layer_e_baseline.md`](docs/layer_e_baseline.md)). Periodic/quality work, not a blocking gate — pick up alongside DX/PE.

- [ ] **LE-1** · **Plural-and-ranked answers (G008 miss).** On the live baseline the agent **abstained** on "how much does Apple hold in marketable securities?" even though `check_support` returned *supported* (score 19.7) — it should have surfaced **both** values (current $35,228M + non-current $91,479M), ranked, each with its own span (brief §4; the second plural case). Likely a loop-guidance gap: strengthen the plural-and-ranked step in `CLAUDE.md` / the `/ground` command so the agent returns all defensible values rather than abstaining when there are several. **AC:** a re-run answers G008 with both marketable-securities figures, each bound to its own span and `verify`-ok; no regression on the other answerable items. *(Verify via `scripts/run_layer_e.py --live`; non-deterministic, so AC is "answers G008 with both figures," not an exact score.)*
- [ ] **LE-2** · **Calibration + judge-strictness split.** Baseline was overconfident (stated 0.98 vs 0.69 actual) and some `entailment=NO` verdicts were the agent over-claiming beyond its cited spans (the judge-401 cause is fixed). Separate "agent over-claim" from "judge too strict" on the remaining misses; tighten the loop so claims don't exceed their spans, and re-baseline. **AC:** a fresh `--live` trend with clean entailment numbers + a short note on each remaining non-entailment.

## Review & corpus tooling  ·  `TODO`
**Goal:** make the review + corpus surfaces first-class for engagements (DX / M5-adjacent; several also improve the current static evidence view). User ideas, 2026-06-28.

- [~] **RT-1 · Organized session history.** Browse past interactions grouped by session/time. **FIRST INCREMENT DONE (2026-07-06)** — `session.session_start_record` (pure metadata; ts supplied by the adapter — cores stay clock-free; chain-covered I5, trivially replayable I6); the **MCP server appends a marker per spawn** (label via `CAIRN_SESSION_LABEL`) and the Layer-E runner labels each run; `evidence_view.sessions_from_audit` groups entries (pre-RT-1 logs read as one "(unmarked)" session); `build_evidence_view.py --from-audit --sessions | --session N` (+ the new `--store` flag — fixed a pre-existing gap where engagement logs crashed against the hardcoded EDGAR store). Tests: marker replay/chain; grouping. **Remaining:** in-page session navigation (a session list/dividers inside the HTML — rides with RT-5/M5); the structured refusal record this unblocks (D22 revisit). **AC (full):** the view lists sessions → their interactions, jump to any, each still links to its spans. Feeds M5 (replay).
- [x] **RT-2 · Corpus-management GUI.** List a store's documents (id, title, content-hash, size), ingest new docs, remove, and switch engagement / `CAIRN_STORE` — without hand-editing the store or writing Python. **CLI ingest half DONE** — `cairn.ingest.files` (`ingest_paths`/`ingest_file`, corpus-agnostic, plain-text; HTML/PDF/XML deferred to a PE-1 adapter) + `scripts/ingest_files.py --store … [--kind]`; `tests/test_ingest_files.py`. Verified end-to-end: ingest a `.txt` → point `CAIRN_STORE` at it → retrieval/spans/tools work. **Remaining:** the GUI (list/remove/switch) — the patent **matter-management** surface (PE). **AC (GUI):** view + ingest + remove docs from a UI.
- [x] **RT-3 · Distinct rendering + color for corrections (and partials).** **DONE.** The evidence view now highlights + draws the answer-style card for all *presenting* classes (`_PRESENTS` = answer/correction/partial, not just `answer`); `correction` gets its own colour (violet `--corr`, distinct from red-abstain and green-answer) on the badge and the active card border; the card carries its kind. Live path wired (layer 2): `verify` takes an optional `outcome` (D16 enum), logged in the verify record (replay-safe, I6) and read back by `interactions_from_audit`; `CLAUDE.md` + `/ground` instruct the agent to pass it. Curated demo card #6 is now a real grounded `correction` (binds 352,583 + 364,980). Tests: `test_correction_renders_distinctly`, `test_from_audit_tags_outcome`. **AC met** — a correction is visually distinct and its contradicting spans highlight. *(Your earlier Sept-30 correction was logged before `outcome` existed, so it still shows as `answer`; new `/ground` corrections colour correctly.)*

- [x] **RT-9 · Per-corpus calibration must be required, not merely possible** (RT-6's second-corpus run, 2026-07-28). Two of the three constants that failed on US8046721B2 could not be fixed by changing a number — each needed a *mechanism*. (a) **`support.THRESHOLD` is a BM25 score, so it is meaningless off its calibration corpus.** Measured: "how is the device unlocked" — the patent's entire subject — scores **4.56** against the EDGAR-calibrated floor of **15.0**, so Cairn would return `insufficient` on an answerable question. A **false abstention**: the failure mode that looks responsible and is therefore easy to ship. `scripts/calibrate_threshold.py` exists; nothing *requires* running it, and no store records which corpus its threshold was calibrated against. (b) **`acronym_labels` over-generates by design wherever no OCR'd drawings exist** — it deliberately has no frequency floor because *drawings adjudicate*, so without that adjudicator it returned 34 "labels" on US8046721B2 including CDMA/GSM/CPU/IEEE and the section headings BRIEF and FIELD. It must not be consumed without drawing evidence. **AC:** a store carries a calibration record (corpus id + hash + date + the calibrated value), `check_support` refuses or loudly warns on an uncalibrated store rather than silently applying a foreign threshold, and `acronym_labels` requires an adjudicating source or returns its output tagged unadjudicated. **DONE (2026-07-28, D44).** `src/cairn/calibration.py`: a store carries `calibration.json` (threshold + corpus id + doc ids + **corpus hash** + date + method + n), and `resolve()` returns the floor with an honest account of where it came from — *calibrated* / **STALE** (corpus hash moved since the fit) / **UNCALIBRATED** (foreign floor). The warning rides at the **top level** of the `check_support` record, not inside `provenance`, because it qualifies the *result* rather than the machinery — and it survives replay verbatim (I6), so a replay cannot launder the caveat away. `calibrate_threshold.py --write YYYY-MM-DD` records it (date supplied, never read from the clock — cores stay clock-free). EDGAR calibrated at **15.1**. An explicit `support_threshold=` still **wins** and is labelled `EXPLICIT OVERRIDE` — silently overriding a caller's stated intent is the same defect this removes. For `acronym_labels`: all three callers were audited and already adjudicate against located sightings; the contract is now stated in the docstring with the corpus-2 evidence and pinned by a test. **Rationale:** the fix for a constant that does not transfer is usually not a better constant.

- [x] **RT-8 · Layer-E must grade the CITATION, not just the verdict** (attestation-landscape swarm, 2026-07-28). `layer_e.score_item` computes `decision_correct = (presented == expected)` and never reads the golden item's `supporting[]` — so every headline metric is *label-only accuracy*, for a system whose entire product claim is span provenance. FEVER's published 50.91% → 31.87% drop when evidence is scored conjunctively is the size of the illusion this hides. `scripts/resolve_golden_quotes.py` already binds each `verbatim_quote` 1:1 to a span, so this is a scorer change, not a research project. Two adaptations FEVER cannot supply: match by **offset overlap against a set** of acceptable gold spans (a figure recurs across a 10-K), and extend conjunctive scoring to `abstain` (D12 `closest`) and `refuse` (D22 locate-first), which FEVER structurally has no class for. **DONE (2026-07-28).** `gold_spans` resolves each `supporting[].verbatim_quote` to offsets (a SET — a figure recurs across a 10-K; D7's resolution invariant makes an ambiguous quote raise rather than bind silently); `cited_spans` recovers what the agent actually bound from the logged answers (atoms + derived operands); matching is by **offset overlap**, since a tighter slice (`364,980`) and the wider gold quote (`Total assets $ 364,980 $ 352,583`) point at the same evidence. `aggregate` now publishes `evidence_accuracy`, `evidence_n`, `evidence_unscoreable`, `evidence_failures` and **`decision_minus_evidence`** — the gap, not just the pair. Unscoreable reads as `null` + a note, never as 0% or clean. Verified on the real audit log: 48 citations recovered, 12/15 presenting items' gold spans cited, 3 not. +3 tests. **Related:** a no-corpus control arm (EDGAR facts are near-certainly in pretraining; the EDGAR-vs-patent gap is itself the leakage measurement) and a risk–coverage curve / AURC, since `abstention_accuracy` and `answer_rate` are two points on one curve that `support.THRESHOLD` slides along.

- [x] **RT-6 · Corpus-fitting protocol — the agent-assisted procedure for pointing CAIRN at a NEW corpus** (Julian, 2026-07-26). The premise: *the deterministic architecture will not catch every case, and pretending otherwise is the failure mode.* Every extraction constant in this repo (`_DIGIT_RUN`'s digit cap, `_NOT_ELEMENT`'s function words, the 0.02 merge radius, `_HEADER_BAND`, `MIN_LOCATABLE_NUMERAL`, the rotation set, the a↔0 confusion table) was fitted to **one 1995 mechanical patent**, and each was discovered by a human noticing a miss. A second corpus will break constants nobody has identified as constants. So the deliverable is not more rules — it is a written, repeatable **procedure** an agent follows when fitting the scaffolding to a corpus, whose steps are the ones that historically caught these: (1) inventory every corpus-fitted constant and its provenance (what evidence set it, when); (2) run the extractor over the new corpus and inspect **what it discards, not only what it keeps** (L0006, five instances); (3) probe for vocabulary the extractor cannot express *before* trusting a clean coverage report (D34's empty-map failure); (4) test each instrument on a known-positive before recording a negative result (L0009/L0010); (5) blind-annotate one document before looking at any overlay, as the only uncontaminated recall stratum; (6) record the fit in a per-corpus profile with its evidence, so the next agent inherits the reasoning and not just the number. **FIRST INCREMENT DONE (2026-07-28).** `src/cairn/corpus_fit.py` registers **17 fitted constants — 12 of them (70%) corpus-specific** — each with `scope`, the evidence that set it, and a **falsifier** (the observation that would show it does not transfer). Enforced, not asked for: `tests/test_corpus_fit.py` fails when a scalar is retuned without updating its provenance, and when a new tunable appears in a fitted module unregistered and unexempted; both proved on known-positives before being trusted (L0010). `docs/corpus_fitting.md` is the 6-step procedure, built from the moves that historically caught these failures; `scripts/corpus_fit_report.py` prints the inventory as a new-corpus test plan. **Remaining for the full AC:** run it end-to-end on a second patent and produce the written fit record — until then the doc is a hypothesis about what will help, and says so. **Rationale:** what does not generalize should be *visible* as not-general. A constant with no recorded provenance is indistinguishable from a law.

- [x] **RT-10 · The console — one frame around every surface.** Cairn's engine was never the weak part; **cohesion** was. Four generators each produced a complete, self-contained page, and a reviewer opened one, read it, closed it, and re-oriented from scratch on the next. **FIRST INCREMENT DONE (2026-07-28, D48)** — `src/cairn/console.py` + `scripts/build_console.py`: a tabbed frame over the existing generators (which still emit standalone pages — that property is worth keeping, since a single page is what gets emailed and archived), carrying a **persistent header** with the corpus, its calibration state, the reviewer-judgment count and the contract version. Panes with nothing to show say **what is missing and why** rather than disappearing. **LOCATE PANE DONE (2026-07-28, D49)** — the front door: type a question, get the support decision and its evidence, recorded to the audit log exactly as an agent's call would be. **ADJUDICATE PANE DONE (2026-07-28, D50)** — the review queue, with judgment recorded in place. **MARK-A-SHEET DONE (2026-07-28, D51)** — drag a box over a sheet to assert a mark OCR missed; it renders back as a `human` mark with full provenance. **Remaining:** the Corpus pane (RT-2).

- [x] **RT-7 · Reviewer-drawn correction as a guided re-search** (Julian, 2026-07-26). In the figures view, let the reviewer drag a box around a numeral OCR missed — or correct one it misread — and use that rectangle to drive a targeted second look: re-OCR that exact region at full resolution across all engines and rotations, with the reviewer's asserted label as the search target (the same locate-guided-by-prediction shape as the text-guided confirmation pass, L0007, but with a human supplying the prediction instead of the spec). Two things must hold: the reviewer's mark is an **evidence source with its own provenance** (`method: "human"`, never blended into an OCR read — D28's honesty model), and the sidecar must become **append-only**, because it is not today: a human confirmation of the FIG-2 "A" was later overwritten by a single-engine OCR read and is now unrecoverable (`docs/ocr_failure_modes.md` §3). Fixing the append-only property is a **prerequisite**, not a follow-up — a correction channel that can silently lose corrections is worse than none. **PREREQUISITE DONE (2026-07-28, D47)** — `src/cairn/adjudication.py` + `scripts/adjudicate.py`: an append-only, hash-chained record of reviewer judgments (confirm / refute / correct / note), built on `AuditLog` so there is one tamper-evidence mechanism in the system rather than two. No update, no delete; a revision appends and names what it supersedes, and the earlier call stays legible. `supersede_ok=False` makes it **impossible for a machine writer to displace a human judgment** — the exact loss that motivated this. `load_manifest` now folds effective judgments in (with `by`/`on`/`adj_id` on every human mark), `refute` is the only way a mark leaves the view, and the legacy mutable file is still read but never written. +11 tests. **Remaining AC:** drag-to-annotate writes a provenance-stamped, append-only entry; the guided re-search reports what it found in that box (including "nothing — your mark stands as the only evidence"); prior human marks are never overwritten by machine reads.

- [~] **RT-5 · The Review Report — package the audit trail as the signable client deliverable** (landscape §3 CANDIDATE, accepted 2026-07-05). A generated per-engagement/session report: corpus identity (doc ids + content hashes), every interaction with its D16/D22 outcome class, every verify record with atom bindings + recomputed derivations, flags and their dispositions, human adjudications + reviewer notes, and the **truth-contract version + declared non-guarantees rendered as a client-facing page** (the §5 positioning note folds in here). The external record's lesson: in a liability regime (ABA Op. 512; verification duty stays with the professional), **the proof-of-process artifact is what the client buys** — and CAIRN already has the substrate no surveyed competitor matches (hash chain, byte-identical replay, TC-2 provenance stamps, evidence view). Mostly assembly; the one new design piece is the adjudication/notes record (same shape D22's refusal-logging revisit anticipates). **FIRST INCREMENT DONE (2026-07-28, D46)** — `src/cairn/review_report.py` + `scripts/build_review_report.py` → a self-contained **Record of inquiry**: declared limits FIRST (D33's ordering applied to the deliverable), corpus identity with per-document ingest hashes for independent re-verification, the RT-9 calibration state rendered client-facing, every interaction with its D16/D22 outcome + citations (doc, offsets, literal, status), **what was surfaced and set aside**, and a signature block placing the 37 CFR 11.18(b) certification with the reviewer. Built against **stated defaults** for the open intake questions (Q13: content-agnostic — the report wraps whatever the log holds, so answering Q13 changes content not shape; Q16: self-contained HTML, emailable and printable for signature). Wording is tested as behaviour: 9 tests including one asserting the report never makes an unqualified claim to *satisfy / ensure / discharge* an inquiry. **Remaining:** human adjudications + reviewer notes (needs RT-7's append-only sidecar), per-session segmentation (RT-1), and Julian's actual Q13/Q16 answers.
- [x] **RT-4 · Figures/diagrams visible in the document view** — **DONE (2026-07-22).** (Imperative for real patent work.) **FIRST INCREMENT DONE (2026-07-08):** the text-only figure model in `patents.py` — `parse_figures` (drawings-block captions, first-contiguous-run so a detailed-desc 'FIG. 1 are…' isn't miscaught), `figure_references` (offset-bearing FIG. N anchors), `reference_numerals` (numeral→element first-mention, ≥10 convention to drop claim/quantity noise) — CI-tested on the synthetic fixture + validated on US5447630A (FIG 1/2/4/5/6; 60 numerals incl. 12/38/89). `scripts/fetch_patent_figures.py` pulls the full-res drawing sheets from Google Patents into the (gitignored) engagement store, each sha256'd in a manifest. `scripts/patent_figures_view.py` renders a self-contained artifact: the 8 drawing sheets beside the figure captions + numeral legend, each numeral annotated with the FIG it is *first discussed with* (nearest text reference — locate-only, D10), click-to-context. Truth-contract (D21): a drawing is *displayed evidence*, never a text citation. **MAIN-VIEW CAPABILITY DONE (2026-07-08):** `evidence_view` gains corpus-agnostic companion figures — `FigurePanel{label,caption,image}` + `Interaction.figures` + a sticky figure strip in the document pane that reveals only the active interaction's drawings (D21: displayed evidence, no span, never verify-checked; inert with no figures, so EDGAR is byte-unchanged). CI-tested with a stub PNG (`tests/test_evidence_view.py` +3). **FIG→SHEET UNBLOCKED (2026-07-17, D28):** Vision OCR of the 8 sheets → frozen `ocr_manifest.json`; `cairn.figures_map` assigns **all 8 figures to their sheets** (7 by OCR label, FIG 4 by flagged single-gap elimination — Vision read its label as 'FIG.A') and locates **41/62 text numerals on sheets**. `patent_figures_view.py` now shows per-figure sheet tags (click → scroll to the sheet), per-numeral OCR locations (`p.N`), and the PE-2 structural-review list (31 surfaced facts). Two extraction bugs found + fixed en route: 'FIGS. 3 A-C' letter-range captions (was a phantom bare '3') and D1-D6 dimension labels polluting numerals (letter-prefix rejection). **MAIN-VIEW PAYOFF DONE (2026-07-22):** `figures_map.relevant_figures` (pure over cited-span text + the frozen manifest) selects the figures a cited span points at — via explicit `FIG. N` refs and recited standalone numerals resolved through OCR sightings → sheets → figures; `build_evidence_view.py` auto-detects a patent engagement (OCR + figures manifests present), builds the `FigurePanel` catalog, and sets each interaction's `.figures`, so clicking an answer about "the separator 10" reveals its drawing in the document pane with OCR-provenance caption. Inert for EDGAR (no manifests → byte-identical). **Judgment call (no confidence floor):** real patent numerals carry leader-line dashes that lower OCR confidence (89 reads "89-" at 0.3), so a floor would delete the payoff, not noise — surfaces provenance instead (L0006). Hermetic CI tests over a synthetic manifest incl. numeral-boundary edge cases. `docs/rt4_payoff_explainer.html`. **Left as tracked polish (not RT-4-blocking):** element-phrase precision on the numeral→element map. Patent drawings carry the reference numerals that claims and the spec turn on — you can't read a claim without the figure. Show the patent's figures in the evidence-view document pane, ideally surfaced when a cited reference numeral / `FIG. N` is in view. **Spans:** ingestion (fetch + store the figure images at ingest — Google Patents / the patent PDF pages; kept with the engagement store), **PE-1** (the `NUMERAL→ELEMENT` / `SPEC→FIGURE` binding — a numeral resolves to the figure that depicts it), and the **GUI** (render images in the doc pane). **Truth-contract note (D21):** a figure is *displayed evidence*, not a text span — grounding still binds claims to the **text** that recites a numeral; the figure is shown alongside via a typed `SPEC→FIGURE` / `NUMERAL→ELEMENT` edge (§4), never asserted as a textual citation. General capability (10-K charts benefit too); patents are the driver. **AC:** opening a patent's evidence view shows its figures; a cited numeral can surface the figure that depicts that element.

## M5 — Demo UI  ·  `TODO`
**Goal:** subsystem 7 (brief §6). The conversion surface — the polished **upgrade** of the M2-T7 static evidence view (D8): two-pane document-beside-response with click-to-source hyperlinks, now replaying from the audit log. Three flows, nothing more.
**Gate:** the three demo flows work end-to-end on EDGAR.

- [ ] **M5-T1** · `branch: feat/m5-scaffold` — React/TS app that **replays from the audit log** (no API backend in v1; brief §6), in the **two-pane layout** (canonical document beside the answer) established by M2-T7. **AC:** loads a logged interaction and renders document + answer side by side.
- [ ] **M5-T2** · `branch: feat/m5-highlight` — Sentence → source-span highlight via click-to-source hyperlinks (the M2-T7 contract, D8): clicking a claim flashes its exact `<mark>`ed span in the document pane. **AC:** clicking a sentence reveals its exact span; an unbound sentence is shown flagged, never silently linked.
- [ ] **M5-T3** · `branch: feat/m5-abstain-demo` — Pre-loaded unanswerable question that visibly shows the system refusing. **AC:** abstention is shown, not hidden.
- [ ] **M5-T4** · `branch: feat/m5-audit-panel` — Audit panel: retrieval + citations + confidence for the last answer. **AC:** panel opens and reflects the real last interaction.

---

## Patent Engagement (client track)  ·  `PLANNING`

**First paying engagement** (D10). Specializes the shared engine for a single patent
refresh-and-update. Design lives in [`CAIRN_Patent_Tailoring_Consideration.md`](CAIRN_Patent_Tailoring_Consideration.md)
(provisional, subordinate to this file); open client decisions in
[`CAIRN_Client_Intake_Questions.md`](CAIRN_Client_Intake_Questions.md).
**Cardinal rule (sharpened):** locate & evidence, **never adjudicate** (§2 of the tailoring doc).
Build order follows tailoring §11; steps PE-1…PE-5 are **mechanism- and confidentiality-agnostic**
and may proceed in parallel with intake.

**Gate (mechanism-agnostic core):** the four structural Layer-0 checks pass on the engagement
patent; claim→spec support mapping returns ranked, plural, paragraph/figure-evidenced output;
priority/effective-filing extractor correct on golden entries; **negative eval** confirms the
agent refuses to adjudicate even when asked.

**Blocked-until-intake (DO NOT INVENT, §10):** refresh mechanism (Q2), jurisdiction (Q5),
published-vs-confidential (Q17), prior-art supplied-vs-assembled (Q10), output format (Q13/Q16),
single-vs-portfolio (Q3). The two hard blockers are **Q2 (mechanism)** and **Q17 (confidentiality)**.

- [ ] **PE-0** · `chore/patent-intake` — Send/curate intake; record answers; reuse audit of the prior `patent_reader` project (append-only annotation model + any USPTO wrappers — **needs a repo pointer**). Provision USPTO **Open Data Portal** API key early (§7). **AC:** §10 blockers resolved or explicitly parked.
- [~] **PE-1** · `feat/patent-docmodel` — Document model & addressability for a single base patent (tailoring §3, **minus** prosecution history): claims as discrete objects (independent/dependent, parsed `depends_on`, decomposed into limitations); spec paragraphs by native numbering (`[0042]`); reference numerals bound to figures + first/subsequent mentions; bibliographic front-matter; priority chain. Builds on the shared `Document`/`SpanStore`. **AC:** each object individually addressable with char-offset provenance carrying **document identity** (§4). **CLAIM MODEL + LIMITATIONS DONE** — `src/cairn/patents.py`: `parse_claims` → `Claim{number, text, char_start, char_end, kind, depends_on}` and `decompose_claim` → `Limitation{claim_number, index, text, char_start, char_end}` (transition-word + semicolon split; nested sub-elements come out flat, `wherein` dependents → one limitation — both refinable). A **post-ingest parsing layer** over the shared canonical text (no separate store); claim *and* limitation spans resolve through `SpanStore` hash-verified (I3). Hermetic synthetic fixture `corpus/samples/sample_patent.txt`; **validated on the real engagement patent** (US5447630A: 22 claims, claim 1 → 8 limitations, all spans resolve). `parse_paragraphs` addresses the **specification as paragraph spans** (native `[0042]` numbering when present, else line-per-paragraph in the description region; claims excluded) — the spec side of PE-3. Locate-only (D10) — no validity/scope opinion. `tests/test_patents.py` (11); validated on US5447630A (22 claims, claim 1 → 8 limitations, **64 spec paragraphs**, all spans resolve). **Remaining:** reference numerals→figures; bibliographic front-matter; priority chain; typed + document-identity provenance (§4, dovetails PE-3). **Claims + limitations + spec paragraphs are now addressable — PE-3 is unblocked.**
- [~] **PE-2** · `feat/patent-layer0` — The four deterministic structural checks as Layer-0 evals (tailoring §5): antecedent basis (§112(b)), claim-dependency integrity, element-numeral consistency, term-usage consistency. Fixtures from the engagement patent. **AC:** CI-blocking; no model in the loop. **DEPENDENCY INTEGRITY DONE** — `patents.check_dependencies(claims)` → `StructuralIssue`s for missing / self / forward references (objective; surfaces the fact, never a validity conclusion, D10). `tests/test_patents.py` (13); clean on US5447630A (all 16 dependent claims valid). **ELEMENT-NUMERAL CONSISTENCY FIRST CUT DONE (2026-07-17, D28)** — the naive text-only version flagged 34/67 (false-positive flood); with the OCR manifest as drawing-side ground truth, `figures_map.element_numeral_issues` reconciles spec numerals vs sheet sightings at high precision: 21 recited-not-located + 10 located-not-recited on US5447630A, every message carrying the OCR-miss-vs-omission indistinguishability caveat (D10 — facts, never §112 conclusions); rendered in the figures view's structural-review section. Hermetic CI tests over a synthetic manifest (`tests/test_figures_map.py`). **ANTECEDENT BASIS MEASURED AND DECLINED (2026-07-28, D58)** — `scripts/census_antecedent_basis.py` quantifies what was previously an assertion: **28% / 24%** of back-references flagged on the two patents under the better of two strategies (51% / 46% under the naive one). The residue is dominated by **coordinated introduction** — "an incoming flow into at least first and second components" introduces both elements once, plural, sharing a determiner, and the claims then refer to "said second component" singly. Resolving that needs coordination and number agreement, i.e. parsing rather than patterns. **Not shipped**, because a defect-shaped flag against a real element at a 1-in-3 rate is worse than no check on a tool whose rule is to locate and evidence, never adjudicate. **Term-usage consistency remains deferred** and is the same class. **PE-2 therefore stands at 2 of 4 built, 1 of 4 measured-and-declined with the measurement reproducible.**
- [~] **PE-3** · `feat/patent-support-map` — Claim-limitation → specification support mapping (tailoring §6.1) with **typed provenance edges** (§4: `CLAIM_TERM→SPEC_SUPPORT`, `SPEC→FIGURE`, `NUMERAL→ELEMENT`, …) and document-identity on every record. Ranked, plural, paragraph/figure-evidenced. **Surfaces support gaps; never concludes support is legally insufficient** (§2). **AC:** golden support-mapping items pass; cross-document answers keep provenance distinct. **FIRST INCREMENT DONE** — `patents.map_claim_support(claim, paragraphs, doc_id)` → per-limitation ranked `SupportEdge`s (typed `CLAIM_LIMITATION→SPEC_SUPPORT`, each an addressable paragraph span); deterministic BM25 over the paragraph set (reuses the engine, I6); an empty list is a **surfaced gap, never a legal-sufficiency conclusion** (D10). `tests/test_patents.py` (13); validated on US5447630A (claim 1 limitations → ¶11/¶47/¶50 etc., sensible). **Reviewable artifact:** `scripts/patent_support_view.py --store … --doc … --claim N` renders a claim's limitation→support mapping as a two-pane **evidence view** (each limitation a card; click → its supporting paragraph(s) highlight in the spec; every citation verify-ok), with the dependency-integrity result. Generic/committable; runs on any ingested patent. **Remaining:** `SPEC→FIGURE` / `NUMERAL→ELEMENT` edges; document-identity field; per-corpus support floor (D20); golden support-mapping items (PE-5).
- [~] **PE-4** · `feat/patent-priority-date` — Priority-chain → effective-filing-date → pre-AIA/AIA regime flag (tailoring §6.2). **AC:** correct on golden date entries; regime flag justified by cited priority docs. **FIRST INCREMENT DONE (2026-07-06)** — `patents.parse_front_matter` (INID-coded front page → patent number, date-of-patent, inventors, appl. no., filed, priority claims, all verbatim), `effective_filing` (earliest of filed + claimed priorities, returned WITH its verbatim basis), and `regime_flag` (pre-AIA/AIA as **date arithmetic over cited text**, with the D10 note that regime applicability to specific claims is a professional determination). US5447630A re-ingested WITH its front matter (filed Apr. 28, 1993 → pre-AIA; inventor John M. Rummler; Appl. No. 08/053,402); synthetic fixture covers the provisional-priority path (effective filing = the provisional date → AIA). Golden P013/P014 flipped to answerable (pre-ratification, so allowed), P015 assignee stays an honest abstain (front page carries none); floor recalibrated 6.1 → **6.2**. `tests/test_patents.py` (15). **Remaining:** multi-generation priority chains (continuations/CIPs — needs family documents, PE-6/intake).
- [x] **PE-5** · `feat/patent-golden` — Golden dataset seeded from the **engagement patent** (tailoring §8). **DONE — RATIFIED 2026-07-08 (D23), labels frozen + CI-enforced append-only.** First cut — [`golden_patent.json`](golden_patent.json): **25 items** curated from the 42 reviewed questions, covering **all five outcomes** (9 answer · 7 abstain · 2 correction · 1 partial · **6 refuse-to-adjudicate** — the §2 boundary negatives, D22) + 3 `support_expectations` for the PE-3 claim→spec mapping. Quotes resolve (unique 1:1 where possible, D7); `tests/test_patent_golden.py` (4 standing tests; **skip in CI** — the engagement store is local-only). **Floor calibrated (D20): `CAIRN_SUPPORT_THRESHOLD = 6.1`** — fitted just under the weakest answerable (6.3); catches true out-of-domain absents (price 4.9, assignee 0.0). **Honest finding:** present/absent **overlap** on this corpus (gap −16.6) — bibliographic questions naming "US 5,447,630" hit the header line at 22.9, and the plausible-but-absent filter-schedule trap retrieves real related text at 11.7 → **content-absence detection catches only truly-alien absents here; the other five fall to agent reasoning (exactly D12's split), measured at Layer-E**. Strengthens the case for the retrieval-upgrade seam. **LIVE BASELINE RUN (2026-07-04,** [`docs/layer_e_patent_baseline.md`](docs/layer_e_patent_baseline.md)**):** the UPL boundary **held 6/6** (`refusal_accuracy` 1.0 — three refusals were instant/tool-less and initially misfiled by the runner's error heuristic, fixed); the header-magnet absents were **all caught by agent reasoning** (D12's second mechanism, live, 4/4); decision accuracy 0.88 corrected; verify caught 15 ungrounded drafts; brier 0.17 (vs EDGAR 0.30). P017 surfaced a **label question** (the agent gave a grounded "no schedule is prescribed" with verifying citations — arguably better than silent abstention). **Remaining:** **human ratification of the labels** (esp. P017; then append-only); refusal *quality* (refuse **and** offer located evidence — 3/6 refused without locating); dates/regime items (needs front-matter ingest, PE-4); figure/numeral items (PE-1 remainder/RT-4).
- [x] **VER-1** · `feat/verify-operations` — **Extend `verify`'s verifiable-computation set** beyond `sum`/`subtract` (shared engine; the **patent engagement is the first driver**, but EDGAR benefits too — ratios, growth %, the √ case that today returns *unverified*). Mechanism-agnostic; not gated by PE-6. Each operation stays a **pure, seeded recomputation from *cited* operands** — result declared-and-recomputed, never cited (D9); no model calls (I6). **Proposed primitives:** `multiply`, `divide`, `percent` / `percent_change`, `ratio`, `min`/`max`/`average`, `count`, `round`, `date_delta`, and `compare` (`>`/`<`/`=`/`within-range` — for claim numerical limitations like "between 5 and 10 wt%"). **Unit conversion** is the hard sub-design: factors are *external constants*, so each conversion must record the **factor used** from a vetted, auditable table (never guessed) — deterministic and reviewable. **Numeric tolerance + rounding** policy so float recomputation is reproducible/auditable. **Boundary (D10):** these *evidence* quantities (locate & evidence); they must **never become adjudication** — patent term, validity, infringement, or claim-scope are refused, not computed (whether a measured value "falls within" a claim range may be *displayed* as a numeric comparison but **not asserted as infringement/novelty**). Any operation still outside the set → the explicit **unverified-arithmetic / partial** path (D16), never dressed as grounded. **AC:** each op has a Layer-0 recomputation test; a patent fixture (claim-range compare + unit convert) passes; a negative item ("compute the patent term" / "does X infringe") is **refused, not computed**. **Gets a new `D#` at design** (extends D9's derived-atom model). **SLICE 1 DONE (D18):** `multiply`/`divide`/`ratio`/`percent_change`, matched at the asserted decimal precision; div-by-zero/bad-arity → not ok. **SLICE 2 DONE (D19):** relational `gt`/`ge`/`lt`/`le`/`eq` + `within_range` (boolean result, claim-limitation comparisons) — numeric fact, never adjudication. `equation()` renders all; MCP enum + `tests/test_verify.py` cover them. **Remaining:** **unit conversion** (external factors → auditable table; needs a `DerivedAtom` factor/unit field) and the **adjudication negative-test** (PE-5/Layer-E: a "does X infringe / compute the term" request is refused, not computed).
- [ ] **PE-6** · *Pause for client input* → then mechanism-specific layer (tailoring §9: continuation/CIP vs reissue vs amendment → prosecution-history ingestion) and multi-document / prior-art topology. **Do not build until Q2 confirmed.**

> **Engine implications to carry while building EDGAR-first (accommodate, don't pre-build):**
> the **abstention/refusal** path (I2) generalizes to the *adjudication refusal class*; the
> **atom resolver** (D9) generalizes to patent atoms (claim terms, numerals, dates); **provenance**
> gains a **document-identity** field for multi-document topology; the **evidence-view GUI** (D8/M2-T7)
> is the natural surface for claim→spec highlighting. Keep these seams clean; don't hard-code EDGAR.

---

## Forward-compatibility — the truth contract  ·  `WIP`
**Goal (D21):** let new understanding/tech/research raise epistemic rigor without a rewrite or a silent weakening. Declaration + provenance + a disciplined upgrade rule — **not** a plugin framework.

- [x] **TC-1** · `docs/truth_contract.md` (**v1**) — the declared, versioned contract: every guarantee → enforcing mechanism → layer (runtime / Layer-0 / Layer-E) → current strength; the deliberate `verified ≠ entailed` non-guarantee; the **monotonic rule** (strengthen freely; weaken only via a new major version + rationale + oracle); the **upgrade ratchet** (swap behind a seam → Layer-0 holds + Layer-E improves-or-holds); and the seams. **DONE.**
- [x] **TC-2** · **Provenance stamping** — **DONE.** Every record carries a `provenance` block: `check_support`/`check_claim` → `{contract, retrieval, threshold}`, `verify` → `{contract, verify_ops}` (`cairn.contract.CONTRACT_VERSION`; `Retriever.method`; `verify.OPS_VERSION`). Additive + backward-compatible; **replay reads the recorded floor so non-default thresholds reproduce byte-identically (I6)**. The evidence view renders the line ("truth-contract v1.0 · retrieval bm25 · floor 15 · verify-ops 1"). `tests/test_session.py` (round-trip + non-default floor). Remaining seam: **entailment provenance** (stamped when a runtime entailment method exists; today entailment is Layer-E only).
- [ ] **TC-3** · **Formalize an extension interface only at its second implementation** — e.g. an `EntailmentProvider` when a non-judge entailer arrives, or a `Calibrator` interface at a second calibration method. The new method plugs in behind the interface; the oracle must hold/improve; bump the contract version iff a guarantee's strength changes. **Anti-trap: not before** a real second implementation exists.

## Backlog (v2) — do not start
API-wrapped service with **inline entailment-gating** (the structural-interception design) · action-taking / write tools · multi-corpus support · reranker upgrades · golden set to 200+ items · auth + multi-tenant for client deployments · **query-relative support floor (no per-corpus constant)** — top-hit outlier / z-score vs the result distribution, or score normalization; a **Layer-E experiment** measured against the calibrated constant (D20) before any adoption, since it adds a moving part to the evidence path · **graded support score, advisory-only** (landscape §3, parked 2026-07-05) — a per-sentence claim↔span similarity score (Clearbrief-style; deterministic-per-version) as an *advisory* rigor line in the evidence view + a Layer-E feature (does low score predict judge non-entailment / LE-2 over-claims?); **never a runtime gate** (that would smuggle a soft judgment into the evidence path, D12/D21); a plausible first `EntailmentProvider` shape whose interface waits for a second implementation (TC-3).

---

## Changelog (append-only)
*(Agent appends one line per merged PR: `YYYY-MM-DD · M#-T# · short note`.)*

- 2026-06-23 · — · ROADMAP created; M0 set as current focus.
- 2026-06-23 · M0-T3 · golden_seed.json added (20 items, Apple FY2024 10-K); pending human verification.
- 2026-06-23 · — · Runtime pivot to Claude Code tool (D6) + schema reconciliation to quote+locator (D7); brief and ROADMAP updated; M2/M4 restructured, oracle split into Layer-0/Layer-E.
- 2026-06-23 · M0-T1 · Repo scaffold merged: src-layout `cairn` package, pytest+ruff, CI skeleton; clean install runs smoke suite green. Switched to single-agent-on-main working mode.
- 2026-06-23 · M0-T2 · Toy corpus assembled: 5 verbatim AAPL FY2024 10-K excerpts + provenance/sha256 manifest (`corpus/toy/`), deterministic build script, integrity test. Flagged inverted current/non-current marketable-securities labels in golden G008 for M0-T3 human review.
- 2026-06-24 · M0-T3 · Golden verification: corrected G008 (inverted labels → current $35,228M / non-current $91,479M) and grounded G009 (Ernst & Young LLP) against the canonical filing; both marked grounded + verification_note.
- 2026-06-24 · M0-T4 · Audition rig `cairn_rig.py` clears the M0 gate (precision/recall/correctness 100%, hallucination 0%, abstention 100%). Standing gate test added. **M0 DONE** — advancing to M1.
- 2026-06-24 · — · Added `demo.py` (guided M0 walkthrough) + README "See it run".
- 2026-06-24 · — · D9: atom-resolver contract for `verify`/`check_claim` (agent supplies located atoms; fixed resolver checks exact literal at offset + hash + scope; independent re-extraction; derived-value operands). Open contingencies tracked under M2-T1; brief §5 updated.
- 2026-07-22 · discussion · **RAG-extension design discussion** filed → [`docs/rag_extension_discussion.html`](docs/rag_extension_discussion.html) (design input, authorizes no work). Finding: retrieval is a swappable front-end; the guarantee lives at `verify`/grounding downstream, so embeddings/chunking/contextual-retrieval don't touch the truth contract — the `RetrievalBackend` seam + `Retriever.method` provenance + D20 floor + the ingest-freeze pattern (I3/OCR) already expose most levers. One real decision: I6's literal 'no runtime model calls' vs. a query embedding (determinism + AI-never-decides preserved; the pitch line gives). Recommendation: (A) write the plug-in contract now [cheap, exposes levers, no contract change]; (B) build embeddings only when a multi-doc corpus's eval shows BM25 misses. **Awaits Julian's Decisions 1-3.**
- 2026-08-09 · D61/M2-T6 · **Layer-E run 3 (billed, authorised).** First run grading the citation: decision 0.95 vs **evidence 0.786** — a **21-point gap**, exactly the illusion RT-8 was built to expose. Misses: G002, G007, G019. 6/20 unscoreable (abstain/refuse have no gold span). Whether the three are genuine wrong-spans or scorer strictness is **not yet established** — a first inspection attempt was too crude to tell, and the caveat is recorded rather than smoothed.
- 2026-08-09 · D60/RAG · **Contract 2.0: I6 → "no model on the deciding path"** (Julian ratified). Retrieval may embed a query with a pinned model; document embeddings stay ingestion-time and frozen, and the model is recorded in provenance. Major bump because the old form was grep-checkable and the new one takes judgment — a real loss of auditability even though no output guarantee moved. `RetrievalBackend`'s four obligations written out and tested; nothing built (Decision 2 move A). Writing the contract caught an over-statement in it — zero signal is not a decision. Adopted the fleet convention: `project.manifest.json` + `traces/`. +3 tests.
- 2026-08-09 · D59/VER-1 · **Verifiable-computation set widened.** +`percent`/`min`/`max`/`average`/`count`/`round`, plus `date_delta` in a new date-operand category dispatched before numeric parsing. `OPS_VERSION` → 2; old records keep 1 via the preserved provenance stamp. D10's boundary is now a **test**: any adjudication-shaped operation (patent_term, validity, infringement…) fails the gate. Every op has a paired reject test. The date parser's own test caught "Septober 4, 2024" parsing as September — months now matched whole. +12 tests. 336 layer0. Unit conversion deliberately deferred (external factors need a vetted, recorded table).
- 2026-07-28 · D58/PE-2 · **Antecedent basis: measured, declined, census shipped.** 51%/46% of back-references flagged naively, 28%/24% with head-noun matching, on both patents. The residue is coordinated introduction ("first and second components" introduced once plural, referenced singly) — parsing, not patterns. Not shipped: a defect-shaped flag against a real element at 1-in-3 is the tool adjudicating badly (D10), worse than no check. `scripts/census_antecedent_basis.py` makes the measurement reproducible. PE-2 now 2/4 built, 1/4 measured-and-declined.
- 2026-07-28 · D56+D57/protocol · **MCP Apps assessed (deferred) + protocol-era audit.** Apps fits Locate/the queue and would remove the loopback server, but its app→tool channel conflicts with D50's "adjudication is not a tool"; elicitation is the right primitive but its flat-schema limit and no drag-a-box make it design work. The Record of Inquiry stays a file — an in-conversation artifact is wrong for something signed and archived. Statelessness: **we already satisfy it** (pure functions, no session state — now tested), but the SDK is legacy-era (`2025-11-25`, has the handshake, no `server/discover`) so a modern client would fail against us. Unbounded `mcp>=1.0` pin bounded to `<2` before 2.0.0 pulled the modern era in silently. +2 tests. 324 layer0.
- 2026-07-28 · D55/RT-9 · **Second-patent separability probe corrects D53.** US8046721B2 overlaps by only −2.1; inspecting US5447630A's outliers showed its −16.7 was mostly two golden questions quoting the patent number, which matches the title line (23.0 → 0.0 without it) — true gap −5.3. Both patents mildly non-separable vs EDGAR's clean +9.2, and EDGAR is 4× longer with 2.5× the vocabulary, so the driver looks like corpus size/diversity, not the domain. New known limit documented in `support.py`: **a question naming the document defeats the floor**. Frozen golden set untouched. +1 test. 322 layer0.
- 2026-07-28 · D54/RT-7c · **Mark-a-sheet shows and edits existing marks.** All 145 located marks overlay the sheets (OCR green, human dashed blue with provenance); click one to revise the reading or refute it. `box_to_display` is the tested inverse of `box_from_pixels` — round-trip plus a one-way test, since a flip in both directions cancels invisibly. Nothing is edited: revise appends a superseding judgment. +6 tests. 321 layer0.
- 2026-07-28 · D53/RT-9 · **Fitted ≠ calibrated.** The patent store CAN be calibrated (golden_patent.json has 5 content-absent items — an earlier claim otherwise was wrong), but the fit **does not separate**: gap −16.7, content-absent scoring up to 23.0 against answerable down to 6.4. Records now carry `separable`/`gap`; a failed fit is written flagged, not hidden, and reads as NON-SEPARABLE everywhere. Consolidated the three-place calibration branch into `calibration.describe()` after the console kept reporting green while the report said otherwise. +3 tests. 315 layer0.
- 2026-07-28 · D52/RT-2 · **Corpus pane — the console is complete.** All seven panes now have content. Shows documents + hashes, the support floor and its staleness, corpus-scoped constants this corpus has NOT exercised (inert ≠ validated), drawings state, and the judgment chain. Deliberately no ingest/remove controls — that invalidates the fitted floor, the offsets logged citations resolve against, and the read-only-corpus posture; the pane states the reasoning in their place. +5 tests. 312 layer0.
- 2026-07-28 · D51/RT-7c · **Mark a sheet.** Drag a box over a drawing to assert a mark OCR missed; renders back as a human mark with provenance. Coordinate conversion in Python (`annotate.py`) so the y-flip that bit L0007 is testable — verified a box drawn near the top of a sheet records y=0.8357. A box is a sighting, not a search: no runtime re-OCR (D28/I6), stated on the page. Sheets copied into the console root rather than widening the server's containment. +12 tests. 307 layer0.
- 2026-07-28 · D50/RT-7b · **Review queue + in-console adjudication.** The reconciliation becomes a worklist ranked by consequence (34 live on the engagement); each row records an append-only, provenance-stamped judgment. No bulk accept, nothing pre-selected, reviewer named by the server not the page, empty queue explicitly not a clean bill of health. `/adjudicate` is not a tool — a machine must not manufacture human judgments. All guards exercised live. +11 tests. 295 layer0.
- 2026-07-28 · D49/RT-10b · **Locate pane + loopback review server.** The console's front door: ask, see the support decision and its evidence, recorded to the audit log. Needed a transport because a JS retrieval implementation would be a second oracle able to drift from the Python one. Loopback-only (non-loopback binds refused), `Origin` validated against DNS rebinding, handlers taken from the same registry so D35/D41 apply unchanged; all guards exercised live. Span text fetched via `get_span` so it is hash-verified. +9 tests. 285 layer0.
- 2026-07-28 · D48/RT-10 · **The console.** Tabbed frame over the existing generators with a persistent header (corpus · calibration state · adjudication count · contract version); absent panes say what is missing and why. Built for both the reference corpus and the patent engagement. Surfaced a calling-convention bug in `build_evidence_view` on its first run. +6 tests. 277 layer0.
- 2026-07-28 · D47/RT-7a · **Append-only adjudication record.** Reviewer judgments (confirm/refute/correct/note) on a hash-chained log built over `AuditLog`; no update or delete, revisions supersede and history survives; a machine writer cannot displace a human judgment (`supersede_ok=False`). `load_manifest` folds effective judgments in with full provenance; `refute` is the only removal path. `scripts/adjudicate.py` records and lists them. Legacy migration recovered nothing on the live engagement — the mutable file is empty, so the lost FIG-2 "A" needs re-recording by hand. +11 tests. 271 layer0.
- 2026-07-28 · D46/RT-5 · **Record of inquiry (first increment).** `review_report.py` + `build_review_report.py` → the signable client deliverable: declared limits first, corpus hashes for independent re-verification, calibration state, outcomes + citations, surfaced-and-set-aside, reviewer signature block. Wording tested as behaviour (never claims to satisfy/ensure/discharge an inquiry; states retrieval is a ranked slice). Built against stated defaults for the open intake Q13/Q16. +9 tests. 260 layer0.
- 2026-07-28 · D45/RT-6 · **Protocol part 2 (drawings).** Fetcher fixed twice (kind-code stripping; second Google Patents URL scheme). `HEADER_BAND` made conditional on per-sheet header evidence — it was deleting 13 spec-recited numerals on a corpus with no headers (188 → 206; corpus 1 byte-identical). `_FIG_LABEL` now matches "Figure" as well as "FIG.". Added `geometry_report` (cross-sheet rendition check — 3 flagged on corpus 2, clean on corpus 1). Merge radius **transfers** (+0.0072 vs corpus 1's +0.0002). Open: SIGBUS in `tiled_search` on the odd-geometry sheet. +2 tests. 251 layer0.
- 2026-07-28 · D44/RT-9 · **Per-corpus calibration record.** Stores carry `calibration.json` (floor + corpus hash + date + method); `check_support` records now say *calibrated* / *STALE* / *UNCALIBRATED* / *EXPLICIT OVERRIDE*, at the record's top level, preserved verbatim on replay (I6). EDGAR calibrated at 15.1. Caught and fixed a silent-override bug introduced mid-build (the store's record was beating an explicitly-passed threshold). `acronym_labels`' unadjudicated contract documented with the corpus-2 evidence + pinned by a test; all three callers audited and already adjudicating. +7 tests. 249 layer0.
- 2026-07-28 · D43/RT-6 · **Protocol run end-to-end on a second patent** (US8046721B2). Found a real bug: `_FIG_CAPTION` could not match range captions, which both lost them and inflated the caption gap past 400 — `parse_figures` silently dropped FIG. 6/9/10. Fixed (22 → 25 figures; corpus 1 unchanged). Also: `support.THRESHOLD` does NOT transfer (false abstention on the patent's own subject at 4.56 vs a 15.0 floor) and `acronym_labels` over-generates without drawing evidence (34 junk labels incl. section headings) — both filed as **RT-9**, since neither is fixable by changing a number. `NUMERAL_DIGITS=4` validated; `MIN_LOCATABLE_NUMERAL` inert, recorded as still untested. +1 test. 242 layer0.
- 2026-07-28 · D42/RT-6 · **Corpus-fit registry + protocol.** 17 constants registered with evidence + falsifiers (12 corpus-specific); gate fails on silent retuning and on unregistered new tunables (both proved on known-positives). Building it found three defects: D34's digit-cap fix was incomplete (list siblings still capped at 3 — "pipes 1002, 1010" dropped 1010), the sibling pattern missed and-joined list tails, and two fitted constants existed in duplicate free to diverge. `docs/corpus_fitting.md` + `scripts/corpus_fit_report.py`. +8 tests. 241 layer0.
- 2026-07-28 · D41/security · **MCP hardening P0.1 + P0.2.** Tool-manifest hash pinned by a standing test (proved against 4 known-positive drift shapes before trusting it); description lint for instruction-like language; reject-by-default value bounds (`doc_id` pattern + maxLength, capped top-k, bounded query/claim/offsets). +3 tests. 233 layer0.
- 2026-07-28 · D40/naming · **ATTEST → Cairn.** Package, modules, `CAIRN_*` env vars, CLI, docs, git remote. `corpus/` (hash-verified, I3) and `audit_log/` (append-only) untouched — the chain contains zero occurrences of the name, so nothing was invalidated. **The two research reports in `docs/` are left VERBATIM and annotated**: their trademark findings are factual claims about the *ATTEST* mark, and a mechanical rename fabricated "YOUCAIRN", "cosign cairn" and "askcairn.com" before it was caught. Gate green (230), CLI + demo + imports smoke-tested.
- 2026-07-28 · D39/RT-8 · **Layer-E now grades the citation.** `gold_spans` + `cited_spans` + overlap matching; `aggregate` publishes `evidence_accuracy` and the `decision_minus_evidence` gap. Smoke-tested on the real audit log (48 citations recovered). Unscoreable reads as null + note, never as clean. +3 tests. 230 layer0.
- 2026-07-28 · D38/RT-8 · **Landscape swarm** (73 agents, 8 lenses) → `private/attestation_landscape.md` (**local-only from 2026-07-28** — its §5 enumerates third-party patent filings and drafts an FTO brief, which is counsel's material to control; see `docs/attestation_landscape.README.md`. Moving it does not undo prior exposure). Scoped two shipped overclaims found in our own tree (`demo.py`'s "100% citation precision, 0 hallucinations"; the "zero-hallucination oracle"). Added RT-8 (Layer-E grades the verdict, not the citation — label-only accuracy for a provenance product). Trademark + patent-neighbourhood findings recorded for attorney review; no legal conclusions drawn (D10 discipline applied to ourselves).
- 2026-07-28 · D37/VER · **`verify` counted-coverage fix (I1).** Atom coverage was a set difference, so one binding discharged every repeat of a figure in a sentence — a second, differently-attributed assertion of the same number passed as grounded. Now `Counter`-based. Found by the attestation-landscape swarm, reproduced first; +1 test; 227 layer0; no pre-existing test changed.
- 2026-07-26 · D36/RT-4 · **Strobogrammatic twin rejection** (Julian's inversion idea). Tested both halves of the density proposal: nearest-neighbour spread cleanly separates the rotation AXIS (on-axis 0.005–0.05 vs off-axis 0.10–0.21) but cannot separate a direction from its 180° inverse. The inversion test does, mechanistically — same page position + inverse label = one mark read twice. 19 coincidences found, catching every hand-identified phantom, including multi-engine ones that cross-engine corroboration admits. +2 tests. 226 layer0.
- 2026-07-26 · D35/security · **MCP hardening audit (P0.3).** Fleet brief applied to CAIRN. No shell/eval/subprocess on the served path. Found `DocumentStore.doc_dir` built a path from an unvalidated doc_id — pathlib discards the root on an absolute operand, so traversal needed no `..`. Not reachable today (SpanStore is dict-backed, verified empirically) but guarded only by accident; now canonicalized + containment-checked. +1 test, 224 layer0. RT-6/RT-7 added (corpus-fitting protocol; reviewer-drawn guided re-search).
- 2026-07-26 · D34/RT-4 · **OCR failure-mode swarm + three fixes.** 72 agents, 9 lenses, 62 candidates, 43 surviving hostile review → `docs/ocr_failure_modes.md`. Spot-checked its repo claims and reproduced four: 4-digit numerals unreadable; `12A` silently asserted as `12` (grounding path); temperature quantities bound as parts; `image_sha256` written and never read; Tesseract exit code and stderr never checked (L0009, prose-only for four milestones). Fixed all; +4 tests; 223 layer0. Report's Tier-A audits and the merge-radius headroom await triage.
- 2026-07-25 · D33/RT-4 · **Discrepancies lead the report.** Figures view now opens with a discrepancy tally (recited-not-located · located-not-recited · figure mismatches · unresolved engine disagreements · unlocated markers), each chip anchoring to its detail, above a caveat separating frozen-manifest determinism from the reading's own version-sensitivity, and stating that absence of a flag is not evidence of correctness. After D32 the recited-not-located count is **0**.
- 2026-07-25 · D32/RT-4 · **Rotation union replaces best-angle.** Measured that the header parses at all 4 angles while isolated numerals do not — rotation is per-glyph, so pick-one-angle loses labels on 7/8 sheets and mis-picks 90° on the one sheet with ground truth. Now OCRs at 0/90/180/270 and unions; rotated-only finds need spec recital or cross-engine agreement, with 180° (the 6↔9 generator) held to cross-engine only. Every 180° phantom (98/86/95/90/10/89/96/901) rejected; all of Julian's FIG-1 numerals retained; FIG 1 27 → 29. +3 tests. 219 layer0.
- 2026-07-25 · D31/RT-4 · **Rotated-sheet OCR.** FIG 1 is printed sideways; engines read upright text only. Per-sheet `detect_orientation` (evidence: most label-shaped tokens) + `unrotate_observation` coordinate transform, angle recorded per page. Transform validated against 16 known controls (mean Δ 0.002 both axes) — a first derivation was off by one glyph width in y and the controls caught it. FIG 1: 22 → 27 labels, **all of Julian's 20/33/38/40/43/44 recovered**; other seven sheets correctly detected upright. +2 tests. 216 layer0.
- 2026-07-25 · D30/RT-4 · **Sub-10 numerals → text-only (D30) + figure ordering.** Sub-figures now sort beside their parent (1, 2, **3A, 3B, 3C**, 4, 5, 6) — `numeral_key` made case-insensitive so uppercase sub-figure suffixes sort naturally. Sub-10 numerals are no longer OCR-located anywhere (capture, prediction, boxes, page tags all gated by `is_locatable`); they render as `2 · toilet · FIG. 1 · text-only`, and their absence is no longer mis-reported as an OCR miss. +4 tests. 214 layer0.
- 2026-07-25 · D29/RT-4 · **All-page QA round 3** (Julian reviewed every sheet). Five root causes, each a structural fix: **(1) sub-figures are figures** — 3A/3B/3C share one caption ('FIGS. 3 A-C are…') so they never matched the caption pattern; `parse_figures` now emits them from the references, and all **8 figures map to sheets by OCR label** (elimination no longer needed) and are selectable. **(2) Dimension callouts D1-D6** were deliberately discarded (L0005 letter-prefix rule) but the spec recites them ('the respective dimensions D1-D6 can be as follows: D1 3.25"…') and engines read them at conf 1.0 — new `patents.dimension_labels` class, all six now chipped/boxed on FIG 6. **(3) Sub-10 numerals** — the element pattern required a bare word before the numeral, so '…garbage disposal) 3, dishwasher 4' hid 3/4/5; punctuation now allowed, all of 1-5 recited. **(4) Same-spot engine DISAGREEMENT** (Julian: '58 identified as both 58 and 38', '54 as both 54 and 94'): two different labels at the SAME position (±0.003) are ONE mark read two ways — resolved toward the text-tied reading (38→58, 94→54 on FIG 2), and where the spec ties neither (FIG 3C's shared caption) reported as **NOT resolved** for a reviewer (54 vs 34, 58 vs 38, 56 vs 36). A first attempt at 'one digit apart + not tied' flooded 42 false pairs — L0006 again; positional coincidence is the precise signal. **(5)** noise guards for garbled captions/sub-figure suffixes/bare 0. +5 tests. 211 layer0. **Still open (honest):** FIG-1 sub-10 marks 1/3 predicted but OCR cannot locate them; 20/33 are not recited anywhere so only corroboration could promote them.
- 2026-07-22 · D29/RT-4 · **Unguided all-page label audit** (Julian: run the process across all pages). New `scripts/audit_sheet_labels.py` — read-only sweep of every sheet with every engine (+ tesseract edge bands), reporting tokens the manifest lacks; **acceptance rule is cross-engine CORROBORATION, not text prediction** (the complement to D28's guided pass, which can only look for what the spec predicts). First run: 45 candidates, **exactly 2 corroborated — and both real** (`CZ` "the shaded region CZ represents a cooling zone", `CL` "CL designates the center line"); the other 43 were single-engine tesseract line-art noise (AN/FH/MH/QW/LK/UY…). Corroboration is therefore a working acceptance filter: 2/2 precision, 43/43 noise rejection. Root causes fixed: **(1) L0006 5th instance** — `_MIN_ACRONYM_MENTIONS=2` deleted CZ/CL (defined ONCE each); floor removed, the drawings adjudicate (PVC is a candidate and simply never found). **(2)** garbled-caption digits leaked ('FICG.6'→6) — fuzzy FIG guard; **(3)** sub-figure captions leaked ('3A'→3) — patents use UPPERCASE sub-figure suffixes vs lowercase numeral suffixes ('12a'), so a digit followed by an uppercase letter is a caption fragment; **(4)** bare '0' is never a numeral. Result: CZ/CL now recovered by the normal guided path (no manual annotation); noise 3/0/20 gone; drawn-not-recited 11→5; re-audit shows **0 corroborated candidates remaining**. 12a confirmed twice on FIG 3A as Julian reported. +3 tests. 207 layer0.
- 2026-07-22 · D29 · **Multi-engine verified on a fully-provisioned machine.** All three engines live (vision + tesseract 5.5.2 + rapidocr 1.2.3); provenance holes closed (real pip version via importlib.metadata; text-guided recoveries now carry the same `engines` shape as first-pass — 0/139 untagged). **Complementarity quantified on US5447630A: 139 numeral records — 24 read by ALL THREE engines, 51 corroborated by >1, and 37/28/23 found by vision/tesseract/rapidocr ALONE** (i.e. ~88 records would be lost to a single-engine ingest). The A is attributed to tesseract (conf 0.963). **Determinism confirmed:** two identical multi-engine runs → byte-identical manifest hash, so I6 holds across the engine union.
- 2026-07-22 · D29 · **Multi-engine OCR** (Vision + Tesseract + RapidOCR, `--engines auto`): pure converters + cross-engine same-spot merge with `engines` provenance (CI-tested); tesseract edge-band marker rescue **finds the A automatically** (conf 0.96) — manual annotation retired; FIG 4 label reads directly; recited-not-drawn **empty**; prose-fragment guard. Instrument lesson: tesseract 'blindness' on crops was a SANDBOX file-access failure (/tmp) with stderr unread — sanity-check the instrument on a known-positive before concluding engine blindness. `[ocr]` extra gains rapidocr-onnxruntime + Pillow (cross-platform). +3 tests. 205 layer0.
- 2026-07-22 · RT-4/D28 · **Human/visual confirmation channel** (Julian: 'are you certain the run was pointed at FIG 2? The A is very obvious'). Verified: the run WAS pointed at FIG 2 (A in p.3's target list) — and reproduced the miss on the exact crop containing the glyph: **Apple Vision is totally blind to this A** (recognizer in all modes, tight crops, 3× upscale, legacy revision, even the raw character-box detector: zero). The honest terminal state for OCR ≠ the end of the story when a human verified the mark: `figures/manual_annotations.json` sidecar → merged by `load_manifest` with `method:"human"`, conf 1.0, full by/date/note provenance — renders as a dotted 👁 p.N tag + confirmation box, distinct from every OCR read; coverage clears the flag. The A annotated (reported by Julian, position visually confirmed by the agent on the sheet crop). This is the adjudication-record shape RT-5 anticipates. C stays flagged (expected on FIG. 2, not yet humanly confirmed). +1 test. 202 layer0.
- 2026-07-22 · RT-4 · **View-marker location corrected (Julian: 'A was on FIG 2, not FIG 3A').** The markers physically sit ON the PARENT figure (the one the views are taken of); `figures_map.sub_figure_parent` derives it from the family's own caption ('FIGS. 3 A-C are …views …of FIG. 2' → parent 2), with an abbreviation-safe sentence stop ('FIG. ' carries its own '. '). Markers are now searched/reclassified ONLY on the parent's sheet — which also structurally prevents the FIG-6 'C!' hallucination class — and the UI says where: chip 'view marker on FIG. 2 — the FIG. 3B view direction'; flags 'A — expected on FIG. 2…', 'C — expected on FIG. 2…'. No parent derivable → searched nowhere, not everywhere. +1 test. 201 layer0.
- 2026-07-22 · RT-4/PE-2 · **QA round 2 (Julian: 14a/64 visible on FIG 2; A/B view markers).** (1) **a↔0 confusion recovery:** Vision reads a suffix 'a' as '0' ('14a'→'140', '12a'→'120'); a target with an 'a' suffix now also matches its 0-variant, gated on the variant not being a known label, recorded under the TRUE label — 14a recovered; coverage resolves positional twins as `likely_misreads` (120→12a, 140→14a), cleaning the anomaly list. (2) **Finer-tile fallback (8×4)** for still-missing targets — 64 (conf 1.0) + 88 recovered. (3) **View-marker letters** (A/B/C from sub-figure suffixes): accepted ONLY from whole-image observations — tiles hallucinate letters on line art (a curly leader read 'C!'); B sighted + chipped '(view marker → FIG. 3B)'; **A unreadable by Vision → surfaced as 'View marker not located… review'**, never silently absent. (4) **Fragment rejection** (box-overlap): tiles cutting '12b'→'1', '13'→'1J', FIG-label digits, prose tails ('…EFFLUENT. 1') all rejected; fresh hits also cross-filter each other ('84' beats its embedded '4'). (5) **List-sibling recitation:** 'pipes 56, 58' now recites 58 (inherits the head's element) — 58 was invisible to the extractor (list-only mentions). (6) STM/LTM count as recited in coverage (were false anomalies). Pure helpers moved into `figures_map` for CI testability. Recoveries 32; FIG-2 list fully closed except the honest A flag. +6 tests. 200 layer0.
- 2026-07-22 · RT-4/PE-2 · **Reference-label model completed** (Julian's figures-view QA). (1) Labels are strings: **letter-suffixed numerals** (12a/12b/14a) extract as distinct parts (int-collapse reported 12 present + 12a missing, both wrong); `numeral_key` natural ordering. (2) **Repeats kept:** the same label twice on one sheet yields two boxed instances (overlap-tile dedup by position, 0.02). (3) **Figure-context window 500→2000** (measured: ties 14a/54/62/64/66 to FIG 2 exactly as reported; wider adds nothing) + fixed a sentence-final-period boundary regression. (4) **Acronym labels** (STM/LTM): spec-derived candidates (≥2 mentions, units/FIG-syntax excluded), text-guided searched on sheets — the drawings adjudicate (PVC predicted, correctly not found); legend chips for sighted acronyms. (5) OCR furniture guard: 'Sheet 1 of 8' no longer boxable as numeral 1/8 (committed separately). Net: recoveries 16→32 on US5447630A; 5 of Julian's 7 reported FIG-2 gaps located (12a, 54, 58, 62, 66), the other two (14a, 64) correctly FLAGGED recited-not-drawn. +7 tests. 194 layer0.
- 2026-07-22 · D28/RT-4 · **Text-guided OCR confirmation — "push the truth from all angles" (Julian's ask).** When the spec predicts a numeral on a figure's sheet but the first OCR pass missed it (the numeral-coverage `figure_mismatches`, e.g. Julian's separator-10→FIG-4: the "10" is plainly on FIG 4 but Vision whiffed), a **tiled, higher-recall re-OCR** searches that sheet for *exactly* the predicted numeral(s). `ocr_patent_figures.py --confirm --doc`: `tiled_search` OCRs 4×2 overlapping full-res tiles (recovers small numerals the whole-image pass drops on sparse line-art) using only `topCandidates` matching the text prediction — so higher recall adds no tiling noise. **Recovered 16 spec-predicted numerals the first pass missed on US5447630A, incl. the 10 on FIG 4** (mismatches 16→9); recovered sightings tagged `method:"text-guided"` (`NumeralSighting.method`), shown as ↻ p.N + a confirmation box. Tile-bbox y-flip (top-left tile rect vs Vision's bottom-left bbox) found + fixed — box lands dead-on the '10'. Still ingestion-time + frozen/hashed (D28) → runtime stays deterministic (I6); a numeral the targeted pass STILL can't find stays flagged (stronger human-review signal). UI: click a numeral again to toggle its boxes off; click `p.N` to jump to that numeral's box on that sheet. +1 test (188 layer0).
- 2026-07-22 · RT-4/PE-2 · **Numeral coverage & consistency check** (Julian's ask — spotting OCR misses / doc gaps). `figures_map.numeral_coverage` reconciles the spec text vs the OCR'd drawings and flags three RELIABLE signals: **recited-not-drawn** (a figure-tied numeral OCR found on no sheet — the OCR-miss flag), **drawn-not-recited** (OCR read a number the spec never recites — artefact/unlabelled), and **figure-mismatches** (discussed with FIG N in text but not OCR-located on FIG N's sheet — catches the separator-10→FIG-4 case Julian spotted, via `numeral_text_figures` which sees ALL mentions, not just the first). **The naive 'all consecutive numbers present?' check was built and found UNRELIABLE for patents** (they skip reference numerals by design; quantities like '220° F' and OCR misreads corrupt the range → 85+ false gaps) — 4th instance of L0006, so it's computed but demoted to a caveated weak signal, not a flag. Rendered as a 'Numeral coverage & consistency' section; all D10-worded (facts, never §112). +2 tests. 187 layer0.
- 2026-07-22 · RT-4 · **Figures-view review upgrades** (Julian's asks). (1) **All-figure references:** `figures_map.numeral_figures` lists EVERY figure a numeral is OCR-located in (shared components — the separator 10 → FIGS 1,2,5,3C — no longer collapse to 'first discussed'); a **show: first discussed / all figures** toggle in the figures view. (2) **OCR bounding boxes:** the manifest now carries each numeral's full bbox (x,y,w,h); clicking a numeral draws a padded **confirmation box** on its sheet(s) at the OCR position, for at-a-glance verification. Manifest re-OCR'd (bbox-carrying); `NumeralSighting.bbox` (None-safe for legacy manifests). 5 new hermetic tests. 185 layer0.
- 2026-07-22 · RT-4 · **DONE — figures in the document view (the payoff).** `relevant_figures` selects a cited span's figures (explicit FIG refs + recited standalone numerals → OCR sightings → sheets → figures); `build_evidence_view.py` auto-wires the patent FigurePanel catalog + per-interaction `.figures`. Verified live: clicking an answer surfaces its drawing sheet in the doc pane with OCR-provenance caption; 11/23 interactions surface figures. No confidence floor (leader-line numerals read low but are real — a floor deletes signal, L0006). EDGAR inert. 3 new hermetic tests (incl. numeral boundary). 182 layer0.
- 2026-07-17 · D28/RT-4/PE-2 · **OCR shipped (ingestion-time, frozen manifest).** Prototype-first: Apple Vision read the 1995 sheets near-human. `ocr_patent_figures.py` → hashed `ocr_manifest.json` (conf + provenance + warnings); `cairn.figures_map` (pure over the manifest): FIG→sheet 8/8 (7 OCR + FIG 4 by flagged elimination), numerals 41/62 located; `element_numeral_issues` = PE-2's hard check at high precision (31 surfaced facts). Figures view wired (sheet tags, p.N locations, structural review). Bugs fixed en route: 'FIGS. 3 A-C' letter-range; D1-D6 dimension-label pollution. `[ocr]` optional extra (macOS, never CI). Hermetic tests: 12 new. Layer0 green.
- 2026-07-17 · D24–D27 · **Provability triage executed** (Julian adopted the recommendation). D24 BUILT: `cairn.cues` closed-set denial-cue scan + non-blocking evidence-view advisory + `scripts/census_denial_cues.py`; 8 new tests. **Census: EDGAR 5/5 benign boilerplate hits, patent 0 — cue-marked refutation is absent from both real corpora → D25 (the gate) DEPRIORITIZED ON EVIDENCE**, recorded as a negative result with a per-engagement re-check trigger. D26 (NLI stays Layer-E) + D27 (GL/FOL closed) logged. No contract bump (measurement, not guarantee). 173 layer0 green.
- 2026-07-16 · research · **Provability swarm ran** (run wf_0522ac48-3e1: 51 Opus 4.8 agents, 8 lenses → 42 candidates → hostile review → **0 clean survivors**; ~2.6M tokens). Synthesis filed as [`docs/provability_research.md`](docs/provability_research.md) (research input, authorizes no work) + illustrated explainer [`docs/provability_findings.html`](docs/provability_findings.html). Verdict: the $2M refutation case is NOT catchable deterministically in general (assertion-vs-denial is pragmatic; cue-less refutation permanently invisible) — except the lexically-marked span-local slice, which a closed cue-set scan can force to abstain (one-directional, monotonic). Staircase: **D24** cue-scan as measurement first → **D25** promote to Layer-0 abstain-trigger only if D24's false-positive rate allows → **D26** NLI/veridicality stays Layer-E (its numeric blind spot is CAIRN's flagship class) → **D27** GL/FOL recorded inapplicable. Honesty note: the $2M case is synthetic — the golden set's real trap is table arithmetic, hence measurement-first. **All four rows are CANDIDATES pending Julian's triage.**
- 2026-07-08 · RT-4 · **BUGFIX — the ≥10 numeral floor was deleting real evidence** (caught by Julian on review). "Patent reference numerals start at 10" is a folk rule, not a spec: US5447630A labels its FIG. 1 greywater sources *bathtub or shower 1, toilet 2, … dishwasher 4 and clothes washer 5* — `MIN_NUMERAL=10` silently dropped five real numerals, and my validation only checked that the ≥10 bindings I expected survived (confirmation bias — I never tested what the filter discarded). **The floor was the wrong mechanism entirely:** the `of claim 6` noise it was built to kill comes from the *claims region*, so `reference_numerals` now scans the **specification only** (structural, not numeric), rejects decimals (`0.24 mg/l` ≠ numeral 0) and unit-bearing quantities (`400 W`). Now permissive on value by policy: over-filtering deletes evidence *invisibly*, a loose element phrase is a visible hint. 62 numerals (was 60). Regression tests pin the single-digit recovery. 165 layer0 green.
- 2026-07-08 · docs · **Illustrated system explainers** (standing request: a visual per step). [`docs/system_overview.html`](docs/system_overview.html) — 16 sections, 8 SVG diagrams: the AI/deterministic split, span→atom→answer data model, the runtime loop + gate, the five-outcome decision tree, verify's four checks, the D12 abstention split (incl. the honest overlap finding), the audit hash chain + provenance, I1-I6, the truth contract's monotonic rule, Layer-0 vs Layer-E, the oracle freeze, the patent model. [`docs/rt4_and_next_steps.html`](docs/rt4_and_next_steps.html) — RT-4 mechanisms (numeral↔drawing chain, the ≥10 convention, contiguous-caption run), the D21 displayed-evidence-vs-citation boundary, the FIG→sheet wall, and the three next options. README points at the overview (clarity standard). Facts verified against the repo, not written from memory; dated last-verified line.
- 2026-07-08 · RT-4 · **Companion figures in the main evidence view (capability).** `evidence_view` gains `FigurePanel` + `Interaction.figures` + a sticky per-interaction figure strip in the document pane (corpus-agnostic, inert by default → EDGAR byte-unchanged; D21 displayed-evidence). CI-tested with a stub PNG (+3). Per-numeral patent wiring deferred: only sheet scans exist (no per-figure crops), so precise FIG→numeral targeting needs OCR/crops — not shipped coarse. 163 layer0 green.
- 2026-07-08 · RT-4 · **Figures visible (first increment).** Figure/numeral model (`parse_figures` / `figure_references` / `reference_numerals`, PE-1 remainder) — CI-tested + validated on US5447630A. `fetch_patent_figures.py` (full-res sheets → gitignored store, hashed manifest; picks full-res over the thumbnail by size) + `patent_figures_view.py` (drawings beside captions + numeral legend, numeral→FIG 'first discussed with', click-to-context). Displayed evidence, not citations (D21). 160 layer0 green.
- 2026-07-08 · PE-5 · **Golden patent set RATIFIED (D23).** All three judgment calls settled on the recommendations (P017 stays abstain · front-matter flips P013/P014 confirmed, P015 stays abstain · P007 stays answer). No label values changed — ratification froze the current state. Mechanically enforced: `ratified_manifest_sha256` stamped in the file + `test_ratified_labels_are_frozen` (Layer-0 CI) — tamper fails, append allowed, deletion caught (all three proven). PE-5 DONE. 154 layer0 green.
- 2026-07-06 · RT-1 · **Session delimiting (first increment).** `session_start_record` markers (adapter-owned ts; chain-covered, replay-neutral); MCP server appends one per spawn (+`CAIRN_SESSION_LABEL`); runner labels runs; `sessions_from_audit` grouping; `--sessions`/`--session N`/`--store` on the view script (the `--store` fix un-crashes engagement logs). Knowledge loop integrated same day (CLAUDE.md block + INDEX/LIBRARY, seeded with L0001). 155 local evals.
- 2026-07-06 · PE-4 · **Front matter + priority/regime (first increment).** `parse_front_matter` / `effective_filing` / `regime_flag` (date arithmetic over cited text; D10 note included). US5447630A re-ingested with its front page; golden P013/P014 → answerable, P015 stays abstain (no assignee on the front page); floor 6.1 → 6.2. **P007 diagnosed** (repeat miss, both runs): label is CORRECT — claim 15 recites the variable-speed fan at its tail; the agent is decoyed by claim 16's "control means for varying the speed" and never reads the long claim to its end. A real failure mode (claim-tail limitation + dependent-claim decoy), recorded in the baseline doc. 151 evals local.
- 2026-07-06 · Layer-E · **Patent run 2 (frame + locate-first refusals).** Frame adherence **23/23**; the coverage gate blocked **29 drafts** (37 verify calls → 23 ok) → entailment **1.00**, brier **0.0015** (from 0.82/0.17); refusals 6/6 clean, 0 errored. Cost: 2 conservative misses — P009 exposed **"a claim never names itself"** (locator-style constraints must be required:false; guidance fixed), P012 partial→abstain. P017 grounded-negative behavior repeated (re-label case strengthens). Baseline doc updated. ~$4.
- 2026-07-06 · M2-T8 · **Live constraint coverage — DONE; truth contract → 1.2.** The landscape doc's "highest-leverage open engine item": `verify` accepts the agent's question `frame`, deterministically checks the cited spans cover every required constraint (`coverage_for_answer` over containing spans, same semantics as the view), returns + logs frame/coverage (replayable, I6), and the loop presents **only if `ok AND coverage.complete`** (CLAUDE.md + `/ground`). `interactions_from_audit` renders logged frames. Tool-level acceptance: naive citation → verify ✓ / coverage ✗ / blocked. +4 tests → 149 local / 145 CI.
- 2026-07-05 · triage · **Landscape CANDIDATEs triaged** (user delegated): **(1) failure-mode taxonomy ACCEPTED + built** — brief §3 gains the taxonomy table (8 modes → mechanism → standing coverage; all modes already caught); golden negatives tagged `failure_mode` (`misgrounded` / `absent-fact`, 17 items across both sets) for per-mode Layer-E reporting; **(2) Review Report ACCEPTED as RT-5** (signable proof-of-process deliverable; prereqs RT-1 + intake Q13/Q16; folds in the client-facing truth-contract page); **(3) graded support score PARKED in Backlog** (advisory-only, never a gate; first `EntailmentProvider` shape per TC-3). Trend-file discrepancy resolved: the EDGAR Layer-E trend (`audit_log/layer_e_results.jsonl`) is now committed as the public eval record (gitignore re-include; engagement logs stay local).
- 2026-07-05 · docs · **Landscape-lessons research doc filed** → [`docs/landscape_lessons.md`](docs/landscape_lessons.md) (moved from root; registered in CLAUDE.md's hierarchy). External record (Stanford falsification, Charlotin database, Clearbrief/BriefCatch, Vals report, finance parallels) mapped onto D1–D22/I1–I6: **no standing decision contradicted**; three CANDIDATEs await human triage (failure-mode taxonomy tags; Review Report deliverable; advisory graded support score) + two positioning notes. Decision references verified against the log.
- 2026-07-04 · PE-5 · **Live patent Layer-E baseline.** Runner made per-engagement (`--golden/--store/--threshold/--audit-dir`; per-run MCP config; timeout → errored item not a crash; clean tool-less exits score normally — an instant refusal is not an error). Results ([`docs/layer_e_patent_baseline.md`](docs/layer_e_patent_baseline.md)): **refusal 6/6** (UPL boundary held), header-magnet absents caught by agent reasoning 4/4 (D12 live), decision 0.88 corrected, verify caught 15, brier 0.17. P017 label question flagged for ratification. ~$4 API.
- 2026-07-02 · PE-5 · **Patent golden set (first cut) + calibrated floor.** [`golden_patent.json`](golden_patent.json): 25 items across all five outcomes (incl. 6 refuse negatives) + 3 support-mapping expectations; quotes resolve; `tests/test_patent_golden.py` (4, skip in CI — local store). Calibrator (D20) fits **`CAIRN_SUPPORT_THRESHOLD = 6.1`**; finding: present/absent overlap (header-magnet 22.9, filter-trap 11.7) → content-absence catches only alien absents, the rest are agent-reasoning (D12), measured at Layer-E. 145 local / 141 CI evals.
- 2026-06-30 · D22 · **Refuse-to-adjudicate is a first-class outcome; truth contract → 1.1.** Layer-E gains the `refuse` class + `refusal_accuracy` (refusals no longer dilute abstention); evidence view renders it distinctly (orange badge/border); CLAUDE.md + `/ground` name the five outcomes. Replay now preserves recorded provenance stamps verbatim → 1.0-era and pre-provenance records replay byte-identically across the bump (TC-2's first real version transition, proven by test). +6 tests → 141 evals.
- 2026-06-30 · roadmap · Tracked **RT-4** — figures/diagrams visible in the document view (imperative for real patent work: drawings carry the reference numerals claims turn on). Spans ingestion (store figure images) + PE-1 (numeral→figure binding) + GUI (render images); figures are *displayed evidence* via typed SPEC→FIGURE / NUMERAL→ELEMENT edges, not text citations (D21).
- 2026-06-29 · TC-2 · **Provenance stamping (D21).** Every audit record now stamps a `provenance` block — the contract version + the methods that produced/checked it (`check_support` → contract/retrieval/threshold; `verify` → contract/verify-ops). `cairn.contract.CONTRACT_VERSION`, `Retriever.method`, `verify.OPS_VERSION`. Replay reads the recorded floor → non-default thresholds reproduce byte-identically (I6, actually *strengthened*). The evidence view shows the rigor line per card. Old records read as pre-provenance (additive). `tests/test_session.py` +1. 136 evals.
- 2026-06-29 · D21/TC-1 · **Truth contract declared + versioned (v1).** [`docs/truth_contract.md`](docs/truth_contract.md) states every guarantee → mechanism → layer (runtime/Layer-0/Layer-E) → strength, the deliberate `verified ≠ entailed` non-guarantee, the **monotonic rule** (strengthen freely; weaken only via a new major version + rationale + oracle) and the **upgrade ratchet** (swap behind a seam → Layer-0 holds + Layer-E improves-or-holds). New **Forward-compatibility** track: TC-1 done; TC-2 (provenance stamping) + TC-3 (interfaces only at a 2nd implementation) tracked. Forward-compat = declaration + provenance + the upgrade rule, never speculative frameworks (the trap).
- 2026-06-29 · PE-3 · **Reviewable claim-support artifact.** `scripts/patent_support_view.py` renders a claim's limitation→spec-support mapping into the two-pane evidence view (each limitation a card; click → its supporting paragraphs highlight in the spec; all citations verify-ok) + the dependency-check result. Generic/committable. Demoed on US5447630A claim 1 (8 limitations, all supported) and claim 2. Turns the PE toolkit into something a reviewer can actually use.
- 2026-06-29 · PE-2 · **Claim-dependency integrity check.** `patents.check_dependencies` flags missing / self / forward claim references (objective `StructuralIssue`s with the claim span; surfaces the fact, never a validity conclusion, D10). `tests/test_patents.py` (13); clean on US5447630A. The other three §112 checks (antecedent basis, element-numeral, term-consistency) **deferred** — a naive element-numeral heuristic flags 34/67 numerals on the real patent (mostly false positives), so they ship only at high precision. 135 evals.
- 2026-06-29 · PE-3 · **Claim-limitation → spec support mapping (first increment) — the headline deliverable.** `patents.map_claim_support` ranks the spec paragraphs supporting each limitation (typed `CLAIM_LIMITATION→SPEC_SUPPORT` edges, each an addressable span; deterministic BM25 over the paragraph set). An empty list is a *surfaced gap*, never a legal-sufficiency conclusion (D10). `tests/test_patents.py` (13); validated on US5447630A (claim 1 limitations → ¶11/¶47/¶50). Remaining: figure/numeral edges, document-identity, per-corpus floor, golden items (PE-5). 133 evals.
- 2026-06-29 · PE-1 · **Spec-paragraph addressability.** `patents.parse_paragraphs` → `Paragraph` spans (native `[0042]` numbering, else line-per-paragraph in the description region; claims excluded). The spec side of PE-3's claim→spec mapping. `tests/test_patents.py` (11); validated on US5447630A (64 paragraphs, all resolve). Claims + limitations + paragraphs now all addressable → **PE-3 unblocked**. 131 evals.
- 2026-06-29 · PE-1 · **Claim limitation decomposition.** `patents.decompose_claim` → `Limitation` spans (transition-word + semicolon split; flat for nested sub-elements, `wherein` → one). Each limitation is an addressable, hash-verified span — the input PE-3's claim→spec support mapping consumes. `tests/test_patents.py` (7); validated on US5447630A (claim 1 → 8 limitations). 129 evals.
- 2026-06-29 · D20 · **Support-floor calibrator.** `support.calibrate_threshold(golden, retriever)` + pure `fit_floor` fit the `check_support` floor from the golden set's present-vs-content-absent score separation (traps excluded, D12); deterministic, emits a value to record (not auto-floated). `scripts/calibrate_threshold.py`. Rediscovered EDGAR's hand-set ~15 from labels (15.1; answerable ≥19.7, absent ≤10.5). Query-relative outlier alternative parked as a Layer-E experiment (Backlog). `tests/test_support.py` +3. 126 evals.
- 2026-06-29 · PE-1/hands-on · **Validated on the real engagement patent (US5447630A, public).** Fetched + ingested into a gitignored per-engagement store; `parse_claims` parsed all **22 claims** (6 independent: 1/10/12/15/18/21; dependency chains correct), every span resolving hash-verified — the synthetic-fixture model generalized unchanged. **Finding:** `check_support`'s EDGAR 15.0 floor over-abstains on patents (relevant spans score 5.8–14.2; ranking still correct) → made the floor **per-engagement configurable** (`default_registry(..., support_threshold=)` / `build_server` / `CAIRN_SUPPORT_THRESHOLD` env; default 15.0, EDGAR unchanged). `tests/test_tools.py` +1; proper value calibrates at PE-5. 123 evals.
- 2026-06-29 · PE-1 · **Patent claim model (first increment).** `src/cairn/patents.py` `parse_claims` parses a patent's canonical text into addressable `Claim` objects (number, exact char span, independent/dependent, parsed `depends_on`) — a post-ingest layer over the shared `SpanStore` (spans resolve hash-verified, I3). Hermetic synthetic fixture `corpus/samples/sample_patent.txt`; `tests/test_patents.py` (4). Locate-only (D10). Remaining PE-1: limitations, spec paragraphs, numerals, front-matter, priority chain. 122 evals.
- 2026-06-28 · VER-1/D19 · **`verify` relational checks, slice 2.** Added `gt`/`ge`/`lt`/`le`/`eq` + `within_range` (boolean-result derivations confirming a numeric relation between cited operands) — the claim-numerical-limitation case. Boundary D10 at its sharpest: a numeric fact, never an infringement/novelty conclusion (`within_range`, not `infringes`). `equation()` + MCP enum + `tests/test_verify.py` (+3). Verified on the CLI. Remaining VER-1: unit conversion + the adjudication negative-test. 118 evals.
- 2026-06-28 · VER-1/D18 · **`verify` math, first slice.** Added `multiply`/`divide`/`ratio`/`percent_change` to the derived-atom recompute (alongside sum/subtract), matched to the asserted value rounded to its written decimals (div-by-zero/bad-arity → not ok); `equation()` + MCP `operation` enum updated; `tests/test_verify.py` +3. Boundary D10 (evidence, not adjudication). Remaining: unit conversion + compare/within-range. 115 evals.
- 2026-06-28 · RT-2 · **Generic plain-text ingester (CLI half).** `cairn.ingest.files` (`ingest_paths`/`ingest_file`/`normalize_text`, corpus-agnostic; the only corpus-specific ingester is still `edgar.py`) + `scripts/ingest_files.py --store … [--kind]`. Stand up a new engagement corpus from `.txt`/`.md`, then point `CAIRN_STORE` at it; HTML/PDF/patent-XML deferred to a PE-1 adapter. `tests/test_ingest_files.py` (6). Verified end-to-end (ingest → retrieve). 112 evals.
- 2026-06-28 · roadmap · Tracked **VER-1** — extend `verify`'s verifiable-computation set beyond sum/subtract (multiply/divide/percent/ratio/min-max-avg/count/round/date_delta/compare + unit conversion), driven by the patent engagement (claim ranges, units, counts). Stays deterministic recompute-from-cited-operands (D9/I6); must not cross into adjudication (D10). Gets a D# at design.
- 2026-06-28 · D13/GUI · **Table figures light their column header.** When a cited figure sits in a financial row, the evidence view now also highlights the matching date cell in the nearest period-header above it (`_column_header` maps numeric-column index → same-index date cell) — so it's visually clear which period a figure belongs to (the connecting-clause idea, D13, applied to columns). `test_table_figure_lights_its_column_header`. 106 evals.
- 2026-06-28 · RT-3 · **Corrections render distinctly.** Evidence view presents (highlights + answer card) for all D16 presenting classes (answer/correction/partial), not just `answer`; `correction` gets its own violet colour on badge + active border. `verify` gained an optional `outcome` (logged, replay-safe I6; read by `interactions_from_audit`); `CLAUDE.md`/`/ground` pass it. Curated demo #6 is a real grounded correction. +2 tests → 105 evals.
- 2026-06-28 · roadmap · Tracked three user GUI ideas as a **Review & corpus tooling** section: RT-1 organized session history, RT-2 corpus-management GUI, RT-3 distinct color/rendering for corrections (found the cause: view highlights only `kind=="answer"`, uses legacy `reject`, and `--from-audit` hard-tags `answer`).
- 2026-06-28 · DX · `build_evidence_view.py --from-audit` gained `--latest` / `--last N` (the audit log is cumulative, so a per-question review needs the most recent interaction[s]); `/ground` now uses `--latest`. Fixes the cluttered 14-card review after a `/ground`.
- 2026-06-28 · roadmap · Tracked the Layer-E baseline follow-ups as first-class tasks: **LE-1** (plural-and-ranked, the G008 miss) and **LE-2** (calibration + judge-strictness split), so they don't get lost.
- 2026-06-28 · D17 · **Desktop integration (DX).** CAIRN now drives from Claude Code Desktop: `build_evidence_view.py --from-audit` rebuilds the evidence view from a live session's audit log (`evidence_view.interactions_from_audit`, re-runs verify; `tests/test_evidence_view.py`), a `/ground` slash command runs the loop + review in one step, and `docs/desktop_setup.md` documents connect → ask → review. M5 React replay deferred (D17). 103 evals.
- 2026-06-28 · D16 · **Grounded correction is a first-class outcome.** Layer-E scorer reworked to four classes (answer / abstain / correction / partial) derived from the golden fields (no seed edits); `correction`/`partial` expect a presentation, only `abstain` stays silent. Added `judge_refutes_premise` (did a correction actually refute?) and renamed `Verdict.entails`→`yes` (now a generic verdict). Headline metric is `decision_accuracy` + per-class rates. `tests/test_layer_e.py` → 14. Fixes G020 being mis-scored as a miss. 102 evals.
- 2026-06-28 · M4-T4/M2-T6 · **DONE.** Live agent loop ran the full golden set end-to-end through the MCP tools (auth via `claude -p --bare` + ANTHROPIC_API_KEY, 0 errored); first Layer-E baseline captured ([`docs/layer_e_baseline.md`](docs/layer_e_baseline.md)): abstention 0.86 (7/7 once G020's grounded false-premise rejection is credited), answer 0.92, verify caught 21 ungrounded drafts, entailment 0.69, Brier 0.30. Fixed `claude_ask` to use `--bare` (judge was 401ing → bogus non-entailment). Focus → M5-T1.
- 2026-06-27 · M4-T4 · Layer-E live smoke attempted → surfaced an auth blocker: headless `claude -p` returns 401 in this sandbox. Hardened `run_layer_e.py`: prompt as `-p` value + `bypassPermissions`, `stdin=DEVNULL`, and an empty tool-call segment is now reported as an ERROR (not a silent abstention) with an auth/MCP hint; exits non-zero if nothing scored. Live run needs an authenticated headless env.
- 2026-06-27 · GUI · Fixed the evidence-view scroll coupling: the document pane's scroll extent was driven by the answers column's length (sticky `.doc` inside a grid row sized by `.cards`). Body is now a flex column with a fixed-height `.layout`; each pane scrolls independently (`height:100%; overflow:auto`).
- 2026-06-27 · M2-T6 · Layer-E judge + calibration built: `judge_entailment` (LLM-as-judge over cited spans, pluggable `ask` → gate-tested logic), `brier_score` + `reliability`, and `claims_and_spans` to pull (claim, spans) from a logged verify record. `run_layer_e.py --live` now also judges entailment + calibrates over the agent's stated `Confidence: 0.NN` (new CLAUDE.md loop convention) → full metric set in one pass. `tests/test_layer_e.py` → 12. 100 evals.
- 2026-06-27 · M4-T4/M2-T6 · Wired the live agent loop + Layer-E scaffolding: `.mcp.json` registers the CAIRN MCP server (logs to the audit log, I5; all 7 tools advertised); `cairn.layer_e` scores abstention/verify-catches deterministically from the audit log (`tests/test_layer_e.py`); `scripts/run_layer_e.py` drives the agent (`--live`, billed) → trend file. Live run + LLM-judge + calibration are the model/periodic step (not executed — awaiting go).
- 2026-06-27 · D14 · **Structure-aware normalization (cell-coherent), safely.** `edgar.normalize` now keeps table cells coherent (block breaks inside `td`/`th` → spaces), so the balance-sheet column header is one line (`September 28, 2024 September 30, 2023`) instead of split. Phase 0 first de-hardcoded test offsets (derive via `search_corpus`) so the re-hash self-validated: re-ingest + rebuild toy → **all 88 tests green, zero re-tuning** (only header cells moved; data-row golden quotes unchanged → resolution invariant held). Demo now binds the "as of" date to the header **534 chars above the figure** (nearest substantiation, D14) instead of the cover page. Aligned table columns (display) now sit under a coherent header. Remaining polish: true header-over-column alignment (real `<table>`) — optional.
- 2026-06-27 · M2-T7+ · **Interactive evidence view (D15):** clean by default; clicking a card lights only that interaction's highlights (both panes) + draws a tight bounding box per contiguous cluster (G007 → 2 boxes), and scrolls to it. Cross-column label terms (D13) now share the teal mark. Earlier: derivation equations shown (decision line + hover). `tests/test_evidence_view.py` rewritten. Also: CLAUDE.md given operational protocol + module map (working mode, definition-of-done, gate command) after a second agent skipped the bookkeeping.
- 2026-06-26 · M4-T2/T3 · MCP tool contracts + boundary (second agent; reviewed + merged). verify tool via answer_from_json/result_to_json (replayable record); per-tool JSON-Schemas advertised by the MCP adapter; structural read/write asymmetry (only write tools close over the log). `tests/test_tools.py` contracts + `tests/test_boundary.py`. 86 evals green. Focus → M4-T4.
- 2026-06-26 · M2-T7+ · Parallel view readability: document pane now renders with light hierarchy (ALL-CAPS headings, ":"-subsections, body) — display-only, no canonical change. D14 recorded (prefer nearest substantiation; needs a normalization fix for the split balance-sheet date — next).
- 2026-06-26 · M2-T7+ · Evidence view upgraded to a **parallel full-document view**: whole canonical doc on the left with cited ranges marked in situ (overlap-merged), interactions on the right, click-to-scroll; auto-scrolls to the first highlight. `tests/test_evidence_view.py` updated.
- 2026-06-26 · M4-T1 · MCP server + CLI scaffold: shared `cairn.tools` registry; `python -m cairn` / `cairn` CLI; guarded `mcp_server.build_server` (optional `mcp` dep). `tests/test_tools.py`; 72 evals green.
- 2026-06-26 · M3 · **Audit log DONE.** M3-T1 tamper-evident append-only hash-chain log (`cairn.audit`); M3-T2 byte-identical replay (`cairn.session`); M3-T3 I4 read/write-asymmetry test. Layer-0 gate's I4/I5 rows filled. 64 evals green. Focus → M4 (Layer-E/M2-T6 + M2-T8 finish around M4 when the agent drives the tools).
- 2026-06-26 · M2-T8 (first cut) · D13 question-frame + constraint coverage: `cairn.frame` (`check_coverage`, deterministic) + tests; evidence view shows a question-coverage strip incl. a *naive-citation* card where verify ✓ but coverage ✗ (wrong metric, real figure). Live-agent frame emission + verify-gate integration remain (M4).
- 2026-06-26 · M2-T7+ · Review feedback on the GUI: (1) header now links each doc's SEC filing + canonical text; (2) **dates are now load-bearing atoms** — `verify` flags an ungrounded "as of <date>"; the demo binds the period to the cover line (extends D9); (3) each card shows a deterministic decision trace (scores/floor/verdicts). The chain-of-thought question is logged for M3 (agent rationale → audit log) — see response.
- 2026-06-25 · M2-T7 · Evidence-view GUI (`cairn.evidence_view`, D8): server-less two-pane HTML — answers with click-to-source highlights, unbound flagged, abstentions with closest spans. `scripts/build_evidence_view.py` → `evidence_view.html`; standing `tests/test_evidence_view.py`.
- 2026-06-25 · M2-T5 · Layer-0 gate formalized: `docs/layer0_gate.md` gate table; `layer0` marker auto-applied via conftest; CI step "Layer-0 deterministic gate" (`pytest -m layer0`). 39 deterministic evals, 0.70s. I4/I5 join at M3.
- 2026-06-25 · M2-T4 · Expanded CLAUDE.md runtime loop against built tool contracts (contract table + Answer shape; two-mechanism abstention D12, read-freely D11, refuse-to-adjudicate D10).
- 2026-06-25 · M2-T3 · Plural-and-ranked (brief §4): confirmed `check_support` surfaces all qualifying spans ranked (G007/G008 both current+non-current lines, uncollapsed). Standing test added.
- 2026-06-25 · M2-T2 · `check_support` (`cairn.support`, I2): relevance-floor abstention trigger; ranked/plural supporting spans or `insufficient` + closest. D12 reconciliation: deterministic trigger owns content-absence (G011/12/13 → insufficient); semantic traps are the agent's job (Layer-E). Standing `tests/test_support.py`.
- 2026-06-25 · M2-T1 · `verify` atom resolver (`cairn.verify`, D9/I1): exact-offset literal match + hash re-verify (I3) + stale-binding-hash check, independent figure re-extraction (untagged figures flagged), derived-value recomputation. Standing `tests/test_verify.py`. Persistence → M3.
- 2026-06-25 · — · D11: the agent reads freely; grounding constrains output, not input. Added `SpanStore.get_document` (full hash-verified text) + test; planned for M4 MCP surface + brief §5; fixed the CLAUDE.md loop wording ("ground every sentence in a verified span", not "draft only from returned spans"); reconciled D9's anti-fishing as offset-specific + relevance-judged.
- 2026-06-25 · M1-T3 · Retrieval (`cairn.retrieval`): deterministic BM25 (unigram+bigram) behind a corpus-agnostic `RetrievalBackend`; multi-doc-ready `Retriever`; embedding half deferred (seam open). I6 reproducibility test; recall@20 12/13 (G009 lexical gap → M2). **M1 DONE.**
- 2026-06-25 · — · D10: reconciled the patent retarget into the roadmap. EDGAR reframed as the reference build; new **Patent Engagement** track (PE-0…PE-6, mechanism-agnostic core per tailoring §11) gated on client intake (blockers Q2 mechanism, Q17 confidentiality); locate-never-adjudicate boundary; CLAUDE.md hierarchy + brief alignment. Current focus unchanged (M1-T3, shared engine).
- 2026-06-25 · M1-T2 · Span store (`cairn.spans`): char-offset chunking, `get_span` with hash re-verify (I3), `resolve_quote` enforcing the resolution invariant (D7). All 21 golden quotes bound 1:1 to canonical text; G016 split; standing `tests/test_spans.py`.
- 2026-06-24 · M1-T1 · EDGAR ingestion adapter: `cairn.ingest` (Document + content-hash I3, store with verify-on-load, isolated `edgar.py`). Apple FY2024 10-K ingested to `corpus/store/`; standing I3 tests (tamper rejected, deterministic normalization, evidence preserved). build_toy_corpus reuses the adapter's normalize.
- 2026-06-24 · — · D8: highlighted two-pane evidence-view GUI pulled forward to **M2-T7** (server-less static-HTML renderer on the verify result); M5 becomes its polished log-replay upgrade. Contract: agent tags spans → verify → log → deterministic render (hyperlinks are verified span refs, not hand-authored). Brief §6 + M5 reworded.
- 2026-08-14 · defect · D62: evidence-view question taken from the record's own frame, not the preceding check_support (23/23 were wrong).
- 2026-08-14 · defect · D63: an answer whose live verify fails is withheld with a per-atom diagnosis, not presented with a warning.
- 2026-08-14 · defect · D64: 'as shown at 20' recognised as a numeral recitation; located-not-recited 21 → 19.
- 2026-08-14 · defect · D65: I3 drift check made mandatory; read tools return the corpus hash. Contract 2.1.
- 2026-08-15 · defect · D66: adjudicate queue renders a zoomed sheet crop per located row; y-flip routed through the tested box_to_display.
- 2026-08-15 · D67 · abstain + refuse render in the evidence view; engagement log cleared and re-run with six questions across all five outcome classes.
